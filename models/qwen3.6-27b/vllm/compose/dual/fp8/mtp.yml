# ===========================================================================
# Profile (at-a-glance):
#   Model:     Qwen3.6-27B FP8 (near-lossless 8-bit; embedded mtp.fc head)
#   Topology:  Dual 3090 (TP=2, NVLink auto-detected via NVLINK_MODE)
#   Drafter:   MTP n=3 (built-in — the fp8 checkpoint embeds the mtp.* head)
#   KV:        fp8 → e4m3 (1 byte/token; runs at scale=1.0 — checkpoint is weight-only —
#              → FlashInfer backend → flat decode at depth). NOT fp8_e5m2 — see "NOT enabled" note.
#   Vision:    yes
#   Max ctx:   262K — fp8 KV keeps the full ctx at TP=2. Validation proxy for
#              the TP=4 multi-max (vllm/qwen-27b-multi-max, same config).
#   Genesis:   none — intentionally Genesis-free
#   Status:    ⚠️ Production w/ caveats
#   Caveats:   MTP drafter exposed to OPEN vllm#50021 (GDN spec-decode crash
#              class, quant-independent). Mitigate: SPEC=off. v0.26.0 predates
#              the fix. Detail: Upstream-risk below + docs/UPSTREAM.md row.
#   Upstream-risk:  • ⚠️ MTP × hybrid-GDN wild write — vllm#50021, OPEN and LIVE IN THIS PIN.
#                The GDN spec-decode path builds a state-block index from an UNBOUNDED
#                accepted-token count; a zero/stale count dereferences a wild address →
#                `CUDA error: unspecified launch failure` + Xid 13/31 and a dead worker,
#                typically after minutes of agent-shaped traffic rather than at boot. Not
#                quant-, topology- or depth-specific: hit on Ampere+Ada (#758) and on
#                2× 5090 across NVFP4 AND W4A8 alike (#838). The vendored pr48375 fixes
#                vllm#43559 (corruption) and does NOT cover this fault.
#                MITIGATION: run the drafter off — delete the --speculative-config
#                line below. Lower n cuts frequency, NOT exposure (#758 called
#                n=3 stable; #838 crashed AT n=3). Clears when
#                #50021 merges and a pin bump inherits it — v0.26.0 PREDATES it.
#                Detail + re-test trigger: docs/UPSTREAM.md.
#              ⤷ Status flip TAKEN 2026-08-01 (maintainer call): moved to ⚠️
#                while vllm#50021 is open; the summary now lives on the
#                required `Caveats:` line above. Reverts to the prior status
#                when a fix-carrying pin bump lands (trigger on the
#                docs/UPSTREAM.md row).
#   Quality:   109/150 (--full, thinking-off, 2026-07-06, jesse #594) — TIES int8-PTH's 107/150
#              (+2, within noise). Quality-neutral despite scale=1.0: `calculate_kv_scales` is
#              disabled on Qwen3-Next hybrid (GDN recurrent state → unreliable warmup scales),
#              so 1.0 is the only fp8 KV; e4m3's exponent range tolerates it. NIAH also ties → 240K.
#   Best for:  max-accuracy Qwen on 2 cards, and the validation proxy for the 4-card
#                vllm/qwen-27b-multi-max — FP8 + fp8 KV at full 262K ⭐
# ---------------------------------------------------------------------------
# Dual RTX 3090 — Qwen3.6-27B FP8 "max accuracy" tier (vllm/qwen-27b-dual-max).
# FP8 weights + MTP n=3 + fp8 KV + vision. The validation proxy for the 4-card
# vllm/qwen-27b-multi-max (same config @ TP=4). NOTE: some prose below is inherited from the
# AutoRound "fast" template — the at-a-glance block above is authoritative for this variant.
#
# What this gives you (vs the single-card default):
#   - TP=2 across both 3090s
#   - max_num_seqs=2 → 2 concurrent agents at full 262K context (KV pool 2.36×)
#   - max_model_len=262144 → model's natural max, no rope_scaling needed
#   - vision + tools + MTP n=3 + recall — everything on, no compromises
#   - Measured (canonical 262K 2-boot mean, v0.25.1, 2026-07-20 #662 A/B): decode ~69 narr / ~90 code
#     single-stream — DECODE-TIED with the fast tier (fast ~72/90). The prior 83/108 (v0.24.0 2026-06-30)
#     was cross-session inflation; single boots swing ~5 TPS on code, so do not compare 1-draw figures.
#     max degrades at full ctx (1.13× KV pool). TTFT 158/167 ms, ~21.4 GB/card.
#     NOTE: the decode 83/108 above is the int8-PTH short-prompt bench. With fp8/e4m3 KV
#     (#594): decode stays flat at depth (114→115 @35K, 2.3× int8-PTH), 8-pack 109/150. See BENCHMARKS.md.
#
# What's intentionally NOT enabled:
#   - int8-PTH KV — switched to fp8 (e4m3) KV. Why: int8-PTH is confined to the TRITON_ATTN
#     backend, whose single-stream decode craters with context (measured 130.8 → 50.7 TPS @ 35K);
#     fp8/e4m3 KV routes to FlashInfer → decode stays flat (~114 → 115 @ 35K, ≈2.3×) at ~2× prefill
#     and equal NIAH recall to 240K + 8-pack quality tie (109 vs 107) (jesse, #594). ⚠️ Must be `fp8`
#     (= e4m3, runs at scale=1.0 — the checkpoint is weight-only, and `calculate_kv_scales` is disabled
#     on Qwen3-Next hybrid: GDN recurrent state → unreliable warmup scales, so 1.0 is the only fp8 KV).
#     NOT `fp8_e5m2` — scale-free and hard-rejected with FP8 checkpoints
#     (`ValueError: fp8_e5m2 kv-cache is not supported with fp8 checkpoints`). Don't "simplify" to e5m2.
#     Trade vs int8-PTH: ~14% slower short-ctx (<5K) + tighter VRAM margin (509 MB free @ deepest soak
#     fill, 0 MiB growth / 100% retention / p50 125.5 — soak-continuous PASS 2026-07-06).
#     For 4-stream concurrency at 262K, switch to
#     dual/turbo.yml (which uses TurboQuant KV + Genesis P65).
#
# Dependencies:
#   - 2× RTX 3090 (Ampere SM 8.6), PCIe-only (no NVLink — works fine)
#   - vLLM STABLE image (v0.24.0, from the `vllm-stable` engine profile — the version lives
#     in install.spec, #254; the launcher injects it over the compose default) — no source overlays
#     required (validated 2026-05-25: marlin-pad / PR-35936 / PR-41800 all
#     unnecessary on a current vLLM; AutoRound INT4 TP=2 + MTP + vision boot clean).
#
# Qwen3.6-27b vLLM dual/multi configs (post-2026-05-31 prune — settled on one
# dual config; the A/B variants were deprecated):
#
#   Slug               Ctx    Streams  KV    Vision  Topology   Notes
#   vllm/dual (this)   262K   2        fp8   ✅      2× PCIe    THE dual default ⭐
#   vllm/dual4         262K   4        fp8   ✅      4× PCIe    4-card
#   vllm/dual4-dflash  262K   2        FP16  ✅      4× PCIe    4-card, peak code TPS
#   (deprecated 2026-05-31, recover from git if needed: dual-dflash, dual-dflash-noviz,
#    dual-tq3-nomtp, dual-bf16, dual-int8 — A/B leftovers superseded by vllm/dual.
#    dual-turbo / dual-tq3-mtp* were already deprecated.)
#   (NVLink rigs: any dual compose auto-detects NVLink at boot — no separate
#    nvlink-* variant. NVLINK_MODE=force_on forces it if auto-detect misses.)
#
# Run:
#   cd <repo>/models/qwen3.6-27b/vllm/compose
#   docker compose -f dual/docker-compose.yml up -d
# ===========================================================================
# Hardware metadata (parsed by scripts/preflight.sh):
# Requires-min-vram-gb: 24
# Engine-profile: vllm-stable
# Requires-min-gpu-count: 2
# Tensor-parallel: 2
services:
  vllm-qwen36-27b-dual:
    # Pinned to a vLLM STABLE release (immutable — never purged from Docker Hub,
    # unlike nightlies; this is the #407 pin-drift fix). Genesis-free + fp8 KV
    # means no patch overlays are anchored here, so we ride stable releases.
    # Bump the tag to a newer stable after a verify-stress + soak gate.
    # Override with VLLM_IMAGE=... if you need a specific build.
    image: ${VLLM_IMAGE:-vllm/vllm-openai:v0.25.1}
    container_name: "${ESTATE_CONTAINER:-vllm-qwen36-27b-dual-max}"
    restart: ${CLUB3090_RESTART:-unless-stopped}
    ports:
      - "${BIND_HOST:-0.0.0.0}:${ESTATE_PORT:-${PORT:-8013}}:8000"
    volumes:
      - ${MODEL_DIR:-../../../../../../models-cache}:/root/.cache/huggingface
      # torch.compile + Triton kernel caches — first boot warms (~60-90 sec);
      # subsequent boots reuse cached graphs. Pattern from Sander's PROD launch.
      # Closes club-3090 #22.
      - ../../../cache/torch_compile:/root/.cache/vllm/torch_compile_cache
      - ../../../cache/triton:/root/.triton/cache
      # NOTE: vLLM source overlays (marlin-pad #40361, PR-35936 required-tool
      # fallback, PR-41800 truncate_prompt_tokens) were dropped 2026-05-25 — all
      # confirmed unnecessary on stable v0.21.0 (PR-41800 merged upstream; the
      # other two don't trip on a current vLLM). Keeping zero source mounts is
      # what makes this path pin-drift-immune. See docs/UPSTREAM.md.
      # froggeric/Qwen-Fixed-Chat-Templates qwen3.6 — fixes 7 default-template
      # bugs (empty <think></think> spam, </thinking> hallucination, unclosed
      # think before tool call, no-user-query crash, developer role, etc.).
      # See docs/UPSTREAM.md "Community templates / model assets" + the row at
      # https://huggingface.co/froggeric/Qwen-Fixed-Chat-Templates
      - ../../../patches/froggeric-chat-template/chat_template.jinja:/etc/qwen-froggeric-chat-template.jinja:ro
      # NVLink auto-detection — runs inside container at boot.
      - ../../../../../../scripts/detect_nvlink.sh:/etc/club3090/detect_nvlink.sh:ro
      # Vendored vllm#48375 (MambaManager drop_eagle_block) — inert under the
      # prefix-off default; protects PREFIX_CACHE_ARG users. See patches.yml.
      - ../../../patches/vllm-pr48375-mamba-drop-eagle-block:/etc/club3090/pr48375:ro
    environment:
      - NVIDIA_VISIBLE_DEVICES=${ESTATE_GPUS:-${NVIDIA_VISIBLE_DEVICES:-all}}
      # CUDA-level GPU mask — the runtime-agnostic half of GPU selection
      # (#610): launch.sh --gpus exports GPU UUIDs here. CDI runtimes (NixOS,
      # nvidia-ctk cdi) IGNORE NVIDIA_VISIBLE_DEVICES, so this mask is what
      # actually pins cards there; on the classic runtime the UUID form is
      # renumbering-proof. Unset → absent (no masking).
      - CUDA_VISIBLE_DEVICES
      - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN:-}
      - HF_HUB_OFFLINE=${HF_HUB_OFFLINE:-${OFFLINE:-0}}
      - TRANSFORMERS_OFFLINE=${TRANSFORMERS_OFFLINE:-${OFFLINE:-0}}
      - VLLM_WORKER_MULTIPROC_METHOD=spawn
      - NVLINK_MODE=${NVLINK_MODE:-auto}
      - NCCL_CUMEM_ENABLE=0
      - NCCL_P2P_DISABLE=1
      - VLLM_NO_USAGE_STATS=1
      # FlashInfer sampler OFF: measured -3.0% narrative / -1.7% code decode on the
      # 2x3090 flagship (bench.sh, 5 measured runs/arm, symmetric boots, single
      # variable). With MTP spec-decode active, sampling routes through the
      # rejection-sampler path where the FlashInfer kernel adds overhead without a
      # win. Also trims sampler workspace, which is load-bearing on 24 GB (same
      # reasoning as nemotron dual/w4a16/turbo.yml). Reported by @Aduer in #671.
      - VLLM_USE_FLASHINFER_SAMPLER=0
      - OMP_NUM_THREADS=1
      # DeepGEMM (vLLM's fp8-GEMM path) is built for Hopper (sm_90) + datacenter
      # Blackwell (sm_100/103). Consumer Blackwell (5090 / PRO 6000 / GB10,
      # sm_120/121) has no recipe and hard-fails "recipe not found" at boot
      # (disc #571). Pass-through: switch.sh/launch.sh auto-inject
      # VLLM_USE_DEEP_GEMM=0 on those cards; if you run raw `docker compose` on a
      # consumer Blackwell card, set it yourself (VLLM_USE_DEEP_GEMM=0). Unset =
      # vLLM's arch default (DeepGEMM stays on where it works).
      - VLLM_USE_DEEP_GEMM
      # expandable_segments:True crashes boot on some setups (likely cuMemMap path).
      # Known: JusefPol on NVLink (PR #31), WSL2 single-card 3090 Ti.
      # Override via .env: PYTORCH_CUDA_ALLOC_CONF=expandable_segments:False
      - PYTORCH_CUDA_ALLOC_CONF=${PYTORCH_CUDA_ALLOC_CONF:-expandable_segments:True,max_split_size_mb:512}
    shm_size: "16gb"
    ipc: host
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    entrypoint:
      - bash
      - -c
      - |
        # VLLM_ENFORCE_EAGER=1 in compose/.env disables CUDA graphs — use on
        # hardware where Cliff 2 GDN activation spikes occur at runtime
        # (~50-65K active context tokens). Costs ~20-30% TPS in exchange for
        # stability. See docs/HARDWARE.md "Note for WSL2 / Windows users".
        # NVLink auto-detection (sets NCCL env vars, _NVLINK_ENABLED).
        source /etc/club3090/detect_nvlink.sh
        bash /etc/club3090/pr48375/install.sh || exit 1
        if [ "$${_NVLINK_ENABLED:-0}" = "1" ]; then
          exec vllm serve $${VLLM_ENFORCE_EAGER:+--enforce-eager} "$$@"
        else
          exec vllm serve $${VLLM_ENFORCE_EAGER:+--enforce-eager} --disable-custom-all-reduce "$$@"
        fi
      - --
    command:
      - --model
      - /root/.cache/huggingface/qwen3.6-27b-fp8
      - --served-model-name
      - qwen3.6-27b
      - qwen3.6-27b-autoround
      - --quantization
      - fp8
      - --dtype
      - bfloat16
      - --tensor-parallel-size
      - "${TP:-2}"
      - --pipeline-parallel-size
      - "${PP:-1}"
      - --max-model-len
      - "${MAX_MODEL_LEN:-262144}"
      - --gpu-memory-utilization
      - "${GPU_MEMORY_UTILIZATION:-0.92}"
      - --max-num-seqs
      - "2"
      - --max-num-batched-tokens
      - "${MAX_NUM_BATCHED_TOKENS:-8192}"
      - --kv-cache-dtype
      - "${KV_CACHE_DTYPE:-fp8}"
      - --trust-remote-code
      # froggeric chat-template override (see volumes block + docs/UPSTREAM.md)
      - --chat-template
      - /etc/qwen-froggeric-chat-template.jinja
      - --reasoning-parser
      - qwen3
      - --default-chat-template-kwargs
      - '{"enable_thinking": false}'
      - --enable-auto-tool-choice
      - --tool-call-parser
      - qwen3_coder
      # Prefix-caching ON by default (flipped 2026-07-17, #710/#736): the
      # vllm#43559 fix (vllm#48375) is VENDORED on this compose and applied at
      # boot, plus 7-day production + 6-arm probe evidence of the pair clean on
      # v0.25.x. Opt out: PREFIX_CACHE_ARG=--no-enable-prefix-caching
      - "${PREFIX_CACHE_ARG:---enable-prefix-caching}"
      - --enable-chunked-prefill
      - --speculative-config
      - '{"method":"mtp","num_speculative_tokens":3}'
      - --override-generation-config
      - '{"temperature":${TEMP:-${TEMPERATURE:-0.6}},"top_p":${TOP_P:-0.95},"top_k":${TOP_K:-20},"min_p":${MIN_P:-0.0},"repetition_penalty":${REPEAT_PENALTY:-1.0}}'
      - --host
      - 0.0.0.0
      - --port
      - "8000"
