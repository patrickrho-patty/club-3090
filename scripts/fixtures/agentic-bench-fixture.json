[
  {
    "turn": 1,
    "user_msg": "Run: ls <repo>/ && echo \"---\" && ls <repo>/scripts/ 2>/dev/null && echo \"---\" && ls <",
    "tool_name": "Bash",
    "tool_result": "Exit code 2\nAGENTS.md\nBENCHMARKS.md\nCHANGELOG.md\nCONTRIBUTING.md\ndocs\nLICENSE\nmodels\nmodels-cache\nREADME.md\nresults\nscripts\ntools\n---\nbench.sh\nhealth.sh\nlaunch.sh\npower-cap-sweep.sh\npreflight.sh\nreport.sh\nsetup.sh\nsoak-helper.py\nsoak-test.sh\nswitch.sh\nupdate.sh\nverify-full.sh\nverify-stress.sh\nverify.sh\n---",
    "chars": 307
  },
  {
    "turn": 2,
    "user_msg": "Run: ls <repo>/models/ && echo \"---\" && ls <repo>/docs/",
    "tool_name": "Bash",
    "tool_result": "gemma-4-31b\nqwen3.6-27b\n---\nARCHITECTURE.md\nCLIFFS.md\nCOMPARISONS.md\nCONTAINER_RUNTIMES.md\nDUAL_CARD.md\nengines\nEXAMPLES.md\nFAQ.md\nGLOSSARY.md\nHARDWARE.md\nimg\nINFERENCE_ENGINES.md\nKV_MATH.md\nMULTI_CARD.md\nSINGLE_CARD.md\nSTRUCTURED_COT.md\nUPSTREAM.md",
    "chars": 249
  },
  {
    "turn": 3,
    "user_msg": "Run: nvidia-smi --query-gpu=index,name,memory.total,memory.used,memory.free,clocks.gr",
    "tool_name": "Bash",
    "tool_result": "0, NVIDIA GeForce RTX 3090, 24576 MiB, 23198 MiB, 1342 MiB, 1860 MHz, 350.20 W, N/A\n---\n \tGPU0\t\n GPU0\tX\t\n\nLegend:\n\n  X    = Self\n  OK   = Status Ok\n  CNS  = Chipset not supported\n  GNS  = GPU not supported\n  TNS  = Topology not supported\n  NS   = Not supported\n  U    = Unknown\n",
    "chars": 278
  },
  {
    "turn": 4,
    "user_msg": "Read <repo>/scripts/bench.sh and tell me what's relevant to the issue.",
    "tool_name": "Read",
    "tool_result": "1\t#!/usr/bin/env bash\n2\t#\n3\t# Canonical bench against the running vLLM service.\n4\t#   - Runs both the canonical narrative AND code prompts in one invocation.\n5\t#     This matches the README's narrative/code TPS pairing.\n6\t#   - 3 warmup + N measured runs per prompt (default 5 narrative + 5 code).\n7\t#   - per-run: wall time, TTFT (via streaming), completion tokens,\n8\t#     wall_TPS (= comp / wall), decode_TPS (= comp / (wall - TTFT))\n9\t#   - per-prompt summary: mean / std / CV for both TPS metrics + mean TTFT\n10\t#   - shows MTP SpecDecoding metrics from docker logs at the end\n11\t#\n12\t# Why two TPS metrics:\n13\t#   - wall_TPS  = \"user-perceived speed\" (includes prefill cost)\n14\t#   - decode_TPS = \"model decode rate\" (excludes prefill)\n15\t#   For long prompts the two can differ a lot. For short prompts they\n16\t#   converge. Reporting both keeps comparisons honest across configs.\n17\t#\n18\t# Why narrative + code:\n19\t#   MTP acceptance varies wildly by prompt structure. Code (repetitive,\n20\t#   token-predictable) accepts at ~80% per position; prose (semantically\n21\t#   rich) at ~50%. Reporting only one half is misleading. README claims\n22\t#   like \"66 / 85 TPS\" pair them; bench should too.\n23\t#\n24\t# Prereq: stack is running and reports \"Application startup complete\".\n25\t#\n26\t# Env vars:\n27\t#   URL                Endpoint. Default: http://localhost:8020\n28\t#   MODEL              Served model name. Default: qwen3.6-27b\n29\t#   CONTAINER          Container for log scraping. Default: vllm-qwen36-27b\n30\t#   RUNS               Measured runs per prompt. Default: 5\n31\t#   WARMUPS            Warm-up runs (shared across both). Default: 3\n32\t#   PROMPT_NARR        Override narrative prompt\n33\t#   PROMPT_CODE        Override code prompt\n34\t#   MAX_TOKENS_NARR    Default: 1000\n35\t#   MAX_TOKENS_CODE    Default: 800\n36\t#   ONLY               Set to \"narr\" or \"code\" to skip the other. Default: both\n37\t#   QUIET              Set to 1 to skip per-run lines (just print summary)\n38\t#\n39\t# Usage:\n40\t#   bash scripts/bench.sh\n41\t#   ONLY=code bash scripts/bench.sh\n42\t#   RUNS=10 bash scripts/bench.sh\n43\t\n44\tset -euo pipefail\n45\t\n46\t# Auto-detect running container + port (URL/CONTAINER env vars still win).\n47\t# See scripts/preflight.sh::preflight_autodetect_endpoint.\n48\tROOT_DIR=\"$(cd -- \"$(dirname -- \"${BASH_SOURCE[0]}\")/..\" && pwd)\"\n49\tif [[ -f \"${ROOT_DIR}/scripts/preflight.sh\" ]]; then\n50\t  # shellcheck source=preflight.sh\n51\t  source \"${ROOT_DIR}/scripts/preflight.sh\"\n52\t  preflight_autodetect_endpoint\n53\tfi\n54\tURL=\"${URL:-http://localhost:8020}\"\n55\tMODEL=\"${MODEL:-qwen3.6-27b}\"\n56\tCONTAINER=\"${CONTAINER:-vllm-qwen36-27b}\"\n57\tRUNS=\"${RUNS:-5}\"\n58\tWARMUPS=\"${WARMUPS:-3}\"\n59\tMAX_TOKENS_NARR=\"${MAX_TOKENS_NARR:-1000}\"\n60\tMAX_TOKENS_CODE=\"${MAX_TOKENS_CODE:-800}\"\n61\tPROMPT_NARR=\"${PROMPT_NARR:-Write a detailed 800-word essay explaining transformer attention.}\"\n62\tPROMPT_CODE=\"${PROMPT_CODE:-Write a Python implementation of quicksort with comments explaining each step.}\"\n63\tONLY=\"${ONLY:-both}\"\n64\tQUIET=\"${QUIET:-0}\"\n65\t\n66\tneed() {\n67\t  command -v \"$1\" >/dev/null 2>&1 || { echo \"ERROR: '$1' not in PATH.\" >&2; exit 1; }\n68\t}\n69\tneed curl\n70\tneed python3\n71\t\n72\tif ! curl -sf \"${URL}/v1/models\" >/dev/null; then\n73\t  echo \"ERROR: service not reachable at ${URL}/v1/models\" >&2\n74\t  echo \"  Start with: cd compose && docker compose up -d\" >&2\n75\t  exit 1\n76\tfi\n77\t\n78\tpython3 - \"$URL\" \"$MODEL\" \"$WARMUPS\" \"$RUNS\" \"$QUIET\" \"$ONLY\" \\\n79\t            \"$PROMPT_NARR\" \"$MAX_TOKENS_NARR\" \\\n80\t            \"$PROMPT_CODE\" \"$MAX_TOKENS_CODE\" << 'PYEOF'\n81\timport json, sys, time, urllib.request, statistics as s\n82\t\n83\t(URL, MODEL, WARMUPS, RUNS, QUIET, ONLY,\n84\t PROMPT_NARR, MAX_NARR, PROMPT_CODE, MAX_CODE) = sys.argv[1:]\n85\tWARMUPS = int(WARMUPS); RUNS = int(RUNS); QUIET = int(QUIET) == 1\n86\tMAX_NARR = int(MAX_NARR); MAX_CODE = int(MAX_CODE)\n87\t\n88\tdef run_once(prompt, max_tokens):\n89\t    body = json.dumps({\n90\t        \"model\": MODEL,\n91\t        \"messages\": [{\"role\": \"user\", \"content\": prompt}],\n92\t        \"max_tokens\": max_tokens,\n93\t        \"temperature\": 0.6,\n94\t        \"top_p\": 0.95,\n95\t        \"stream\": True,\n96\t        \"stream_options\": {\"include_usage\": True},\n97\t        \"chat_template_kwargs\": {\"enable_thinking\": False},\n98\t    }).encode()\n99\t    req = urllib.request.Request(f\"{URL}/v1/chat/completions\", data=body,\n100\t                                 headers={\"Content-Type\": \"application/json\"})\n101\t    t_send = time.time()\n102\t    ttft = None\n103\t    completion_tokens = 0\n104\t    with urllib.request.urlopen(req, timeout=600) as r:\n105\t        for line in r:\n106\t            line = line.decode(\"utf-8\", errors=\"ignore\").rstrip()\n107\t            if not line.startswith(\"data: \"):\n108\t                continue\n109\t            payload = line[6:]\n110\t            if payload == \"[DONE]\":\n111\t                break\n112\t            try:\n113\t                chunk = json.loads(payload)\n114\t            except json.JSONDecodeError:\n115\t                continue\n116\t            choices = chunk.get(\"choices\") or []\n117\t            if choices:\n118\t                delta = choices[0].get(\"delta\", {})\n119\t                content = delta.get(\"content\") or delta.get(\"reasoning_content\")\n120\t                if content and ttft is None:\n121\t                    ttft = time.time() - t_send\n122\t            usage = chunk.get(\"usage\")\n123\t            if usage:\n124\t                completion_tokens = usage.get(\"completion_tokens\", completion_tokens)\n125\t    t_end = time.time()\n126\t    wall = t_end - t_send\n127\t    if ttft is None:\n128\t        ttft = wall\n129\t    return wall, ttft, completion_tokens\n130\t\n131\tdef fmt(label, wall, ttft, toks):\n132\t    decode_t = max(wall - ttft, 1e-6)\n133\t    wtps = toks / wall if wall > 0 else 0\n134\t    dtps = toks / decode_t\n135\t    line = f\"  {label:<10s} wall={wall:6.2f}s  ttft={ttft*1000:6.0f}ms  toks={toks:>4d}  wall_TPS={wtps:6.2f}  decode_TPS={dtps:6.2f}\"\n136\t    return wtps, dtps, ttft, line\n137\t\n138\tdef stats(name, xs, unit=\"\"):\n139\t    m = s.mean(xs)\n140\t    sd = s.stdev(xs) if len(xs) > 1 else 0\n141\t    cv = (sd / m * 100) if m > 0 else 0\n142\t    return f\"  {name:<14s} mean={m:7.2f}{unit}   std={sd:6.2f}   CV={cv:4.1f}%   min={min(xs):.2f}   max={max(xs):.2f}\"\n143\t\n144\tdef run_set(label, prompt, max_tokens):\n145\t    print(f\"\\n========== {label.upper()} (prompt={len(prompt)} chars, max_tokens={max_tokens}) ==========\")\n146\t    print(f\"=== warmups ({WARMUPS}) ===\")\n147\t    for i in range(WARMUPS):\n148\t        try:\n149\t            w, t, k = run_once(prompt, max_tokens)\n150\t            _, _, _, line = fmt(f\"warm-{i+1}\", w, t, k)\n151\t            if not QUIET:\n152\t                print(line)\n153\t        except Exception as e:\n154\t            print(f\"  warm-{i+1}  FAIL: {e}\")\n155\t    print(f\"\\n=== measured ({RUNS}) ===\")\n156\t    walls, decodes, ttfts = [], [], []\n157\t    for i in range(RUNS):\n158\t        try:\n159\t            w, t, k = run_once(prompt, max_tokens)\n160\t            wtps, dtps, ttft, line = fmt(f\"run-{i+1}\", w, t, k)\n161\t            if not QUIET:\n162\t                print(line)\n163\t            walls.append(wtps); decodes.append(dtps); ttfts.append(ttft)\n164\t        except Exception as e:\n165\t            print(f\"  run-{i+1}  FAIL: {e}\")\n166\t    if walls:\n167\t        print(f\"\\n=== summary [{label}] (n={len(walls)}) ===\")\n168\t        print(stats(\"wall_TPS\",   walls))\n169\t        print(stats(\"decode_TPS\", decodes))\n170\t        print(f\"  TTFT          mean={s.mean(ttfts)*1000:6.0f}ms  std={s.stdev(ttfts)*1000 if len(ttfts) > 1 else 0:5.0f}ms  min={min(ttfts)*1000:.0f}ms  max={max(ttfts)*1000:.0f}ms\")\n171\t\n172\tif ONLY in (\"both\", \"narr\"):\n173\t    run_set(\"narrative\", PROMPT_NARR, MAX_NARR)\n174\tif ONLY in (\"both\", \"code\"):\n175\t    run_set(\"code\", PROMPT_CODE, MAX_CODE)\n176\tPYEOF\n177\t\n178\t# GPU state\n179\tif command -v nvidia-smi >/dev/null 2>&1; then\n180\t  echo \"\"\n181\t  echo \"=== GPU state ===\"\n182\t  nvidia-smi --query-gpu=index,utilization.gpu,memory.used,memory.total,power.draw,temperature.gpu \\\n183\t             --format=csv,noheader\n184\tfi\n185\t\n186\t# MTP / spec-decode stats\n187\tif command -v docker >/dev/null 2>&1 && docker inspect \"${CONTAINER}\" >/dev/null 2>&1; then\n188\t  echo \"\"\n189\t  echo \"=== Last 3 SpecDecoding metrics ===\"\n190\t  docker logs \"${CONTAINER}\" 2>&1 | grep \"SpecDecoding metrics\" | tail -3 || true\n191\tfi\n192\t",
    "chars": 8373
  },
  {
    "turn": 5,
    "user_msg": "Run: docker logs vllm-qwen36-27b-bounded-thinking 2>&1 | grep -E \"(enforce|cuda_graph",
    "tool_name": "Bash",
    "tool_result": "[INFO:genesis.apply_all]   [ON ] P40                TQ k8v4 GQA grouping kernel (vllm#40792)\n[INFO:genesis.apply_all]   [ON ] P82                SGLang-style acceptance threshold OR-clause\n[INFO:genesis.apply_all]           gain: 0% TPS, but ~1-2 GiB total VRAM headroom\n[INFO:genesis.apply_all]           why: Verified ~1 GiB VRAM saved per GPU on 35B-A3B-FP8 + MTP K=3. Use freed VRAM for higher gpu-mem-util or longer ctx.\n[INFO:genesis.apply_all] [Genesis] skipped: P5b KV page-size pad-smaller-to-max (env-opt-in) — opt-in: set GENESIS_ENABLE_P5B=1 to enable pad-smaller-to-max KV page-size strategy (saves ~34% per-block VRAM on Qwen3.6 hybrid vs P5 v1 LCM-pad-up; BLAST-RADIUS is KV allocator → benchmark on VM 100 before enabling in prod)\nINFO 05-08 20:08:16 [nixl_utils.py:20] Setting UCX_RCACHE_MAX_UNRELEASED to '1024' to avoid a rare memory leak in UCX when using NIXL.\nWARNING 05-08 20:08:17 [interface.py:689] Using 'pin_memory=False' as WSL is detected. This may slow down the performance.\n[INFO:genesis.dispatcher] [Genesis Dispatcher] APPLY PN59 — Streaming-GDN orchestrator (Variant D Phase 2) — true Cliff 2b OOM fix | opt-in env (config: neutral)\n[INFO:genesis.wiring.text_patch] [PN59 streaming GDN orchestrator (Variant D Phase 2)] applied 1 sub-patches: pn59_chunk_orchestrator_dispatch\n[INFO:genesis.apply_all] [Genesis] applied: PN59 streaming GDN orchestrator — PN59 applied: streaming-GDN dispatcher inserted; runtime engages when single-seq long-T (eliminates Cliff 2b OOM)\n[INFO:genesis.dispatcher] [Genesis Dispatcher] SKIP  PN54 — GDN contiguous-call deduplication (P0.7 Cliff 2b OOM mitigation) | opt-in only — set GENESIS_ENABLE_PN54_GDN_CONTIGUOUS_DEDUP=1 to engage\n[INFO:genesis.apply_all] [Genesis] skipped: PN54 GDN contiguous dedup (P0.7 Cliff 2b) — opt-in only — set GENESIS_ENABLE_PN54_GDN_CONTIGUOUS_DEDUP=1 to engage\n[INFO:genesis.apply_all] [Genesis] applied: P66 cudagraph_capture_sizes spec-decode divisibility filter — P66 applied: cudagraph_capture_sizes will be filtered to sizes divisible by uniform_decode_query_len when spec-decode is active. Boot 2-4x faster, less peak GPU memory, no mixed-q_len capture risks.\n[INFO:genesis.dispatcher] [Genesis Dispatcher] APPLY P82 — SGLang threshold_single OR-clause acceptance (BIASED — opt-in research) | opt-in env (config: neutral)\n[INFO:genesis.apply_all] [Genesis] skipped: P82 SGLang threshold_single OR-clause acceptance (BIASED — opt-in research) — upstream drift marker 'sample_recovered_tokens_kernel' in /usr/local/lib/python3.12/dist-packages/vllm/v1/sample/rejection_sampler.py — upstream may have absorbed this fix or independent threshold patch\n[INFO:genesis.wiring.text_patch] [P83 v1/core/single_type_kv_cache_manager.py — MTP keep-last-cached-block (vllm#38182 mitigation)] applied 2 sub-patches: p83_full_attention_skip_pop, p83_sliding_window_skip_pop\n[INFO:genesis.apply_all] [Genesis] applied: P103 FLA Cliff 2 chunked fwd_h+fwd_o orchestrator — text-patch=applied (chunk.py self-install hook appended (survives `exec vllm serve` + worker spawn)); setattr already idempotent (chunk.py self-install fired or prior apply still active in this process)\n[INFO:genesis.apply_all] [Genesis] applied: PN35 inputs_embeds optional for text-only — PN35 v7.69 applied: inputs_embeds buffer now skipped on text-only models (gpu_model_runner + llm_base_proposer spec-decode proposer). Saves ~64 MiB GPU + ~64 MiB pinned CPU per site on Qwen3.6-27B at max_num_tokens=4096. Auto-retires when upstream vllm#35975 lands. Particularly useful on borderline-OOM configs (single-24GB-GPU + long context + spec-decode, WSL2 setups with extra display overhead). NO-OP on multimodal models — guard preserves original allocation when supports_mm_inputs=True.\n[INFO:genesis.apply_all] [Genesis] applied: PN33 spec-decode warmup K-aware (vllm#37521 extended) — PN33 applied: spec-decode warmup uses real num_speculative_tokens instead of dummy K=1. Closes (a) ampersandru mid-stream OOM via propose_draft_token_ids and (b) noonghunna workspace-lock AssertionError on TQ + MTP K=3 single-card. Disable via GENESIS_DISABLE_PN33_SPEC_DECODE_WARMUP_K=1 if warmup OOMs.\n[INFO:genesis.dispatcher] [Genesis Dispatcher] SKIP  PN32 — GDN _forward_core chunked-prefill v2 (Cliff 2 fix for single-24GB-GPU OOM) | opt-in only — set GENESIS_ENABLE_PN32_GDN_CHUNKED_PREFILL=1 to engage\n[INFO:genesis.apply_all] [Genesis] skipped: PN32 GDN chunked-prefill (Cliff 2 fix) — opt-in only — set GENESIS_ENABLE_PN32_GDN_CHUNKED_PREFILL=1 to engage\n[INFO:genesis.dispatcher] [Genesis Dispatcher] APPLY PN12 — FFN intermediate scratch pool — Cliff 1 fix on TQ3 path | opt-in env (config: neutral)\n[INFO:genesis.wiring.text_patch] [PN12 model_executor/layers/activation.py — SiluAndMul forward_cuda FFN intermediate pool (Cliff 1 fix on TQ3)] applied 1 sub-patches: pN12_silu_and_mul_pool\n[INFO:genesis.apply_all] [Genesis] applied: PN12 FFN intermediate scratch pool (Cliff 1 fix) — PN12 applied: SiluAndMul.forward_cuda now acquires output via FFNIntermediateCache pool when GENESIS_ENABLE_PN12_FFN_INTERMEDIATE_POOL=1. Closes Cliff 1 OOM on TQ3 path that PN8 couldn't address (different memory class).\n[INFO:genesis.apply_all] [Genesis] applied: P15B FA varlen max_seqlen_k clamp on TQ path (Issue #15 fix) — P15B applied: _flash_attn_varlen now clamps max_seqlen_k to actual cu_seqlens_k span. Prevents 50 MiB FA wrapper workspace OOM on long-context continuation-prefill (Issue #15). Adds one GPU->CPU sync per call on infrequent path.\n[INFO:genesis.dispatcher] [Genesis Dispatcher] APPLY PN25 — SiluAndMul.forward_native opaque-op pool (Cliff 1 mech B compile path) | opt-in env (config: neutral)\n[INFO:genesis.wiring.text_patch] [PN25 model_executor/layers/activation.py — SiluAndMul forward_native opaque-op pool (Inductor-safe Cliff 1 mech B)] applied 2 sub-patches: pN25_silu_and_mul_import_time_register, pN25_silu_and_mul_forward_native_opaque\n[INFO:genesis.apply_all] [Genesis] applied: PN25 SiluAndMul.forward_native opaque-op pool (Cliff 1 mech B compile-path) — PN25 applied: SiluAndMul.forward_native now dispatches through genesis::silu_and_mul_pooled opaque custom op so torch.compile/Inductor cannot inline the FFN intermediate alloc. Closes Cliff 1 mech B on inductor-compiled FFN forward (club-3090#16 reproducer class). Sister to PN12 — PN12 covers eager forward_cuda, PN25 covers compile forward_native; both share the same FFNIntermediateCache pool and can be enabled simultaneously without conflict.\n[INFO:genesis.wiring.text_patch] [PN13 compilation/cuda_graph.py — CUDAGraphWrapper gc.collect/empty_cache lambda arity fix (vllm#41235)] upstream marker 'patch(\"gc.collect\", lambda *args, **kwargs: None)' detected — patch obsolete, skip\n[INFO:genesis.apply_all] [Genesis] skipped: PN13 CUDAGraphWrapper lambda arity (vllm#41235 backport) — PN13 compilation/cuda_graph.py — CUDAGraphWrapper gc.collect/empty_cache lambda arity fix (vllm#41235): upstream_merged — marker 'patch(\"gc.collect\", lambda *args, **kwargs: None)' present\n[INFO:genesis.apply_all] [Genesis] applied: PN17 FA2 softmax_lse runtime clamp (Issue #11 Cliff 1 mechanism A) — PN17 applied: FA2 softmax_lse buffer now clamped to actual seqused_k at runtime, freeing 50-100 MiB on long-ctx (Cliff 1 mechanism A fix per noonghunna Genesis Issue #11).\n[INFO:genesis.wiring.p40_tq_grouped_decode] [Genesis P40] rebound vllm.v1.attention.ops.triton_turboquant_decode.triton_turboquant_decode_attention (opt-in GQA-grouped decode stage1 for k8v4; fallback to scalar for MSE presets)\n[INFO:genesis.apply_all] [Genesis] applied: P40 TurboQuant GQA-grouped decode stage1 (opt-in) — module-level fn wrapped (dispatcher to grouped kernel)\n[INFO:genesis.wiring.p38_tq_continuation_memory] [Genesis P38] rebound TurboQuantAttentionImpl._continuation_prefill (persistent K_full/V_full buffers replace torch.cat peak)\n[INFO:genesis.apply_all] [Genesis] skipped: P7 GDN dual-stream in_proj parallelism — deferred — incompatible with torch.compile fullgraph (CUDA streams not SymPy-graphable); custom op implementation required. Re-enable with GENESIS_ENABLE_P7=1 + --enforce-eager.\nPN59  | APPLY  | Streaming-GDN orchestrator (Variant D Phase 2 | opt-in env (config: neutral)                                 | Genesis-original 2026-05-05, V\nPN54  | SKIP   | GDN contiguous-call deduplication (P0.7 Cliff | opt-in only — set GENESIS_ENABLE_PN54_GDN_CONTIGUOUS_DEDUP=1 | Genesis-original 2026-05-04, i\nP82   | APPLY  | SGLang threshold_single OR-clause acceptance  | opt-in env (config: neutral)                                 | SGLang team (sgl-project/sglan\nPN32  | SKIP   | GDN _forward_core chunked-prefill v2 (Cliff 2 | opt-in only — set GENESIS_ENABLE_PN32_GDN_CHUNKED_PREFILL=1  | Genesis-original v7.69 v2 (202\nPN12  | APPLY  | FFN intermediate scratch pool — Cliff 1 fix o | opt-in env (config: neutral)                                 | Genesis-original 2026-04-29 —",
    "chars": 8912
  },
  {
    "turn": 6,
    "user_msg": "Run: ls <repo>/results/ 2>/dev/null | head -20 && echo \"---\" && cat <repo>/scripts/sw",
    "tool_name": "Bash",
    "tool_result": "grammar-ab-20260503-224235\ngrammar-full-20260504-003118-gpu0\ngrammar-full-20260504-003118-gpu1\ngrammar-full-lcb-20260504-021003\nlucebox-dual-gpu-20260504-142832\nlucebox-k8v4-20260504-144647\nlucebox-pflash-niah-20260504-145351\nlucebox-pflash-niah-20260504-150321\nlucebox-pflash-niah-q8k-20260504-150600\nreport-soak-20260507-185348\nreport-soak-20260507-201956\nreport-soak-20260508-150734\nreport-soak-20260508-162225\nreport-soak-20260508-210153\nv0.20-migration\n---\n#!/usr/bin/env bash\n#\n# Switch between club-3090 compose variants.\n#\n# Brings down whatever's currently running, brings up the new variant,\n# and (optionally) waits for the server to report ready on /v1/models.\n# Stateless — re-run any time you want a different config.\n#\n# Usage:\n#   bash scripts/switch.sh <variant>           # switch + tail until ready\n#   bash scripts/switch.sh <variant> --no-wait # switch and return immediately\n#   bash scripts/switch.sh --list              # show all variants\n#   bash scripts/switch.sh --down              # just bring down whatever's up\n#\n# Variant names (engine/file, file is the docker-compose.<file>.yml stem):\n#\n#   Single-card vLLM:\n#     vllm/default            48K + TQ3 + MTP + vision + tools (recommended)\n#     vllm/long-vision        198K + TQ3 + vision (cliff-safe; Cliff 2 single-prompt >50K still applies)\n#     vllm/long-text          180K + TQ3 + MTP + text-only (Balanced MTP — 60K single-prompt closed via v7.69 + #35975)\n#     vllm/long-text-no-mtp   200K + TQ3 + no MTP + text-only (Max-context — same Cliff 2 closure, more KV pool, slower decode)\n#     vllm/bounded-thinking   180K + TQ3 + structured-CoT FSM in reasoning (recommended grammar: DeepSeek scratchpad — 87.4% combined HE+/LCB v6)\n#     vllm/tools-text         75K + fp8 + MTP + text-only (IDE agents — Cline / Cursor)\n#     vllm/minimal            32K + fp8 (no Genesis, no spec-decode, simplest)\n#\n#   Dual-card vLLM (TP=2):\n#     vllm/dual             262K + fp8 + 2 streams + vision (recommended dual)\n#     vllm/dual4            262K + fp8 + 4 streams + vision (4× 3090 PCIe baseline)\n#     vllm/dual4-dflash     262K + FP16 + DFlash N=5 + 2 streams + vision (4× 3090 code)\n#     vllm/dual-turbo       262K + TQ3 + 4 streams + vision (multi-tenant)\n#     vllm/dual-dflash      185K + FP16 + DFlash N=5 + vision (peak code TPS)\n#     vllm/dual-dflash-noviz 200K + FP16 + DFlash N=5 + no vision (peak code, max ctx)\n#     vllm/dual-nvlink          262K + fp8 + 2 streams + vision (REQUIRES NVLink bridge — community/experimental)\n#     vllm/dual-nvlink-turbo    262K + TQ3 + 4 streams + vision (REQUIRES NVLink bridge — community/experimental)\n#     vllm/dual-nvlink-dflash   185K + FP16 + DFlash N=5 + vision (REQUIRES NVLink bridge — community/experimental)\n#     vllm/dual-nvlink-dflash-noviz 188K + FP16 + DFlash N=5 + no vision (REQUIRES NVLink bridge — community/experimental)\n#     vllm/gemma-mtp        Gemma-4-31B + Google MTP drafter (32K, bf16 KV, vision — community/experimental, pre-merge)\n#\n#   Single-card llama.cpp:\n#     llamacpp/default      Q3_K_XL + 262K + q4_0 KV + vision (max ctx, no cliffs)",
    "chars": 3106
  },
  {
    "turn": 7,
    "user_msg": "Run: docker logs vllm-qwen36-27b-bounded-thinking 2>&1 | grep -E \"(Application startu",
    "tool_name": "Bash",
    "tool_result": "[INFO:genesis.apply_all] [Genesis registry] 126 dispatcher entries — schema-clean, dependency graph consistent.\n[INFO:genesis.apply_all]           why: club-3090#51 boot log: `CUDAGraphMode.FULL_AND_PIECEWISE is not supported with spec-decode for FlashInferBackend; setting cudagraph_mode=PIECEWISE`. P100 backports vllm#41127 to route uniform query_len>1 batches through prefill wrapper in cudagraph mode. Bit-identical, +5-10% TPS expected.\n[INFO:genesis.wiring.text_patch] [P27 Qwen3 BEFORE-THINK fallback] applied 3 sub-patches: p27_nonstream_capture, p27_nonstream_return_pr35687, p27_stream_start\n[INFO:genesis.wiring.text_patch] [P5 KV cache page size unification (v1_lcm_pad_max)/p5_import_math] anchor not found — soft skip (sibling patches continue)\n[INFO:genesis.wiring.text_patch] [P5 KV cache page size unification (v1_lcm_pad_max)] applied 1 sub-patches: p5_v1_lcm_pad_max_from_baseline\n[INFO:genesis.apply_all] [Genesis] applied: P5 KV cache page size unification — text-patch v2 succeeded (pad-smaller-to-max)\n[INFO:genesis.dispatcher] [Genesis Dispatcher] SKIP  P65 — TurboQuant spec-decode cudagraph downgrade | opt-in only — set GENESIS_ENABLE_P65_TURBOQUANT_SPEC_CG_DOWNGRADE=1 to engage\n[INFO:genesis.apply_all] [Genesis] skipped: P65 TurboQuant spec-decode cudagraph downgrade — opt-in only — set GENESIS_ENABLE_P65_TURBOQUANT_SPEC_CG_DOWNGRADE=1 to engage\n[INFO:genesis.dispatcher] [Genesis Dispatcher] APPLY P66 — cudagraph_capture_sizes spec-decode divisibility filter | opt-in env (config: neutral)\n[INFO:genesis.wiring.text_patch] [P66 config/vllm.py — cudagraph_capture_sizes spec-decode filter] applied 1 sub-patches: p66_size_filter\n[INFO:genesis.apply_all] [Genesis] applied: P66 cudagraph_capture_sizes spec-decode divisibility filter — P66 applied: cudagraph_capture_sizes will be filtered to sizes divisible by uniform_decode_query_len when spec-decode is active. Boot 2-4x faster, less peak GPU memory, no mixed-q_len capture risks.\n[INFO:genesis.dispatcher] [Genesis Dispatcher] SKIP  P78 — TurboQuant .tolist() capture-guard (adapted from noonghunna) | opt-in only — set GENESIS_ENABLE_P78_TOLIST_CAPTURE_GUARD=1 to engage\n[INFO:genesis.apply_all] [Genesis] skipped: P78 TurboQuant .tolist() capture-guard (adapted from noonghunna) — opt-in only — set GENESIS_ENABLE_P78_TOLIST_CAPTURE_GUARD=1 to engage\n[INFO:genesis.dispatcher] [Genesis Dispatcher] APPLY P100 — FlashInfer FULL CUDA graph for spec-decode (vllm#41127) | opt-in env (config: neutral)\n[WARNING:genesis.apply_all] [Genesis] DRIFT skipped: P100 FlashInfer FULL CUDA graph for spec-decode (vllm#41127) — P100 flashinfer.py — native FULL CG for spec-decode (vllm#41127): required_anchor_missing — sub-patch 'p100_build_query_len_scan_branch': anchor not found in file\n[INFO:genesis.dispatcher] [Genesis Dispatcher] SKIP  P95 — Marlin TP cudagraph cap on Ampere (vllm#40385) | opt-in only — set GENESIS_ENABLE_P95=1 to engage\n[INFO:genesis.apply_all] [Genesis] skipped: P95 Marlin TP cudagraph cap on Ampere (vllm#40385 backport) — opt-in only — set GENESIS_ENABLE_P95=1 to engage\n[INFO:genesis.apply_all] [Genesis] applied: P87 Marlin sub-tile output dim pad-on-load (vllm#40361 backport) — P87 v7.62.10 applied (TEXT-PATCH, 5 sub-patches): marlin.py MarlinLinearKernel.{can_implement, _maybe_pad_n (NEW), process_weights_after_loading prelude, apply_weights output slice} rewritten in-place. Sub-tile output dims now zero-padded at load + sliced at apply. Replaces v7.62 class-rebind which crashed under torch.dynamo cudagraph capture.\n[INFO:genesis.apply_all] [Genesis] applied: P38B P38 compile-safe in-source hook (Issue #14 fix) — P38b applied: text-patch applied (P38b in-source hook injected); dispatcher installed on TurboQuantAttentionImpl. _continuation_prefill now survives aot_compile_fullgraph capture. Fixes Issue #14 (noonghunna).\n[INFO:genesis.wiring.text_patch] [PN13 compilation/cuda_graph.py — CUDAGraphWrapper gc.collect/empty_cache lambda arity fix (vllm#41235)] upstream marker 'patch(\"gc.collect\", lambda *args, **kwargs: None)' detected — patch obsolete, skip\n[INFO:genesis.apply_all] [Genesis] skipped: PN13 CUDAGraphWrapper lambda arity (vllm#41235 backport) — PN13 compilation/cuda_graph.py — CUDAGraphWrapper gc.collect/empty_cache lambda arity fix (vllm#41235): upstream_merged — marker 'patch(\"gc.collect\", lambda *args, **kwargs: None)' present\n[INFO:genesis.apply_all] [Genesis] applied: P67b TurboQuant spec-verify forward() routing — P67b forward() spec-verify routing injected. K+1 batches now bypass _prefill_attention entirely (cudagraph-safe).\n[INFO:genesis.apply_all] [Genesis] applied: P58 async-scheduler -1 placeholder fix — P58 backport applied: 3 files modified, 0 already-applied. Async scheduler -1 placeholder leakage fixed; spec-decode + cudagraph workloads should no longer loop or IMA. Validate with our regression smoke suite before serving traffic.\n[INFO:genesis.dispatcher] [Genesis Dispatcher] SKIP  P57 — TQ spec-decode capture-safe buffers (deprecated — research artifact) | opt-in only AND empirically deprecated — keeping skip; set GENESIS_ENABLE_P57_SPEC_DECODE_CAPTURE_SAFE=1 only for diagno\n[INFO:genesis.apply_all] [Genesis] skipped: P57 TQ spec-decode capture-safe buffers — opt-in only AND empirically deprecated — keeping skip; set GENESIS_ENABLE_P57_SPEC_DECODE_CAPTURE_SAFE=1 only for diagnostics\n[INFO:genesis.apply_all] [Genesis] skipped: P7b GDN dual-stream via torch.library.custom_op (opt-in) — opt-in: set GENESIS_ENABLE_P7B=1 to enable GDN dual-stream via torch.library.custom_op (graph-safe alternative to P7; validates numeric equiv + compile-cache freshness before prod)\n[INFO:genesis.apply_all] [Genesis] skipped: P7 GDN dual-stream in_proj parallelism — deferred — incompatible with torch.compile fullgraph (CUDA streams not SymPy-graphable); custom op implementation required. Re-enable with GENESIS_ENABLE_P7=1 + --enforce-eager.\n[WARNING:genesis.apply_all] [Genesis] ⚠️  P100 FlashInfer FULL CUDA graph for spec-decode (vllm#41127) — P100 flashinfer.py — native FULL CG for spec-decode (vllm#41127): required_anchor_missing — sub-patch 'p100_build_query_len_scan_branch': anchor not found in file\nP65   | SKIP   | TurboQuant spec-decode cudagraph downgrade    | opt-in only — set GENESIS_ENABLE_P65_TURBOQUANT_SPEC_CG_DOWN | Genesis-original (root cause f\nP66   | APPLY  | cudagraph_capture_sizes spec-decode divisibil | opt-in env (config: neutral)                                 | Genesis-original (mirrors fhl2",
    "chars": 6495
  },
  {
    "turn": 8,
    "user_msg": "Run: RUNS=3 WARMUPS=1 bash scripts/bench.sh 2>&1",
    "tool_name": "Bash",
    "tool_result": "[autodetect] using running container=vllm-qwen36-27b-bounded-thinking url=http://localhost:8020  (skip: PREFLIGHT_NO_AUTODETECT=1)\n\n========== NARRATIVE (prompt=65 chars, max_tokens=1000) ==========\n=== warmups (1) ===\n  warm-1     wall= 30.84s  ttft=  1133ms  toks=1000  wall_TPS= 32.42  decode_TPS= 33.66\n\n=== measured (3) ===\n  run-1      wall= 28.55s  ttft=   167ms  toks=1000  wall_TPS= 35.03  decode_TPS= 35.23\n  run-2      wall= 28.99s  ttft=   168ms  toks=1000  wall_TPS= 34.50  decode_TPS= 34.70\n  run-3      wall= 28.65s  ttft=   167ms  toks=1000  wall_TPS= 34.91  decode_TPS= 35.11\n\n=== summary [narrative] (n=3) ===\n  wall_TPS       mean=  34.81   std=  0.28   CV= 0.8%   min=34.50   max=35.03\n  decode_TPS     mean=  35.02   std=  0.28   CV= 0.8%   min=34.70   max=35.23\n  TTFT          mean=   167ms  std=    1ms  min=167ms  max=168ms\n\n========== CODE (prompt=78 chars, max_tokens=800) ==========\n=== warmups (1) ===\n  warm-1     wall= 16.42s  ttft=   168ms  toks= 766  wall_TPS= 46.65  decode_TPS= 47.14\n\n=== measured (3) ===\n  run-1      wall= 17.33s  ttft=   164ms  toks= 795  wall_TPS= 45.86  decode_TPS= 46.30\n  run-2      wall= 17.85s  ttft=   169ms  toks= 800  wall_TPS= 44.83  decode_TPS= 45.26\n  run-3      wall= 16.82s  ttft=   166ms  toks= 800  wall_TPS= 47.58  decode_TPS= 48.05\n\n=== summary [code] (n=3) ===\n  wall_TPS       mean=  46.09   std=  1.39   CV= 3.0%   min=44.83   max=47.58\n  decode_TPS     mean=  46.54   std=  1.41   CV= 3.0%   min=45.26   max=48.05\n  TTFT          mean=   166ms  std=    2ms  min=164ms  max=169ms\n\n=== GPU state ===\n0, 74 %, 23108 MiB, 24576 MiB, 263.45 W, 67\n\n=== Last 3 SpecDecoding metrics ===\n(APIServer pid=1) INFO 05-09 14:48:30 [metrics.py:101] SpecDecoding metrics: Mean acceptance length: 3.39, Accepted throughput: 31.60 tokens/s, Drafted throughput: 39.60 tokens/s, Accepted: 316 tokens, Drafted: 396 tokens, Per-position acceptance rate: 0.947, 0.826, 0.621, Avg Draft acceptance rate: 79.8%\n(APIServer pid=1) INFO 05-09 14:48:40 [metrics.py:101] SpecDecoding metrics: Mean acceptance length: 3.44, Accepted throughput: 32.70 tokens/s, Drafted throughput: 40.20 tokens/s, Accepted: 327 tokens, Drafted: 402 tokens, Per-position acceptance rate: 0.940, 0.813, 0.687, Avg Draft acceptance rate: 81.3%\n(APIServer pid=1) INFO 05-09 14:48:50 [metrics.py:101] SpecDecoding metrics: Mean acceptance length: 3.58, Accepted throughput: 34.00 tokens/s, Drafted throughput: 39.60 tokens/s, Accepted: 340 tokens, Drafted: 396 tokens, Per-position acceptance rate: 0.977, 0.871, 0.727, Avg Draft acceptance rate: 85.9%",
    "chars": 2576
  },
  {
    "turn": 9,
    "user_msg": "Read <repo>/scripts/preflight.sh and tell me what's relevant to the issue.",
    "tool_name": "Read",
    "tool_result": "1\t#!/usr/bin/env bash\n2\t#\n3\t# Pre-flight checks library. Sourced by setup.sh and launch.sh — not run\n4\t# directly. Functions return 0 on success, 1 on failure (caller decides\n5\t# whether to exit). Soft warnings print and return 0.\n6\t#\n7\t# Functions:\n8\t#   preflight_docker          — docker binary + 'docker compose' subcommand work\n9\t#   preflight_gpu [min]       — nvidia-smi works, GPU detected, count >= min\n10\t#   preflight_disk <path> <gb>— free space at path covers <gb> gigabytes\n11\t#   preflight_gpu_idle        — warn if GPUs have significant VRAM already in use\n12\t#   preflight_running         — warn if a club-3090 container is already up\n13\t#   preflight_genesis_pin     — warn if on-disk Genesis tree differs from setup.sh's pin\n14\t#   preflight_repo_drift      — warn if local HEAD is behind origin/master\n15\t#\n16\t# Style: each function prints one or more \"[preflight] ...\" lines.\n17\t# Hard failures get a one-line ERROR + a \"Fix:\" hint.\n18\t\n19\t# Avoid double-sourcing.\n20\t[[ -n \"${_PREFLIGHT_LOADED:-}\" ]] && return 0\n21\t_PREFLIGHT_LOADED=1\n22\t\n23\tpreflight_docker() {\n24\t  if ! command -v docker >/dev/null 2>&1; then\n25\t    echo \"[preflight] ERROR: 'docker' not found in PATH.\" >&2\n26\t    echo \"            Fix: install Docker — https://docs.docker.com/engine/install/\" >&2\n27\t    return 1\n28\t  fi\n29\t  if ! docker compose version >/dev/null 2>&1; then\n30\t    echo \"[preflight] ERROR: 'docker compose' subcommand not available.\" >&2\n31\t    echo \"            Fix: install Docker Compose v2 plugin (legacy 'docker-compose' is unsupported).\" >&2\n32\t    return 1\n33\t  fi\n34\t  if ! docker info >/dev/null 2>&1; then\n35\t    echo \"[preflight] ERROR: 'docker info' failed — daemon not running or no permission.\" >&2\n36\t    echo \"            Fix: 'sudo systemctl start docker'  OR  add your user to the 'docker' group\" >&2\n37\t    echo \"                 ('sudo usermod -aG docker \\$USER' + log out/in).\" >&2\n38\t    return 1\n39\t  fi\n40\t  echo \"[preflight] docker:  $(docker --version | awk '{print $3}' | tr -d ',') (compose v2 ok)\"\n41\t  return 0\n42\t}\n43\t\n44\tpreflight_gpu() {\n45\t  local min_count=\"${1:-1}\"\n46\t  if ! command -v nvidia-smi >/dev/null 2>&1; then\n47\t    echo \"[preflight] ERROR: 'nvidia-smi' not found — no NVIDIA driver detected.\" >&2\n48\t    echo \"            Fix: install NVIDIA driver R550+ (CUDA 12.4+).\" >&2\n49\t    return 1\n50\t  fi\n51\t  local gpu_lines\n52\t  gpu_lines=$(nvidia-smi -L 2>/dev/null || true)\n53\t  local gpu_count\n54\t  gpu_count=$(echo \"$gpu_lines\" | grep -c '^GPU ' || true)\n55\t  if [[ \"$gpu_count\" -lt \"$min_count\" ]]; then\n56\t    echo \"[preflight] ERROR: needs ${min_count} GPU(s), found ${gpu_count}.\" >&2\n57\t    if [[ \"$gpu_count\" -eq 0 ]]; then\n58\t      echo \"            Fix: confirm 'nvidia-smi' lists your GPU(s); check driver/PCIe wiring.\" >&2\n59\t    else\n60\t      echo \"            Fix: pick a single-card variant, or install/wire the second GPU.\" >&2\n61\t    fi\n62\t    return 1\n63\t  fi\n64\t  echo \"[preflight] gpu:     ${gpu_count}× detected\"\n65\t  echo \"$gpu_lines\" | sed 's/^/[preflight]            /'\n66\t  # nvidia-container-toolkit check — needed for docker GPU access.\n67\t  if ! docker info 2>/dev/null | grep -qi 'Runtimes:.*nvidia'; then\n68\t    echo \"[preflight] WARN:  Docker doesn't list the 'nvidia' runtime. If 'docker compose up' fails\" >&2\n69\t    echo \"                   with 'unknown runtime' or 'could not select device driver', install:\" >&2\n70\t    echo \"                   https://docs.nvidia.com/datacenter/cloud-native/container-toolkit/\" >&2\n71\t  fi\n72\t  return 0\n73\t}\n74\t\n75\tpreflight_disk() {\n76\t  local path=\"$1\"\n77\t  local need_gb=\"$2\"\n78\t  # Walk up to find an existing parent (path may not exist yet).\n79\t  while [[ -n \"$path\" && ! -d \"$path\" ]]; do\n80\t    path=\"$(dirname \"$path\")\"\n81\t  done\n82\t  local avail_kb\n83\t  avail_kb=$(df -Pk \"$path\" 2>/dev/null | awk 'NR==2 {print $4}')\n84\t  if [[ -z \"$avail_kb\" ]]; then\n85\t    echo \"[preflight] WARN:  could not check free space at ${path}\" >&2\n86\t    return 0\n87\t  fi\n88\t  local avail_gb=$(( avail_kb / 1024 / 1024 ))\n89\t  if [[ \"$avail_gb\" -lt \"$need_gb\" ]]; then\n90\t    echo \"[preflight] ERROR: only ${avail_gb} GB free at ${path}, need ~${need_gb} GB.\" >&2\n91\t    echo \"            Fix: free space, or set MODEL_DIR=<path-on-larger-volume> and re-run.\" >&2\n92\t    return 1\n93\t  fi\n94\t  echo \"[preflight] disk:    ${avail_gb} GB free at ${path} (need ~${need_gb} GB)\"\n95\t  return 0\n96\t}\n97\t\n98\tpreflight_gpu_idle() {\n99\t  command -v nvidia-smi >/dev/null 2>&1 || return 0\n100\t  local mem_used_lines\n101\t  mem_used_lines=$(nvidia-smi --query-gpu=index,memory.used --format=csv,noheader,nounits 2>/dev/null || true)\n102\t  [[ -z \"$mem_used_lines\" ]] && return 0\n103\t  local warned=0\n104\t  while IFS=, read -r idx used; do\n105\t    used=$(echo \"$used\" | tr -d ' ')\n106\t    # Threshold: 1 GiB. Below that is desktop / X server / kernel modules — fine.\n107\t    if [[ \"$used\" -gt 1024 ]]; then\n108\t      if [[ $warned -eq 0 ]]; then\n109\t        echo \"[preflight] WARN:  GPU(s) already have significant VRAM in use:\" >&2\n110\t        warned=1\n111\t      fi\n112\t      echo \"[preflight]            GPU $idx: ${used} MiB in use\" >&2\n113\t    fi\n114\t  done <<< \"$mem_used_lines\"\n115\t  if [[ $warned -eq 1 ]]; then\n116\t    echo \"[preflight]        Boot may OOM. Free VRAM with 'nvidia-smi' / 'docker stop ...' first.\" >&2\n117\t  fi\n118\t  return 0\n119\t}\n120\t\n121\tpreflight_running() {\n122\t  command -v docker >/dev/null 2>&1 || return 0\n123\t  local running\n124\t  running=$(docker ps --format '{{.Names}}' 2>/dev/null | grep -E '^(vllm-qwen36-27b|llama-cpp-qwen36-27b|vllm-gemma-4-31b)' || true)\n125\t  if [[ -n \"$running\" ]]; then\n126\t    echo \"[preflight] note:    a club-3090 container is already running:\"\n127\t    echo \"$running\" | sed 's/^/[preflight]            /'\n128\t    echo \"[preflight]          'switch.sh' will bring it down before booting the new variant.\"\n129\t  fi\n130\t  return 0\n131\t}\n132\t\n133\t# preflight_genesis_pin — warn if scripts/setup.sh's declared GENESIS_PIN\n134\t# differs from the on-disk Genesis tree HEAD. This catches the\n135\t# \"user pulled the repo but didn't re-run setup.sh\" failure mode where\n136\t# vLLM boots against an outdated Genesis tree (mysterious patch failures\n137\t# at runtime). Sourceable; soft-warning only — caller decides whether\n138\t# to abort. Returns 0 always; emits a [preflight] WARN line on mismatch.\n139\tpreflight_genesis_pin() {\n140\t  local repo_root=\"${1:-${ROOT_DIR:-$(cd \"$(dirname \"${BASH_SOURCE[0]}\")/..\" && pwd)}}\"\n141\t  local setup_script=\"${repo_root}/scripts/setup.sh\"\n142\t  local genesis_dir=\"${repo_root}/models/qwen3.6-27b/vllm/patches/genesis\"\n143\t\n144\t  # If setup.sh isn't here we're in a weird state — skip silently.\n145\t  [[ -f \"$setup_script\" ]] || return 0\n146\t  # If Genesis hasn't been cloned yet, this isn't a mismatch — it's a\n147\t  # missing-setup case. Skip; setup.sh will handle it on first run.\n148\t  [[ -d \"${genesis_dir}/.git\" ]] || return 0\n149\t\n150\t  # Parse `GENESIS_PIN=\"${GENESIS_PIN:-<default>}\"` to extract the default.\n151\t  local declared_pin\n152\t  declared_pin=$(grep -E '^GENESIS_PIN=' \"$setup_script\" 2>/dev/null | head -1 \\\n153\t    | sed -E 's/.*:-([^}]+)\\}.*/\\1/; t; s/.*=//' \\\n154\t    | tr -d '\"' | tr -d \"'\")\n155\t  [[ -z \"$declared_pin\" ]] && return 0\n156\t\n157\t  # Get on-disk HEAD short SHA (matches setup.sh's `git rev-parse --short HEAD`).\n158\t  local ondisk_pin\n159\t  ondisk_pin=$(cd \"$genesis_dir\" && git rev-parse --short HEAD 2>/dev/null)\n160\t  [[ -z \"$ondisk_pin\" ]] && return 0\n161\t\n162\t  # Compare. setup.sh declares short-form pins (e.g. 2db18df); on-disk\n163\t  # short SHA from git rev-parse --short matches that form. If declared\n164\t  # pin is full-length, take its prefix matching ondisk's length.\n165\t  local declared_short=\"${declared_pin:0:${#ondisk_pin}}\"\n166\t\n167\t  if [[ \"$declared_short\" != \"$ondisk_pin\" ]]; then\n168\t    echo \"[preflight] WARN:  Genesis tree out of sync with setup.sh's declared pin.\" >&2\n169\t    echo \"[preflight]          declared (scripts/setup.sh): ${declared_pin}\" >&2\n170\t    echo \"[preflight]          on-disk (genesis/.git HEAD): ${ondisk_pin}\" >&2\n171\t    echo \"[preflight]        This usually means you pulled latest club-3090 but\" >&2\n172\t    echo \"[preflight]        didn't re-run setup.sh. vLLM may boot against an\" >&2\n173\t    echo \"[preflight]        outdated Genesis tree, causing mysterious patch\" >&2\n174\t    echo \"[preflight]        failures at runtime (see #32 for an example).\" >&2\n175\t    echo \"[preflight]        Fix:  bash scripts/setup.sh qwen3.6-27b\" >&2\n176\t  fi\n177\t  return 0\n178\t}\n179\t\n180\t# preflight_repo_drift — warn if local HEAD is behind origin/master.\n181\t# Catches the most common stale-setup pattern: user cloned weeks ago, master\n182\t# has moved (Genesis pin bumps, compose changes, vendored patch updates),\n183\t# they re-run their compose, hit a stale config, and file an issue we\n184\t# already solved on master.\n185\t#\n186\t# Behavior:\n187\t#   - Skips silently if not in a git repo, on a non-master branch, or if\n188\t#     PREFLIGHT_NO_FETCH=1 (offline rigs / CI / forks tracking elsewhere).\n189\t#   - Runs 'git fetch --quiet origin master' (~1-2s online).\n190\t#   - Compares local HEAD vs origin/master. Behind > 0 → WARN with the\n191\t#     count + last-fetch age + the one-line fix command.\n192\t#   - Returns 0 always; soft-warning only.\n193\tpreflight_repo_drift() {\n194\t  local repo_root=\"${1:-${ROOT_DIR:-$(cd \"$(dirname \"${BASH_SOURCE[0]}\")/..\" && pwd)}}\"\n195\t\n196\t  # Fast bail-outs — silent.\n197\t  [[ \"${PREFLIGHT_NO_FETCH:-0}\" == \"1\" ]] && return 0\n198\t  [[ -d \"${repo_root}/.git\" ]] || return 0\n199\t  command -v git >/dev/null 2>&1 || return 0\n200\t\n201\t  # Only check on master — on a feature branch, \"behind master\" is expected\n202\t  # state, not drift. Forks / contributors live there.\n203\t  local current_branch\n204\t  current_branch=$(git -C \"$repo_root\" rev-parse --abbrev-ref HEAD 2>/dev/null)\n205\t  [[ \"$current_branch\" == \"master\" ]] || return 0\n206\t\n207\t  # Verify origin remote points at noonghunna/club-3090. If they've forked\n208\t  # and re-pointed origin elsewhere, we don't know what's \"behind.\"\n209\t  local origin_url\n210\t  origin_url=$(git -C \"$repo_root\" config --get remote.origin.url 2>/dev/null)\n211\t  [[ \"$origin_url\" == *\"noonghunna/club-3090\"* ]] || return 0\n212\t\n213\t  # Fetch silently. 5s timeout so we don't hang on flaky networks.\n214\t  if ! timeout 5 git -C \"$repo_root\" fetch --quiet origin master 2>/dev/null; then\n215\t    # Network failure / timeout — don't make this fatal or even noisy.\n216\t    return 0\n217\t  fi\n218\t\n219\t  local behind\n220\t  behind=$(git -C \"$repo_root\" rev-list --count HEAD..origin/master 2>/dev/null)\n221\t  [[ -z \"$behind\" || \"$behind\" == \"0\" ]] && return 0\n222\t\n223\t  # Last-fetch age. FETCH_HEAD's mtime is the cleanest proxy.\n224\t  local fetch_head=\"${repo_root}/.git/FETCH_HEAD\"\n225\t  local age_str=\"\"\n226\t  if [[ -f \"$fetch_head\" ]]; then\n227\t    local now mtime age_sec\n228\t    now=$(date +%s)\n229\t    mtime=$(stat -c %Y \"$fetch_head\" 2>/dev/null || stat -f %m \"$fetch_head\" 2>/dev/null)\n230\t    if [[ -n \"$mtime\" ]]; then\n231\t      age_sec=$(( now - mtime ))\n232\t      if (( age_sec < 60 )); then age_str=\"just now\"\n233\t      elif (( age_sec < 3600 )); then age_str=\"${age_sec}s ago\"  # < 1h, surface seconds\n234\t      elif (( age_sec < 86400 )); then age_str=\"$(( age_sec / 3600 ))h ago\"\n235\t      else age_str=\"$(( age_sec / 86400 ))d ago\"; fi\n236\t    fi\n237\t  fi\n238\t\n239\t  echo \"[preflight] WARN:  Your club-3090 checkout is ${behind} commit(s) behind origin/master.\" >&2\n240\t  [[ -n \"$age_str\" ]] && echo \"[preflight]          (last origin fetch: ${age_str})\" >&2\n241\t  echo \"[preflight]        Master may have new configs, patches, or Genesis pin bumps.\" >&2\n242\t  echo \"[preflight]        Easy upgrade:  bash scripts/update.sh\" >&2\n243\t  echo \"[preflight]        (Will refuse if you have local edits — commit or stash first.)\" >&2\n244\t  echo \"[preflight]        Skip this check:  PREFLIGHT_NO_FETCH=1 bash scripts/launch.sh\" >&2\n245\t  return 0\n246\t}\n247\t\n248\t# preflight_hf_token — verify HF_TOKEN is set; warn if not.\n249\t#\n250\t# Soft warning (returns 0) — Qwen3.6-27B is T&C-gated on HuggingFace, so\n251\t# missing HF_TOKEN will cause `hf download` to fail with a generic error\n252\t# later. Surfacing the issue early saves a round-trip.\n253\t#\n254\t# Skip via: PREFLIGHT_NO_HF_TOKEN=1\n255\tpreflight_hf_token() {\n256\t  if [[ \"${PREFLIGHT_NO_HF_TOKEN:-0}\" == \"1\" ]]; then\n257\t    return 0\n258\t  fi\n259\t  if [[ -z \"${HF_TOKEN:-}\" ]]; then\n260\t    echo \"[preflight] WARNING: HF_TOKEN is not set in the environment.\" >&2\n261\t    echo \"[preflight]          Qwen3.6-27B is T&C-gated on HuggingFace; downloads will fail without a token.\" >&2\n262\t    echo \"[preflight]          Fix: visit https://huggingface.co/settings/tokens, create a read token,\" >&2\n263\t    echo \"[preflight]               accept the model T&C at https://huggingface.co/Qwen/Qwen3-Next-80B-A3B-Instruct\" >&2\n264\t    echo \"[preflight]               (and any other Qwen3-Next variant you'll use),\" >&2\n265\t    echo \"[preflight]               then export HF_TOKEN=hf_... in your shell or .env file.\" >&2\n266\t    return 0\n267\t  fi\n268\t  # Sanity check token format — HF tokens start with hf_ and are 30+ chars\n269\t  if [[ ! \"${HF_TOKEN}\" =~ ^hf_ ]] || [[ \"${#HF_TOKEN}\" -lt 30 ]]; then\n270\t    echo \"[preflight] WARNING: HF_TOKEN doesn't look like a valid HF token (expected 'hf_...' format, 30+ chars).\" >&2\n271\t    echo \"[preflight]          If downloads fail later, regenerate at https://huggingface.co/settings/tokens\" >&2\n272\t  fi\n273\t  return 0\n274\t}\n275\t\n276\t# preflight_compose_deps <compose_file> — verify any model directories the compose\n277\t# expects to mount actually exist on the host. Catches the \"you set up the repo\n278\t# but didn't WITH_DFLASH_DRAFT=1, then tried to launch dual-dflash-noviz\" failure\n279\t# mode. See club-3090#37 for the canonical case.\n280\t#\n281\t# Hard error (returns 1) — refuses to proceed if a required model dir is missing.\n282\t# Skip via: PREFLIGHT_NO_COMPOSE_DEPS=1\n283\tpreflight_compose_deps() {\n284\t  local compose_file=\"$1\"\n285\t  if [[ \"${PREFLIGHT_NO_COMPOSE_DEPS:-0}\" == \"1\" ]]; then\n286\t    return 0\n287\t  fi\n288\t  if [[ ! -f \"$compose_file\" ]]; then\n289\t    echo \"[preflight] ERROR: compose file not found: $compose_file\" >&2\n290\t    return 1\n291\t  fi\n292\t\n293\t  local model_dir=\"${MODEL_DIR:-../../../../models-cache}\"\n294\t  # Resolve relative to repo root (compose mounts use ${MODEL_DIR:-../../../../models-cache})\n295\t  if [[ \"$model_dir\" == ../* ]] || [[ \"$model_dir\" == ./* ]]; then\n296\t    model_dir=\"$(cd \"${ROOT_DIR:-$(dirname \"$0\")/..}\" && cd \"$(dirname \"$compose_file\")\" && cd \"$model_dir\" 2>/dev/null && pwd)\"\n297\t  fi\n298\t\n299\t  local missing=()\n300\t  local hint_dflash=0\n301\t  local hint_mtp=0\n302\t  local hint_gguf=0\n303\t\n304\t  # Engine detection: llama.cpp composes mount ${MODEL_DIR}:/models and pass\n305\t  # `-m /models/<path>`; vLLM composes mount ${MODEL_DIR}:/root/.cache/huggingface\n306\t  # and pass `--model /root/.cache/huggingface/<subdir>`.\n307\t  local is_llamacpp=0\n308\t  if grep -qE 'image:.*ggml-org/llama\\.cpp' \"$compose_file\"; then\n309\t    is_llamacpp=1\n310\t  fi\n311\t\n312\t  if [[ $is_llamacpp -eq 1 ]]; then\n313\t    # Scan for `-m /models/<path>` and `--mmproj /models/<path>` to learn what\n314\t    # the compose actually expects. Falls back to the canonical defaults from\n315\t    # docker-compose.yml if the variable expansion isn't grep-resolvable.\n316\t    local gguf_in_container mmproj_in_container\n317\t    gguf_in_container=$(grep -oE '\\-m[[:space:]]+/models/[^[:space:]]+' \"$compose_file\" \\\n318\t      | head -1 | awk '{print $2}' | sed 's|^/models/||')\n319\t    mmproj_in_container=$(grep -oE -- '--mmproj[[:space:]]+/models/[^[:space:]]+' \"$compose_file\" \\\n320\t      | head -1 | awk '{print $2}' | sed 's|^/models/||')\n321\t\n322\t    # Strip ${VAR:-default} expansion: take the default after `:-` if present.\n323\t    gguf_in_container=\"${gguf_in_container//\\$\\{GGUF_FILE:-/}\"\n324\t    gguf_in_container=\"${gguf_in_container%\\}}\"\n325\t    mmproj_in_container=\"${mmproj_in_container//\\$\\{MMPROJ_FILE:-/}\"\n326\t    mmproj_in_container=\"${mmproj_in_container%\\}}\"\n327\t\n328\t    [[ -z \"$gguf_in_container\" ]]   && gguf_in_container=\"qwen3.6-27b/unsloth-q3kxl/Qwen3.6-27B-UD-Q3_K_XL.gguf\"\n329\t    [[ -z \"$mmproj_in_container\" ]] && mmproj_in_container=\"qwen3.6-27b/mmproj-F16.gguf\"\n330\t\n331\t    if [[ -n \"${GGUF_FILE:-}\" ]];   then gguf_in_container=\"$GGUF_FILE\";     fi\n332\t    if [[ -n \"${MMPROJ_FILE:-}\" ]]; then mmproj_in_container=\"$MMPROJ_FILE\"; fi\n333\t\n334\t    if [[ ! -f \"${model_dir}/${gguf_in_container}\" ]]; then\n335\t      missing+=(\"${gguf_in_container} (llama.cpp GGUF weights)\")\n336\t      hint_gguf=1\n337\t    fi\n338\t    if [[ ! -f \"${model_dir}/${mmproj_in_container}\" ]]; then\n339\t      missing+=(\"${mmproj_in_container} (vision projector)\")\n340\t      hint_gguf=1\n341\t    fi\n342\t  else\n343\t    # vLLM path — scan for --speculative-config blocks (DFlash draft, MTP head).\n344\t    # The compose paths reference the in-container path /root/.cache/huggingface/<subdir>;\n345\t    # the host path is ${MODEL_DIR}/<subdir> (set via the volumes: block).\n346\t    if grep -qE '\"model\":[[:space:]]*\"/root/.cache/huggingface/qwen3.6-27b-dflash\"' \"$compose_file\"; then\n347\t      if [[ ! -f \"${model_dir}/qwen3.6-27b-dflash/config.json\" ]]; then\n348\t        missing+=(\"qwen3.6-27b-dflash (DFlash draft model)\")\n349\t        hint_dflash=1\n350\t      fi\n351\t    fi\n352\t    if grep -qE '\"model\":[[:space:]]*\"/root/.cache/huggingface/qwen3.6-27b-mtp-head\"' \"$compose_file\"; then\n353\t      if [[ ! -f \"${model_dir}/qwen3.6-27b-mtp-head/config.json\" ]]; then\n354\t        missing+=(\"qwen3.6-27b-mtp-head (MTP draft head)\")\n355\t        hint_mtp=1\n356\t      fi\n357\t    fi\n358\t\n359\t    # vLLM main model — every vLLM compose on this stack mounts AutoRound INT4.\n360\t    if [[ ! -f \"${model_dir}/qwen3.6-27b-autoround-int4/config.json\" ]]; then\n361\t      missing+=(\"qwen3.6-27b-autoround-int4 (main model)\")\n362\t    fi\n363\t  fi\n364\t\n365\t  if [[ ${#missing[@]} -eq 0 ]]; then\n366\t    return 0\n367\t  fi\n368\t\n369\t  echo \"[preflight] ERROR: compose '$compose_file' expects model files that aren't on host.\" >&2\n370\t  for item in \"${missing[@]}\"; do\n371\t    echo \"[preflight]   missing: ${model_dir}/${item}\" >&2\n372\t  done\n373\t  echo \"[preflight]\" >&2\n374\t  echo \"[preflight] Fix:\" >&2\n375\t  if [[ $hint_gguf -eq 1 ]]; then\n376\t    echo \"[preflight]   hf download unsloth/Qwen3.6-27B-GGUF \\\\\" >&2\n377\t    echo \"[preflight]     Qwen3.6-27B-UD-Q3_K_XL.gguf mmproj-F16.gguf \\\\\" >&2\n378\t    echo \"[preflight]     --local-dir ${model_dir}/qwen3.6-27b/unsloth-q3kxl\" >&2\n379\t    echo \"[preflight]   # mmproj lands at unsloth-q3kxl/ — move it up so the default --mmproj path resolves:\" >&2\n380\t    echo \"[preflight]   #   mv ${model_dir}/qwen3.6-27b/unsloth-q3kxl/mmproj-F16.gguf ${model_dir}/qwen3.6-27b/\" >&2\n381\t    echo \"[preflight]   (~16 GB total. setup.sh today only fetches the vLLM AutoRound weights;\" >&2\n382\t    echo \"[preflight]    GGUF must be fetched separately for any llamacpp/* variant.)\" >&2\n383\t  fi\n384\t  if [[ $hint_dflash -eq 1 ]]; then\n385\t    echo \"[preflight]   WITH_DFLASH_DRAFT=1 bash scripts/setup.sh qwen3.6-27b\" >&2\n386\t    echo \"[preflight]   (downloads z-lab/Qwen3.6-27B-DFlash, ~1.75 GB; required for dual-dflash* composes)\" >&2\n387\t  fi\n388\t  if [[ $hint_mtp -eq 1 ]]; then\n389\t    echo \"[preflight]   bash scripts/setup.sh qwen3.6-27b  (re-run with the right flags for MTP head)\" >&2\n390\t  fi\n391\t  if [[ $hint_dflash -eq 0 ]] && [[ $hint_mtp -eq 0 ]] && [[ $hint_gguf -eq 0 ]]; then\n392\t    echo \"[preflight]   bash scripts/setup.sh qwen3.6-27b\" >&2\n393\t  fi\n394\t  echo \"[preflight] Skip this check:  PREFLIGHT_NO_COMPOSE_DEPS=1 bash scripts/switch.sh ...\" >&2\n395\t  return 1\n396\t}\n397\t\n398\t# preflight_kv_format_hint <compose_file> — soft warning if the target compose\n399\t# uses a KV format known to be sub-optimal for the user's VRAM class.\n400\t#\n401\t# Specifically: dual-turbo.yml uses turboquant_3bit_nc which trips Cliff 2 at 90K\n402\t# on 20 GB Ampere even on TP=2. See docs/HARDWARE.md + #47 for the cross-rig data.\n403\t#\n404\t# Soft warning (returns 0). Skip via: PREFLIGHT_NO_KV_HINT=1\n405\tpreflight_kv_format_hint() {\n406\t  local compose_file=\"$1\"\n407\t  if [[ \"${PREFLIGHT_NO_KV_HINT:-0}\" == \"1\" ]]; then\n408\t    return 0\n409\t  fi\n410\t  if [[ ! -f \"$compose_file\" ]] || ! command -v nvidia-smi >/dev/null 2>&1; then\n411\t    return 0\n412\t  fi\n413\t\n414\t  # Detect smallest VRAM among visible cards (the TP-split ceiling).\n415\t  local min_vram_mib\n416\t  min_vram_mib=\"$(nvidia-smi --query-gpu=memory.total --format=csv,noheader,nounits 2>/dev/null | sort -n | head -1)\"\n417\t  if [[ -z \"$min_vram_mib\" ]] || [[ \"$min_vram_mib\" -ge 24000 ]]; then\n418\t    return 0   # 24 GB+ cards — TQ3 is the right pick, no hint needed\n419\t  fi\n420\t\n421\t  local vram_gb=$((min_vram_mib / 1024))\n422\t\n423\t  # Only fire on TQ3-using composes — that's where the 20 GB swap matters.\n424\t  if grep -qE -- '--kv-cache-dtype[[:space:]]*\\n?[[:space:]]*-?[[:space:]]*turboquant_3bit_nc' \"$compose_file\" 2>/dev/null; then\n425\t    :\n426\t  elif grep -qE 'turboquant_3bit_nc' \"$compose_file\"; then\n427\t    :\n428\t  else\n429\t    return 0   # not a TQ3 compose\n430\t  fi\n431\t\n432\t  echo \"[preflight] HINT: smallest GPU has ~${vram_gb} GB VRAM and target compose uses TurboQuant 3-bit KV.\" >&2\n433\t  echo \"[preflight]       On <24 GB Ampere, TQ3's activation peak during DeltaNet GDN forward exceeds\" >&2\n434\t  echo \"[preflight]       the per-card budget after TP split, and Cliff 2 fires at ~90K.\" >&2\n435\t  echo \"[preflight]       Override with --kv-cache-dtype fp8_e5m2 in the compose file.\" >&2\n436\t  echo \"[preflight]       Cross-rig validation: docs/HARDWARE.md + club-3090#47\" >&2\n437\t  echo \"[preflight]       Predict your config:  bash tools/kv-calc.py --compose <name> --vram ${vram_gb} --kv-format <fp8_e5m2|turboquant_3bit_nc>\" >&2\n438\t  return 0\n439\t}\n440\t\n441\t# autodetect_endpoint — discover the running club-3090 container + its host port.\n442\t#\n443\t# Caller-controlled: the bench / verify scripts default URL=http://localhost:8020\n444\t# and CONTAINER=vllm-qwen36-27b. That assumption breaks when the user is running\n445\t# a different variant (e.g. dual-turbo on 8011, dual-dflash on 8012, etc.) and\n446\t# silently makes verify-full / bench / verify-stress emit false negatives because\n447\t# they're hitting an empty port. Reported by sudepo on club-3090#52.\n448\t#\n449\t# Behaviour:\n450\t#   - If $URL or $CONTAINER is already set in the environment, it WINS — never\n451\t#     overwritten. This preserves explicit override behaviour.\n452\t#   - Otherwise, scan `docker ps` for a club-3090-pattern container and extract\n453\t#     its host port from the port-mapping. Print one [autodetect] line so the\n454\t#     user knows what we picked.\n455\t#   - If nothing is detected (no container running, docker unavailable), the\n456\t#     hardcoded defaults stand — same behaviour as before this helper existed.\n457\t#\n458\t# Outputs (mutates env in caller's scope when sourced):\n459\t#   URL          — http://localhost:<port> if detected\n460\t#   CONTAINER    — running container name if detected\n461\t#\n462\t# Skip via: PREFLIGHT_NO_AUTODETECT=1\n463\tpreflight_autodetect_endpoint() {\n464\t  if [[ \"${PREFLIGHT_NO_AUTODETECT:-0}\" == \"1\" ]]; then\n465\t    return 0\n466\t  fi\n467\t  command -v docker >/dev/null 2>&1 || return 0\n468\t\n469\t  local explicit_url=\"${URL:-}\"\n470\t  local explicit_container=\"${CONTAINER:-}\"\n471\t  if [[ -n \"$explicit_url\" && -n \"$explicit_container\" ]]; then\n472\t    return 0   # both already set — caller knows what they're doing\n473\t  fi\n474\t\n475\t  # Scan for one of our containers + its `0.0.0.0:<host>->8000/tcp` mapping.\n476\t  local found_line\n477\t  found_line=$(docker ps --format '{{.Names}}|{{.Ports}}' 2>/dev/null \\\n478\t    | grep -E '^(vllm-qwen36-27b|llama-cpp-qwen36-27b|vllm-gemma-4-31b)' | head -1)\n479\t  if [[ -z \"$found_line\" ]]; then\n480\t    return 0   # nothing running; defaults stand\n481\t  fi\n482\t\n483\t  local detected_name detected_port\n484\t  detected_name=\"${found_line%%|*}\"\n485\t  # Extract host port from \"0.0.0.0:8011->8000/tcp\", \"[::]:8011->8000/tcp\",\n486\t  # or \"127.0.0.1:8011->8000/tcp\" forms (BIND_HOST=127.0.0.1 produces the last).\n487\t  # llama-cpp container maps to internal 8080, vllm to 8000 — match both.\n488\t  detected_port=$(echo \"${found_line#*|}\" \\\n489\t    | grep -oE '([0-9]{1,3}\\.){3}[0-9]{1,3}:[0-9]+->(8000|8080)/tcp' \\\n490\t    | head -1 \\\n491\t    | sed -E 's|^[^:]+:([0-9]+)->.*|\\1|')\n492\t\n493\t  # Apply, but only fields the user didn't already set explicitly.\n494\t  if [[ -z \"$explicit_container\" && -n \"$detected_name\" ]]; then\n495\t    CONTAINER=\"$detected_name\"\n496\t  fi\n497\t  if [[ -z \"$explicit_url\" && -n \"$detected_port\" ]]; then\n498\t    URL=\"http://localhost:${detected_port}\"\n499\t  fi\n500\t\n501\t  # One-line surface so the user sees what we chose.\n502\t  if [[ -z \"$explicit_url\" || -z \"$explicit_container\" ]]; then\n503\t    local note=\"\"\n504\t    [[ -z \"$explicit_container\" ]] && note=\"container=${CONTAINER}\"\n505\t    [[ -z \"$explicit_url\" ]] && note=\"${note:+$note }url=${URL}\"\n506\t    echo \"[autodetect] using running ${note}  (skip: PREFLIGHT_NO_AUTODETECT=1)\" >&2\n507\t  fi\n508\t  return 0\n509\t}\n510\t",
    "chars": 25250
  },
  {
    "turn": 10,
    "user_msg": "Read <repo>/models/qwen3.6-27b/vllm/compose/docker-compose.bounded-thinking.yml and tell me what's relevant to the issue.",
    "tool_name": "Read",
    "tool_result": "1\t# ===========================================================================\n2\t# Bounded-thinking variant — recommended grammar is the DeepSeek scratchpad\n3\t# (PLAN/NOTE×0-15/VERDICT FSM at `tools/grammar-eval/deepseek-scratchpad.gbnf`).\n4\t#\n5\t# **Phase 3 bench 2026-05-04 final (full HE+ 164 + LCB v6 50, n=214):**\n6\t#   - DeepSeek scratchpad: 93.9% HE+ / 66.0% LCB v6 / 87.4% combined.\n7\t#     Doesn't lose combined accuracy vs the original andthattoo G/A/E\n8\t#     grammar (delta = +0.47pp = 1 problem on n=214, well below noise),\n9\t#     and gains +4pp on LCB v6 (= 2 problems on n=50, design signal not\n10\t#     statistical proof).\n11\t#   - andthattoo G/A/E (the originally-published structured-CoT grammar):\n12\t#     94.5% HE+ / 62.0% LCB / 86.9% combined, tighter think budget\n13\t#     (mean 134 tokens vs DeepSeek's 541). Slightly better on HE+ alone.\n14\t#\n15\t# We ship this single compose with DeepSeek as the recommended grammar\n16\t# because preserving HE+ accuracy while gaining LCB headroom is the most\n17\t# defensible per-workload posture — not because of a categorical accuracy\n18\t# win. The compose itself is\n19\t# grammar-agnostic — the grammar is selected client-side via\n20\t# `extra_body={\"structured_outputs\": {\"grammar\": ...}}`. Users who want\n21\t# tighter compression (the ~4× cheaper andthattoo grammar) can pass\n22\t# `tools/grammar-eval/holiday-tagline.gbnf` (extreme 24-token compression,\n23\t# wins LCB) or the original andthattoo G/A/E grammar from\n24\t# https://github.com/andthattoo/structured-cot — see\n25\t# docs/STRUCTURED_COT.md for the full Phase 3 comparison and grammar\n26\t# decision table.\n27\t#\n28\t# Long-text 214K + structured-CoT grammar enforcement in the reasoning\n29\t# channel. Compresses think tokens ~30× with iso-or-better accuracy on\n30\t# coding benchmarks. See docs/STRUCTURED_COT.md for the full bench, the\n31\t# grammar files, and the harness.\n32\t#\n33\t# ⚠️ NOT SAFE FOR IDE-AGENT WORKLOADS WITH TOOL SCHEMAS. Same Cliff 1 mech B\n34\t# inductor compile-path leak as long-text/long-vision (club-3090#16). Use\n35\t# `tools-text.yml` for IDE agents until Genesis PN25 lands default-on.\n36\t#\n37\t# Additional bug surfaced 2026-05-01 PM on this compose specifically:\n38\t# `VLLM_SSM_CONV_STATE_LAYOUT=DS` + spec-decode + `mamba_cache_mode='align'`\n39\t# + `num_accepted_tokens > 1` raises `NotImplementedError` on certain prompt\n40\t# shapes (LCB/LeetCode-style problems triggered it on every request). Filed\n41\t# as Sandermage/genesis-vllm-patches#17. Pure long-form text generation with\n42\t# bounded thinking is unaffected; LCB-style problem statements may crash.\n43\t#\n44\t# Stack: identical to long-text (vLLM nightly + Genesis v7.64 with PN12 anchor\n45\t# sidecar + compile-safe sidecar + PN17 + P104 + P37/P38 + TurboQuant 3-bit KV\n46\t# + MTP n=3 + 185K text-only at 0.975 mem-util) PLUS one extra flag:\n47\t# --structured-outputs-config.enable_in_reasoning true. That single flag is\n48\t# what makes structured-CoT work end-to-end on this stack — vLLM's default\n49\t# is False, which restricts the grammar mask to post-`</think>` content. Bench\n50\t# numbers don't reproduce without it (the model thinks freely regardless of\n51\t# grammar; FREE and FSM produce identical token counts).\n52\t#\n53\t# Recommended client max_tokens: 4096. The whole design point of this compose\n54\t# is bounded thinking — the grammar caps the think block to ~150-300 structured\n55\t# tokens, then leaves comfortable budget for the answer. 4096 is plenty. (Use\n56\t# 8192+ if you're calling this compose with FREE thinking, but at that point\n57\t# you should be on long-text.yml — the structured-outputs flag costs ~10% TPS\n58\t# for nothing if you're not actually using grammar.)\n59\t#\n60\t# When to pick this over long-text:\n61\t#   - You're using grammar-constrained `<think>` blocks (xgrammar GBNF) to\n62\t#     bound reasoning cost. See docs/STRUCTURED_COT.md for the harness +\n63\t#     example grammars (HumanEval+ GOAL/APPROACH/EDGE; LiveCodeBench\n64\t#     GOAL/STATE/ALGO/EDGE/VERIFY; or your own).\n65\t#   - You want per-request bounded thinking with structural guarantee.\n66\t#     PROMPT_TERSE alone (system prompt asking for terse format) often\n67\t#     suffices on easy problems but loses discipline on hard ones; this\n68\t#     compose adds the FSM safety net.\n69\t#   - You're cost-bounded by output token spend and want ~30× cheaper\n70\t#     thinking with no accuracy regression on coding workloads.\n71\t#\n72\t# When NOT to pick this:\n73\t#   - Anything that doesn't use `structured_outputs.{grammar,json,...}` in\n74\t#     extra_body — the flag is harmless then but you gain nothing. Use\n75\t#     long-text instead.\n76\t#   - Workloads where the model's *unstructured* free thinking is itself\n77\t#     the product (chat-of-thought UIs, scratch-pad apps). Bounded thinking\n78\t#     compresses the reasoning into structured slots — fine for code where\n79\t#     you only consume the answer, not the trace.\n80\t#   - Vision input — same as long-text, this drops the vision tower to free\n81\t#     ~1 GB VRAM. Use long-vision-grammar (TODO) if you need both.\n82\t#\n83\t# Measured (full bench 2026-04-30 PM, RTX 3090):\n84\t#   HumanEval+ 164:   FSM 92.7% pass@1, 96 think tok mean, +4.3pp vs FREE,\n85\t#                     30.7× compression vs FREE thinking (2950 tok mean)\n86\t#   LiveCodeBench v6: FSM 66.0% pass@1, 145 think tok mean, +24.0pp vs FREE,\n87\t#                     26.2× compression vs FREE thinking (3797 tok mean)\n88\t#   Sustained TPS:    ~53 (about 10% slower than long-text due to grammar\n89\t#                     mask compute; but per-problem wall-clock 10× faster\n90\t#                     because of the 30× smaller think output)\n91\t#\n92\t# Caveat:\n93\t#   - Some of the +Δpp accuracy gain comes from FSM dodging the\n94\t#     max_tokens=4096 truncation trap rather than pure reasoning gain.\n95\t#     If you re-bench at max_tokens=8192 the headline shrinks toward\n96\t#     andthattoo's published +0.6pp / +14pp. Compression effect is the\n97\t#     reliably-real win. See docs/STRUCTURED_COT.md \"Honest caveats\".\n98\t#   - Grammar × MTP non-determinism: greedy temp=0 runs on different cards\n99\t#     can produce different verdicts on the same problem (sub-1% rate).\n100\t#   - Cliff 2 (single-prompt ≥50–60K in GDN) still applies — same as\n101\t#     long-text. 218K context is for STEADY-STATE accumulation, not\n102\t#     stuffing 218K of fresh tokens in one request.\n103\t#   - Cliff 1 mech B leak on real coding-agent workloads (OpenCode, Cline)\n104\t#     with mixed sys-prompt + tool-schema prefill: same gap as long-text.\n105\t#     PN12's eager-mode pool is bypassed by inductor-compiled FFN forward.\n106\t#     See issue #16. Workarounds: switch to `tools-text.yml` (75K + fp8 +\n107\t#     PN8 reaches compile path), or add `--enforce-eager` below (~20-30%\n108\t#     TPS hit), or lower `--gpu-memory-utilization` to 0.94.\n109\t#\n110\t# Run with:\n111\t#   cd <repo>/models/qwen3.6-27b/vllm/compose\n112\t#   docker compose -f docker-compose.bounded-thinking.yml up -d\n113\t#   # or via switch.sh:\n114\t#   bash scripts/switch.sh vllm/bounded-thinking\n115\t#\n116\t# Then send requests with structured_outputs in extra_body — see\n117\t# docs/STRUCTURED_COT.md for client examples.\n118\t# ===========================================================================\n119\tservices:\n120\t  vllm-qwen36-27b-bounded-thinking:\n121\t    image: vllm/vllm-openai:nightly-01d4d1ad375dc5854779c593eee093bcebb0cada\n122\t    container_name: vllm-qwen36-27b-bounded-thinking\n123\t    restart: \"no\"\n124\t    ports:\n125\t      - \"${BIND_HOST:-0.0.0.0}:${PORT:-8020}:8000\"\n126\t    volumes:\n127\t      - ${MODEL_DIR:-../../../../models-cache}:/root/.cache/huggingface\n128\t      # torch.compile + Triton kernel caches — first boot warms (~60-90 sec);\n129\t      # subsequent boots reuse cached graphs. Pattern from Sander's PROD launch.\n130\t      # Closes club-3090 #22.\n131\t      - ../cache/torch_compile:/root/.cache/vllm/torch_compile_cache\n132\t      - ../cache/triton:/root/.triton/cache\n133\t      - ../patches/genesis/vllm/_genesis:/usr/local/lib/python3.12/dist-packages/vllm/_genesis:ro\n134\t      - ../patches/local/qwen3coder_tool_parser_deferred_commit.py:/patches/qwen3coder_tool_parser_deferred_commit.py:ro\n135\t      # workspace_lock_disable — relaxes vllm#39226 strict assertion until\n136\t      # Sandermage's P98 marker fix lands. v0.20-only requirement.\n137\t    environment:\n138\t      # Uncomment the next line to pin to a specific GPU (e.g. GPU 0):\n139\t      # - CUDA_VISIBLE_DEVICES=0\n140\t      - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN:-}\n141\t      - VLLM_WORKER_MULTIPROC_METHOD=spawn\n142\t      - NCCL_CUMEM_ENABLE=0\n143\t      - NCCL_P2P_DISABLE=1\n144\t      - VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS=0\n145\t      - VLLM_NO_USAGE_STATS=1\n146\t      # expandable_segments:True crashes boot on some setups (likely cuMemMap path).\n147\t      # Known: JusefPol on NVLink (PR #31), WSL2 single-card 3090 Ti.\n148\t      # Override via .env: PYTORCH_CUDA_ALLOC_CONF=expandable_segments:False\n149\t      - PYTORCH_CUDA_ALLOC_CONF=${PYTORCH_CUDA_ALLOC_CONF:-expandable_segments:True,max_split_size_mb:512}\n150\t      - VLLM_FLOAT32_MATMUL_PRECISION=high\n151\t      - VLLM_USE_FLASHINFER_SAMPLER=1\n152\t      - OMP_NUM_THREADS=1\n153\t      - CUDA_DEVICE_MAX_CONNECTIONS=8\n154\t      - VLLM_ALLOW_LONG_MAX_MODEL_LEN=1\n155\t      - VLLM_MARLIN_USE_ATOMIC_ADD=1\n156\t      - TRITON_CACHE_DIR=/root/.triton/cache\n157\t      # FULL Genesis v7.65 PROD env-var set — same as long-text.yml.\n158\t      - GENESIS_ENABLE_P4=1\n159\t      - GENESIS_ENABLE_P58_ASYNC_PLACEHOLDER_FIX=1\n160\t      - GENESIS_ENABLE_P60_GDN_NGRAM_FIX=1\n161\t      - GENESIS_ENABLE_P60B_TRITON_KERNEL=1\n162\t      - GENESIS_ENABLE_P61_QWEN3_MULTI_TOOL=1\n163\t      - GENESIS_ENABLE_P61B_STREAMING_OVERLAP=1\n164\t      - GENESIS_ENABLE_P62_STRUCT_OUT_SPEC_TIMING=1\n165\t      - GENESIS_ENABLE_P64_QWEN3CODER_MTP_STREAMING=1\n166\t      # P65 dropped 2026-05-03 (mutually exclusive with P67/P67b in v7.69)\n167\t      - GENESIS_ENABLE_P66_CUDAGRAPH_SIZE_FILTER=1\n168\t      - GENESIS_ENABLE_P67_TQ_MULTI_QUERY_KERNEL=1\n169\t      - GENESIS_ENABLE_P68_AUTO_FORCE_TOOL=1\n170\t      - GENESIS_ENABLE_P69_LONG_CTX_TOOL_REMINDER=1\n171\t      - GENESIS_P68_P69_LONG_CTX_THRESHOLD_CHARS=50000\n172\t      - GENESIS_ENABLE_P72_PROFILE_RUN_CAP=1\n173\t      - GENESIS_PROFILE_RUN_CAP_M=4128\n174\t      - GENESIS_ENABLE_P74_CHUNK_CLAMP=1\n175\t      - GENESIS_ENABLE_P83=1\n176\t      # P85 dropped 2026-05-03 (requires P84 which we don't enable in v7.69)\n177\t      - GENESIS_ENABLE_P87=1\n178\t      - GENESIS_ENABLE_P91=1\n179\t      - GENESIS_ENABLE_P94=1\n180\t      - GENESIS_ENABLE_P98=1\n181\t      # PN34: active env-opt-in workspace-lock relaxation. P98 above auto-skips\n182\t      # on v0.20 (UNIFORM_SINGLE_TOKEN_DECODE drift-marker false-positive — see\n183\t      # docs/UPSTREAM.md), so PN34 is what's actually firing today. Belt+suspenders\n184\t      # pattern matches default + long-text composes. Propagated from #82 audit.\n185\t      - GENESIS_ENABLE_PN34_WORKSPACE_LOCK_RELAX=1\n186\t      - GENESIS_ENABLE_P99=1\n187\t      - GENESIS_ENABLE_P100=1\n188\t      - GENESIS_ENABLE_P101=1\n189\t      - GENESIS_ENABLE_P103=1\n190\t      - GENESIS_ENABLE_PN8_MTP_DRAFT_ONLINE_QUANT=1\n191\t      - GENESIS_ENABLE_PN9_INDEPENDENT_DRAFTER_ATTN=1\n192\t      - GENESIS_ENABLE_PN11_GDN_AB_CONTIGUOUS=1\n193\t      - GENESIS_ENABLE_PN12_FFN_INTERMEDIATE_POOL=1\n194\t      - GENESIS_ENABLE_PN13_CUDA_GRAPH_LAMBDA_ARITY=1\n195\t      - GENESIS_ENABLE_PN14_TQ_DECODE_OOB_CLAMP=1\n196\t      - GENESIS_ENABLE_PN17_FA2_LSE_CLAMP=1\n197\t      - GENESIS_ENABLE_PN19_SCOPED_MAX_SPLIT=1\n198\t      - GENESIS_ENABLE_PN22_LOCAL_ARGMAX_TP=1\n199\t      - GENESIS_ENABLE_PN26_SPARSE_V=1\n200\t      - GENESIS_ENABLE_PN59_STREAMING_GDN=1\n201\t      - GENESIS_PN26_SPARSE_V_BLOCK_KV=8\n202\t      - GENESIS_PN26_SPARSE_V_NUM_WARPS=4\n203\t      - GENESIS_PN26_SPARSE_V_THRESHOLD=0.01\n204\t      - GENESIS_ENABLE_P38B_COMPILE_SAFE=1\n205\t      - GENESIS_ENABLE_P15B_FA_VARLEN_CLAMP=1\n206\t      - GENESIS_ENABLE_PN25_SILU_INDUCTOR_SAFE=1\n207\t      # PN30 — RE-ENABLED with our local dst-shaped temp fix\n208\t      # (patch_pn30_dst_shaped_temp_fix.py, applied during setup.sh).\n209\t      - GENESIS_ENABLE_PN30_DS_LAYOUT_SPEC_DECODE=1\n210\t      - GENESIS_PREALLOC_TOKEN_BUDGET=4128\n211\t      - GENESIS_BUFFER_MODE=shared\n212\t      # P82 — SGLang-style acceptance threshold OR-clause. Biased toward single-stream\n213\t      # small-batch INT4+MTP K=3, which IS this compose's workload. Genesis cross-rig\n214\t      # data: +10.5% on 3090 INT4. Disabled in dual-card composes (multi-stream bias).\n215\t      - GENESIS_ENABLE_P82=1\n216\t      - GENESIS_P82_THRESHOLD_SINGLE=0.3\n217\t      # P40 — TQ k8v4 GQA grouping kernel (+15-30% on compute-regime GPUs with L2>=24MB).\n218\t      # RTX 5090 Laptop: 88MB L2 → enable via compose/.env: GENESIS_ENABLE_P40=1\n219\t      # RTX 3090: 6MB L2 → no gain, default off.\n220\t      - GENESIS_ENABLE_P40=${GENESIS_ENABLE_P40:-0}\n221\t      # Explicit OFFs:\n222\t      # P78 (P78_TOLIST_CAPTURE_GUARD) — superseded by Genesis PN34/P78 native\n223\t      # P81 (FP8 block-scaled M<=8) — FP8-specific, no-op on our TQ3 path\n224\t      - GENESIS_ENABLE_P78_TOLIST_CAPTURE_GUARD=0\n225\t      - GENESIS_ENABLE_P81_FP8_BLOCK_SCALED_M_LE_8=0\n226\t      # VLLM_SSM_CONV_STATE_LAYOUT=DS — RE-ENABLED with our local PN30\n227\t      # dst-shaped temp fix (patch_pn30_dst_shaped_temp_fix.py, applied at\n228\t      # setup time). Without our fix, Sander's PN30 a9977d8 corrupts DS row\n229\t      # strides on spec-decode AL>1. With our fix, PN30 builds destination-\n230\t      # shaped temp via collect_mamba_copy_meta. +6% TPS retained.\n231\t      - VLLM_SSM_CONV_STATE_LAYOUT=DS\n232\t      - VLLM_USE_FUSED_MOE_GROUPED_TOPK=1\n233\t    shm_size: \"16gb\"\n234\t    ipc: host\n235\t    deploy:\n236\t      resources:\n237\t        reservations:\n238\t          devices:\n239\t            - driver: nvidia\n240\t              count: all\n241\t              capabilities: [gpu]\n242\t    entrypoint:\n243\t      - /bin/bash\n244\t      - -c\n245\t      - |\n246\t        set -e\n247\t        pip install xxhash pandas scipy -q\n248\t        # Pre-flight: Genesis patches must be populated. Empty volume mount\n249\t        # = silent no-op apply_all = boot fails later with cryptic upstream\n250\t        # error (e.g. \"TurboQuant KV not supported for hybrid models\", #13).\n251\t        if [ ! -f /usr/local/lib/python3.12/dist-packages/vllm/_genesis/patches/apply_all.py ]; then\n252\t          echo \"ERROR: Genesis patches missing — host volume models/qwen3.6-27b/vllm/patches/genesis/ is empty.\" >&2\n253\t          echo \"       Run from repo root before 'docker compose up':\" >&2\n254\t          echo \"         bash scripts/setup.sh qwen3.6-27b\" >&2\n255\t          exit 1\n256\t        fi\n257\t        python3 -m vllm._genesis.patches.apply_all\n258\t        # Tool-parser deferred-commit fix for qwen3coder SSE-silence bug (issue #72).\n259\t        # Drops out when vllm-project/vllm lands the upstream fix.\n260\t        python3 /patches/qwen3coder_tool_parser_deferred_commit.py\n261\t        # VLLM_ENFORCE_EAGER=1 in compose/.env disables CUDA graphs — use on\n262\t        # hardware where Cliff 2 GDN activation spikes occur at runtime.\n263\t        exec vllm serve ${VLLM_ENFORCE_EAGER:+--enforce-eager} \"$@\"\n264\t      - --\n265\t    command:\n266\t      - --model\n267\t      - /root/.cache/huggingface/qwen3.6-27b-autoround-int4\n268\t      - --served-model-name\n269\t      - qwen3.6-27b\n270\t      - --quantization\n271\t      - auto_round\n272\t      - --dtype\n273\t      - float16\n274\t      - --tensor-parallel-size\n275\t      - \"1\"\n276\t      # 180K + 0.95 — parity with long-text.yml. Backed off from 214K + 0.985\n277\t      # on 2026-05-02 to give activation headroom for the PN12+PN25 FFN pool\n278\t      # residence + DeltaNet GDN buffer. See long-text.yml for full rationale.\n279\t      # WSL2 / laptop users: override via compose/.env (gitignored):\n280\t      #   GPU_MEMORY_UTILIZATION=0.94   # RTX 5090 Laptop: ~1.31 GiB boot overhead\n281\t      #   PYTORCH_CUDA_ALLOC_CONF=expandable_segments:False,max_split_size_mb:512\n282\t      #   VLLM_ENFORCE_EAGER=1          # Cliff 2 GDN spike workaround; ~20-30% TPS\n283\t      - --max-model-len\n284\t      - \"${MAX_MODEL_LEN:-180000}\"\n285\t      - --gpu-memory-utilization\n286\t      - \"${GPU_MEMORY_UTILIZATION:-0.95}\"\n287\t      - --max-num-seqs\n288\t      - \"1\"\n289\t      - --max-num-batched-tokens\n290\t      - \"4128\"\n291\t      - --kv-cache-dtype\n292\t      - \"${KV_CACHE_DTYPE:-turboquant_3bit_nc}\"\n293\t      - --language-model-only\n294\t      - --trust-remote-code\n295\t      - --reasoning-parser\n296\t      - qwen3\n297\t      # The whole reason this variant exists. Default is False, which limits\n298\t      # the grammar mask to post-`</think>` content. With this set to true,\n299\t      # xgrammar enforces the GBNF inside the reasoning channel — which is\n300\t      # what structured-CoT is for. Without this flag the grammar is silently\n301\t      # ignored on the `<think>` block and you get FREE-mode token counts.\n302\t      - --structured-outputs-config.enable_in_reasoning\n303\t      - \"true\"\n304\t      - --enable-auto-tool-choice\n305\t      - --tool-call-parser\n306\t      - qwen3_coder\n307\t      - --enable-prefix-caching\n308\t      - --enable-chunked-prefill\n309\t      - --no-scheduler-reserve-full-isl\n310\t      - --speculative-config\n311\t      - '{\"method\":\"mtp\",\"num_speculative_tokens\":3}'\n312\t      - --host\n313\t      - 0.0.0.0\n314\t      - --port\n315\t      - \"8000\"\n316\t",
    "chars": 17407
  },
  {
    "turn": 11,
    "user_msg": "Read <repo>/models/qwen3.6-27b/vllm/compose/docker-compose.long-text.yml and tell me what's relevant to the issue.",
    "tool_name": "Read",
    "tool_result": "1\t# ===========================================================================\n2\t# ⚠️  KNOWN ISSUE 2026-05-05 — Cliff 2b at >50K single-prompt context\n3\t#     Genesis v7.72.2 PN59 streaming-GDN was advertised as the structural\n4\t#     Cliff 2b fix but doesn't engage on the chunked-prefill code path\n5\t#     24 GB single-card configs are forced to take. Filed at:\n6\t#         https://github.com/Sandermage/genesis-vllm-patches/issues/22\n7\t#     Workarounds: use docker-compose.dual.yml / dual-turbo.yml (TP=2\n8\t#     escapes the cliff) or llamacpp recipes (different engine, no\n9\t#     Cliff 2b). See docs/CLIFFS.md for the full diagnosis.\n10\t# ===========================================================================\n11\t# Long-text variant — 214K context, text-only (no vision).\n12\t#\n13\t# ⚠️ NOT SAFE FOR IDE-AGENT WORKLOADS (Cline / OpenCode / Roo / Claude Code /\n14\t# Cursor / any flow with sys-prompt + tool schemas). Use `tools-text.yml`\n15\t# instead until Genesis PN25 lands default-on. See club-3090#16.\n16\t#\n17\t# Reproducer (2026-05-01 PM): a 5,900-char system prompt + 10 typical tool\n18\t# schemas + 346-char user request crashes the engine with a 98 MiB OOM at\n19\t# `empty_strided_cuda((s, 17408), ...)` in `inductor_cache/...py:1208`.\n20\t# vLLM's torch.compile inductor inlines `SiluAndMul.forward_native` and\n21\t# bypasses Genesis PN12's `FFNIntermediateCache` pool. PN25 (the proper\n22\t# compile-path opaque-op fix) is on Genesis dev but blocked by a worker-\n23\t# fork registration bug — see Sandermage/genesis-vllm-patches#16.\n24\t#\n25\t# Workloads that ARE safe on this compose:\n26\t#   - Pure long-form text generation (essay, doc Q&A) — no tool schemas in prompt\n27\t#   - RAG without tool calling (just context + question)\n28\t#   - Chat without tools\n29\t# Workloads that should use `tools-text.yml` (75K + fp8 KV + PN8) instead:\n30\t#   - Any IDE coding agent\n31\t#   - Any flow that puts tool/function schemas in the system prompt\n32\t#\n33\t# Stack: vLLM v0.20.1rc1.dev16 + Genesis v7.69 dev (commit 2db18df) +\n34\t# TurboQuant 3-bit KV + MTP n=3 + vision tower disabled.\n35\t#\n36\t# v7.69 key patches active:\n37\t#   - P103 (Cliff 2 chunked fwd_h+fwd_o orchestrator) — chunk.py self-install\n38\t#     hook survives `exec vllm serve` worker spawn (v7.69 F2 fix). Also\n39\t#     applied: Codex r1 cu_seqlens=[0,T] dense-single-seq gate fix that\n40\t#     allows the chunked path to engage on real serving.\n41\t#   - PN30 v7.68 part1+2+3 — DS conv state layout + spec-decode AL>1 fix\n42\t#     (drift-marker bug fixed in v7.69)\n43\t#   - PN32 v2 — GDN _forward_core chunked-prefill (Cliff 2 outer split)\n44\t#   - PN34 — workspace_lock relaxation (replaces our former\n45\t#     patch_workspace_lock_disable.py sidecar)\n46\t#   - PN26b: Genesis-original sparse-V Triton kernel for SM86 (Ampere\n47\t#     consumer) — first SM86 sparse-V kernel in any public tree. 27B\n48\t#     tuning: BLOCK_KV=8 num_warps=4 threshold=0.01.\n49\t#   - P38B / P15B: Cliff 1 mech B compile-safe hook + FA varlen clamp\n50\t#\n51\t# Cliff 2 closure recipe (cross-rig validated 2026-05-02 PM):\n52\t#   - vllm#35975 backport sidecar (`patch_inputs_embeds_optional.py`) frees\n53\t#     ~444 MiB at boot on text-only models (skips inputs_embeds buffers\n54\t#     for both gpu_model_runner.py + llm_base_proposer.py).\n55\t#   - --gpu-memory-utilization 0.93 — middle ground between 0.92 (cliff\n56\t#     just barely closed) and 0.95 (cliff fires). Recovers ~30K of KV\n57\t#     pool concurrency vs 0.92 while keeping ~494 MiB margin at peak.\n58\t#   - --max-model-len 180000 — engine pre-check admission ceiling on\n59\t#     this config (200K rejected: KV pool needs 3.31 GiB, only 2.19 GiB\n60\t#     available at 0.93). Cliff 2 single-prompt safe envelope: ~60K\n61\t#     verified pass; ~70-80K is the OOM ceiling.\n62\t#\n63\t# Validation (2026-05-02 PM, branch v7.69-cliff2-test):\n64\t#   - 60K MTP-on probe: HTTP 200 in 623s, recall correct, MTP AL=4.00\n65\t#   - 90K MTP-on probe: HTTP 500 OOM at 112s (50 MiB / 24.5 MiB free)\n66\t#   - Probes 2-6 (tool prefill, IDE-agent, multi-turn, LCB-coding,\n67\t#     reasoning-heavy): all PASS\n68\t#\n69\t# For workloads that need >60K single-prompt or 200K declared context,\n70\t# use docker-compose.long-text-no-mtp.yml (max-context safety variant\n71\t# trades MTP decode speedup for prefill stability). For coding agents\n72\t# on dual-card, use docker-compose.dual-turbo.yml.\n73\t#\n74\t# Recommended client max_tokens: 8192 (FREE thinking on this compose; budget\n75\t# for hard reasoning runs). 16384 for competition-grade problems. See\n76\t# docs/EXAMPLES.md \"max_tokens defaults\" section for the full table.\n77\t#\n78\t# When to pick this over long-vision:\n79\t#   - You don't need image input\n80\t#   - You want the maximum context on a single 3090\n81\t#   - You're doing long-context text workloads (codebase analysis, RAG\n82\t#     retrievals, multi-turn chat with steady-state accumulation)\n83\t#\n84\t# When NOT to pick this:\n85\t#   - Anything that needs vision — use long-vision (198K + vision) instead\n86\t#   - Single-shot prompts ≥50–60K tokens — Cliff 2 (DeltaNet GDN) still\n87\t#     applies on single-card. Route those to dual.yml (TP=2 splits the\n88\t#     state across cards) or llamacpp/default (262K, different engine).\n89\t#\n90\t# Local sidecar (single one remaining on this compose):\n91\t#   - patch_workspace_lock_disable.py — relaxes vllm#39226 strict\n92\t#     WorkspaceManager.lock() assertion to a one-shot WARNING. P98 in v7.65\n93\t#     covers this functionally but its drift marker auto-skips on v0.20\n94\t#     (`UNIFORM_SINGLE_TOKEN_DECODE` false-positive). Drop when Sandermage\n95\t#     ships the marker fix.\n96\t#\n97\t# Variants in this dir:\n98\t#   File                                  Ctx     KV       Vision   Notes\n99\t#   docker-compose.yml (default)          48K     TQ3      ✅       Production-safe; below both cliffs\n100\t#   docker-compose.long-vision.yml        198K    TQ3      ✅       Long ctx + vision (cliffs closed by v0.20 + v7.65)\n101\t#   docker-compose.long-text.yml (this)   214K    TQ3      ❌       Engine-ceiling text-only on v0.20 + 0.985 mem-util\n102\t#   docker-compose.bounded-thinking.yml   214K    TQ3      ❌       Long-text + structured-CoT grammar in <think>\n103\t#   docker-compose.tools-text.yml         75K     fp8      ❌       IDE agents — fp8 KV alternative\n104\t#\n105\t# Run with:\n106\t#   cd <repo>/models/qwen3.6-27b/vllm/compose\n107\t#   docker compose -f docker-compose.long-text.yml up -d\n108\t# ===========================================================================\n109\tservices:\n110\t  vllm-qwen36-27b-long-text:\n111\t    image: vllm/vllm-openai:nightly-01d4d1ad375dc5854779c593eee093bcebb0cada\n112\t    container_name: vllm-qwen36-27b-long-text\n113\t    restart: \"no\"\n114\t    ports:\n115\t      - \"${BIND_HOST:-0.0.0.0}:${PORT:-8020}:8000\"\n116\t    volumes:\n117\t      - ${MODEL_DIR:-../../../../models-cache}:/root/.cache/huggingface\n118\t      # torch.compile + Triton kernel caches — first boot warms (~60-90 sec\n119\t      # for Dynamo trace + Inductor compile + Triton JIT); subsequent boots\n120\t      # reuse cached graphs and skip recompile. Pattern from Sandermage's PROD\n121\t      # launch (start_27b_int4_TQ_k8v4.sh:31-32). Closes club-3090 #22.\n122\t      - ../cache/torch_compile:/root/.cache/vllm/torch_compile_cache\n123\t      - ../cache/triton:/root/.triton/cache\n124\t      - ../patches/genesis/vllm/_genesis:/usr/local/lib/python3.12/dist-packages/vllm/_genesis:ro\n125\t      - ../patches/local/qwen3coder_tool_parser_deferred_commit.py:/patches/qwen3coder_tool_parser_deferred_commit.py:ro\n126\t      # workspace_lock_disable DROPPED on v7.69 — Genesis PN34 covers the\n127\t      # boot-time AND runtime _decode_attention paths. Opt-in via\n128\t      # GENESIS_ENABLE_PN34_WORKSPACE_LOCK_RELAX=1 in environment block.\n129\t      # patch_inputs_embeds_optional.py — backport of vllm#35975 (~64 MiB GPU +\n130\t      # 64 MiB pinned CPU savings on text-only models). Drops when PR merges.\n131\t      # NOTE: patch_pn12_ffn_pool_anchor.py + patch_pn12_compile_safe_custom_op.py\n132\t      # + patch_fa_max_seqlen_clamp.py all DROPPED — Genesis v7.65 covers their\n133\t      # surface natively (PN12 native + PN25 + PN17 + P15B). See migration\n134\t      # commit history if you need to resurrect.\n135\t    environment:\n136\t      # Uncomment the next line to pin to a specific GPU (e.g. GPU 0):\n137\t      # - CUDA_VISIBLE_DEVICES=0\n138\t      - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN:-}\n139\t      - VLLM_WORKER_MULTIPROC_METHOD=spawn\n140\t      - NCCL_CUMEM_ENABLE=0\n141\t      - NCCL_P2P_DISABLE=1\n142\t      # v0.20 made cudagraph memory profiling default-ON (vllm#38284), eats\n143\t      # ~0.0055 mem-util ≈ 120 MiB KV pool. Disabling restores ~4K of ctx.\n144\t      - VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS=0\n145\t      - VLLM_NO_USAGE_STATS=1\n146\t      # expandable_segments:True crashes boot on some setups (likely cuMemMap path).\n147\t      # Known: JusefPol on NVLink (PR #31), WSL2 single-card 3090 Ti.\n148\t      # Override via .env: PYTORCH_CUDA_ALLOC_CONF=expandable_segments:False\n149\t      - PYTORCH_CUDA_ALLOC_CONF=${PYTORCH_CUDA_ALLOC_CONF:-expandable_segments:True,max_split_size_mb:512}\n150\t      - VLLM_FLOAT32_MATMUL_PRECISION=high\n151\t      - VLLM_USE_FLASHINFER_SAMPLER=1\n152\t      - OMP_NUM_THREADS=1\n153\t      - CUDA_DEVICE_MAX_CONNECTIONS=8\n154\t      - VLLM_ALLOW_LONG_MAX_MODEL_LEN=1\n155\t      - VLLM_MARLIN_USE_ATOMIC_ADD=1\n156\t      - TRITON_CACHE_DIR=/root/.triton/cache\n157\t      # FULL Genesis v7.65 PROD env-var set per Sandermage's\n158\t      # bare_metal_27b_int4_TQ_k8v4.sh. Validated 2026-05-01 PM dual-3090:\n159\t      # +50% TPS over the prior subset (code 77 → 116 wall_TPS).\n160\t      - GENESIS_ENABLE_P4=1\n161\t      - GENESIS_ENABLE_P58_ASYNC_PLACEHOLDER_FIX=1\n162\t      - GENESIS_ENABLE_P60_GDN_NGRAM_FIX=1\n163\t      - GENESIS_ENABLE_P60B_TRITON_KERNEL=1\n164\t      - GENESIS_ENABLE_P61_QWEN3_MULTI_TOOL=1\n165\t      - GENESIS_ENABLE_P61B_STREAMING_OVERLAP=1\n166\t      - GENESIS_ENABLE_P62_STRUCT_OUT_SPEC_TIMING=1\n167\t      - GENESIS_ENABLE_P64_QWEN3CODER_MTP_STREAMING=1\n168\t      # P65 (cudagraph downgrade workaround) dropped 2026-05-03 — Genesis\n169\t      # v7.69 dispatcher v2 declares P65 mutually exclusive with P67/P67b\n170\t      # (the proper Triton kernel fix that supersedes the workaround).\n171\t      - GENESIS_ENABLE_P66_CUDAGRAPH_SIZE_FILTER=1\n172\t      - GENESIS_ENABLE_P67_TQ_MULTI_QUERY_KERNEL=1\n173\t      # P68/P69 — v7.65 raised default threshold 8000 → 50000 chars (~12.5K\n174\t      # tokens). Now safe with typical IDE-agent system+tool prompts; below\n175\t      # the threshold both gates are no-op. Override via the env var if needed.\n176\t      - GENESIS_ENABLE_P68_AUTO_FORCE_TOOL=1\n177\t      - GENESIS_ENABLE_P69_LONG_CTX_TOOL_REMINDER=1\n178\t      - GENESIS_P68_P69_LONG_CTX_THRESHOLD_CHARS=50000\n179\t      - GENESIS_ENABLE_P72_PROFILE_RUN_CAP=1\n180\t      - GENESIS_PROFILE_RUN_CAP_M=4128\n181\t      - GENESIS_ENABLE_P74_CHUNK_CLAMP=1\n182\t      - GENESIS_ENABLE_P83=1\n183\t      # P85 (hybrid fine-shadow prefix cache) dropped 2026-05-03 — Genesis\n184\t      # v7.69 dispatcher v2 declares P85 requires P84 (hash_block_size\n185\t      # override) which we don't enable. P85 is opt-in optimization not\n186\t      # load-bearing for correctness; enabling P84 separately is a future\n187\t      # decision (would need bench validation).\n188\t      - GENESIS_ENABLE_P87=1\n189\t      - GENESIS_ENABLE_P91=1\n190\t      - GENESIS_ENABLE_P94=1\n191\t      - GENESIS_ENABLE_P98=1\n192\t      - GENESIS_ENABLE_P99=1\n193\t      - GENESIS_ENABLE_P100=1\n194\t      - GENESIS_ENABLE_P101=1\n195\t      # ─── Cliff 2 closure recipe (v7.69, single-24GB-GPU TP=1) ──────────\n196\t      # P103 closes the FLA inner h tensor via chunk.py self-install hook\n197\t      # (v7.69 F2 fix — survives `exec vllm serve` worker spawn).\n198\t      # PN32 v2 closes the outer FLA call via _forward_core direct patch\n199\t      # + initial_state threading (v7.69 F3 fix — composes with P103).\n200\t      # Both required for Cliff 2 ceiling > 50K on TP=1.\n201\t      - GENESIS_ENABLE_P103=1\n202\t      - GENESIS_ENABLE_PN32_GDN_CHUNKED_PREFILL=1\n203\t      - GENESIS_PN32_GDN_CHUNK_SIZE=8192\n204\t      - GENESIS_PN32_GDN_CHUNK_THRESHOLD=16384\n205\t      - GENESIS_FLA_FWD_H_MAX_T=16384\n206\t      # PN34 (workspace_lock relaxation, opt-in default OFF). Replaces our\n207\t      # patch_workspace_lock_disable.py sidecar.\n208\t      - GENESIS_ENABLE_PN34_WORKSPACE_LOCK_RELAX=1\n209\t      - GENESIS_ENABLE_PN8_MTP_DRAFT_ONLINE_QUANT=1\n210\t      - GENESIS_ENABLE_PN9_INDEPENDENT_DRAFTER_ATTN=1\n211\t      - GENESIS_ENABLE_PN11_GDN_AB_CONTIGUOUS=1\n212\t      - GENESIS_ENABLE_PN12_FFN_INTERMEDIATE_POOL=1\n213\t      - GENESIS_ENABLE_PN13_CUDA_GRAPH_LAMBDA_ARITY=1\n214\t      - GENESIS_ENABLE_PN14_TQ_DECODE_OOB_CLAMP=1\n215\t      - GENESIS_ENABLE_PN17_FA2_LSE_CLAMP=1\n216\t      - GENESIS_ENABLE_PN25_SILU_INDUCTOR_SAFE=1\n217\t      # PN30 — RE-ENABLED with our local dst-shaped temp fix\n218\t      # (`patch_pn30_dst_shaped_temp_fix.py`, applied during setup.sh). The\n219\t      # original Sander a9977d8 .contiguous() approach corrupted DS row strides\n220\t      # by raw-memcpying a compact 10240×5 buffer into a 10240×6 destination\n221\t      # block (row 1+ landed at the wrong destination offset, causing later\n222\t      # TQ store CUDA assert). Our fix builds a destination-shaped temp inside\n223\t      # `collect_mamba_copy_meta` and reuses PN30's temp-list lifetime handling.\n224\t      # Diagnosis credit: ChatGPT/Codex CLI cross-check 2026-05-01 PM.\n225\t      - GENESIS_ENABLE_PN30_DS_LAYOUT_SPEC_DECODE=1\n226\t      # PN31 — DISABLED on 1×3090. Per-shape persistent VRAM grows as new\n227\t      # shapes are seen during prefill; on TP=1 + 24GB, the residence cost\n228\t      # outpaces the malloc-pressure relief. Sander explicitly warned in\n229\t      # `753344b` he couldn't validate on 24GB; cross-rig validated 2026-05-01\n230\t      # PM here that 0.95→0.93 mem-util can't carry both PN31 + DeltaNet\n231\t      # `chunk_fwd_o` activation peak at 29K depth. Reported back on\n232\t      # Sandermage/genesis-vllm-patches#15. Use tools-text.yml fp8 path for\n233\t      # 25K+ tool-RETURN workloads in the meantime.\n234\t      # - GENESIS_ENABLE_PN31_FA_VARLEN_PERSISTENT_OUT=1\n235\t      - GENESIS_ENABLE_PN19_SCOPED_MAX_SPLIT=1\n236\t      - GENESIS_ENABLE_PN22_LOCAL_ARGMAX_TP=1\n237\t      # PN26b sparse-V Triton kernel — first SM86 (Ampere consumer) sparse-V\n238\t      # kernel in any public tree. 27B-specific tuning per Sandermage's PROD.\n239\t      - GENESIS_ENABLE_PN26_SPARSE_V=1\n240\t      - GENESIS_ENABLE_PN59_STREAMING_GDN=1\n241\t      - GENESIS_PN59_DEBUG=1\n242\t      - GENESIS_PN26_SPARSE_V_BLOCK_KV=8\n243\t      - GENESIS_PN26_SPARSE_V_NUM_WARPS=4\n244\t      - GENESIS_PN26_SPARSE_V_THRESHOLD=0.01\n245\t      # v7.65 #14 + #15 fixes (compile-safe in-source hook + FA varlen clamp).\n246\t      # Replace our local patch_pn12_compile_safe_custom_op.py + P104 sidecars.\n247\t      - GENESIS_ENABLE_P38B_COMPILE_SAFE=1\n248\t      - GENESIS_ENABLE_P15B_FA_VARLEN_CLAMP=1\n249\t      - GENESIS_PREALLOC_TOKEN_BUDGET=4128\n250\t      - GENESIS_BUFFER_MODE=shared\n251\t      # Explicit OFFs to match Sandermage's PROD env-var set verbatim:\n252\t      # P78 (P78_TOLIST_CAPTURE_GUARD) — superseded by our patch_tolist_cudagraph.py\n253\t      # P81 (FP8 block-scaled M<=8) — FP8-specific, no-op on our TQ3 path\n254\t      # P82 — biased on small-batch single-stream Lorbus INT4 + MTP K=3 (Sander PROD)\n255\t      - GENESIS_ENABLE_P78_TOLIST_CAPTURE_GUARD=0\n256\t      - GENESIS_ENABLE_P81_FP8_BLOCK_SCALED_M_LE_8=0\n257\t      - GENESIS_ENABLE_P82=0\n258\t      - GENESIS_P82_THRESHOLD_SINGLE=0.3\n259\t      # P82 stays OFF — biased on small-batch single-stream Lorbus INT4 + MTP K=3\n260\t      # per Sandermage's PROD memory feedback_p82_*. P78 stays OFF (deprecated).\n261\t      # vLLM env knobs from Sandermage's launch:\n262\t      # VLLM_SSM_CONV_STATE_LAYOUT=DS — RE-ENABLED with our local PN30 fix\n263\t      # (`patch_pn30_dst_shaped_temp_fix.py`, applied during setup.sh).\n264\t      # Without our fix, Sander's PN30 a9977d8 corrupts DS row strides on\n265\t      # spec-decode AL>1 paths. With our fix, PN30 builds a destination-shaped\n266\t      # temp instead of a compact one, preserving DS layout. +6% TPS retained.\n267\t      - VLLM_SSM_CONV_STATE_LAYOUT=DS\n268\t      - VLLM_USE_FUSED_MOE_GROUPED_TOPK=1\n269\t    shm_size: \"16gb\"\n270\t    ipc: host\n271\t    deploy:\n272\t      resources:\n273\t        reservations:\n274\t          devices:\n275\t            - driver: nvidia\n276\t              count: all\n277\t              capabilities: [gpu]\n278\t    entrypoint:\n279\t      - /bin/bash\n280\t      - -c\n281\t      - |\n282\t        set -e\n283\t        pip install xxhash pandas scipy -q\n284\t        # Pre-flight: Genesis patches must be populated. Empty volume mount\n285\t        # = silent no-op apply_all = boot fails later with cryptic upstream\n286\t        # error (e.g. \"TurboQuant KV not supported for hybrid models\", #13).\n287\t        if [ ! -f /usr/local/lib/python3.12/dist-packages/vllm/_genesis/patches/apply_all.py ]; then\n288\t          echo \"ERROR: Genesis patches missing — host volume models/qwen3.6-27b/vllm/patches/genesis/ is empty.\" >&2\n289\t          echo \"       Run from repo root before 'docker compose up':\" >&2\n290\t          echo \"         bash scripts/setup.sh qwen3.6-27b\" >&2\n291\t          exit 1\n292\t        fi\n293\t        python3 -m vllm._genesis.patches.apply_all\n294\t        # Tool-parser deferred-commit fix for qwen3coder SSE-silence bug (issue #72).\n295\t        # Drops out when vllm-project/vllm lands the upstream fix.\n296\t        python3 /patches/qwen3coder_tool_parser_deferred_commit.py\n297\t        # patch_tolist_cudagraph.py — still needed pending upstream\n298\t        # vllm#40807 .tolist() capture-stream guard fix.\n299\t        # patch_inputs_embeds_optional.py — backport of vllm#35975\n300\t        # (free ~64 MiB GPU + ~64 MiB pinned CPU on text-only models).\n301\t        # Drops when PR merges upstream.\n302\t        # VLLM_ENFORCE_EAGER=1 in compose/.env disables CUDA graphs — use on\n303\t        # hardware where Cliff 2 GDN activation spikes occur at runtime.\n304\t        exec vllm serve ${VLLM_ENFORCE_EAGER:+--enforce-eager} \"$@\"\n305\t      - --\n306\t    command:\n307\t      - --model\n308\t      - /root/.cache/huggingface/qwen3.6-27b-autoround-int4\n309\t      - --served-model-name\n310\t      - qwen3.6-27b\n311\t      - --quantization\n312\t      - auto_round\n313\t      - --dtype\n314\t      - float16\n315\t      - --tensor-parallel-size\n316\t      - \"1\"\n317\t      # 180K + 0.97 — backed off from 214K + 0.985 on 2026-05-01 PM after\n318\t      # verify-stress probe 1 (10K-token long-context needle) crashed with\n319\t      # GDN forward OOM (`fla.ops.chunk.chunk_gated_delta_rule_fwd_h`\n320\t      # `k.new_empty(B, NT, H, V, K)` requested 98 MiB, 46 MiB free). At\n321\t      # 0.985 mem-util, activation budget was ~46 MiB at 10K prefill —\n322\t      # tight enough that ANY single prompt over a few thousand tokens was\n323\t      # a coin flip. The \"Cliff 2 fires only at 50-60K\" framing was\n324\t      # config-specific to the dev205 + 0.92 default; at tighter mem-util\n325\t      # the GDN forward buffer outpaces available activation much earlier.\n326\t      #\n327\t      # New target (validated 2026-05-01 PM via verify-stress.sh):\n328\t      # 180K + 0.97 → ~6.4 GB after weights, ~5.9 GB KV pool, ~500 MiB\n329\t      # activation safety. Probe 1 (10K needle) and probe 6 (math reasoning\n330\t      # at max_tokens=8192) PASS. Safe single-prompt envelope ~30K; full\n331\t      # 180K context for steady-state turn accumulation only.\n332\t      #\n333\t      # IDE-agent workloads (sys + tool schemas) STILL crash here per\n334\t      # club-3090#16 — different bug (Cliff 1 mech B inductor leak), not\n335\t      # fixed by mem-util tuning. Use tools-text.yml for those.\n336\t      - --max-model-len\n337\t      - \"${MAX_MODEL_LEN:-180000}\"\n338\t      # 0.95 — backed off from 0.97 on 2026-05-01 PM after PN25 v3 closed\n339\t      # Cliff 1 mech B at the FFN intermediate buffer (which now sits in\n340\t      # PN12+PN25's resident FFNIntermediateCache pool). Pool residence\n341\t      # adds ~140 MiB persistent VRAM, which tightened activation budget\n342\t      # at DeltaNet `chunk_fwd_o` (`o = torch.empty_like(v)`, 50 MiB) —\n343\t      # 0.97 left only 26 MiB free, OOM at 9.8K probe. 0.95 frees ~480 MiB\n344\t      # activation budget, validated up to 30K probes.\n345\t      - --gpu-memory-utilization\n346\t      - \"${GPU_MEMORY_UTILIZATION:-0.93}\"\n347\t      - --max-num-seqs\n348\t      - \"1\"\n349\t      - --max-num-batched-tokens\n350\t      - \"4128\"\n351\t      - --kv-cache-dtype\n352\t      - turboquant_3bit_nc\n353\t      # Drops the vision tower → frees ~1 GB VRAM → lifts engine ceiling from\n354\t      # 198K (long-vision at 0.98) to 218K (this variant at 0.985). This is the only difference vs long-vision.\n355\t      - --language-model-only\n356\t      - --trust-remote-code\n357\t      - --reasoning-parser\n358\t      - qwen3\n359\t      - --enable-auto-tool-choice\n360\t      - --tool-call-parser\n361\t      - qwen3_coder\n362\t      - --enable-prefix-caching\n363\t      - --enable-chunked-prefill\n364\t      - --no-scheduler-reserve-full-isl\n365\t      - --speculative-config\n366\t      - '{\"method\":\"mtp\",\"num_speculative_tokens\":3}'\n367\t      - --host\n368\t      - 0.0.0.0\n369\t      - --port\n370\t      - \"8000\"\n371\t",
    "chars": 21299
  },
  {
    "turn": 12,
    "user_msg": "Read <repo>/models/qwen3.6-27b/vllm/compose/docker-compose.long-text.yml and tell me what's relevant to the issue.",
    "tool_name": "Read",
    "tool_result": "1\t# ===========================================================================\n2\t# ⚠️  KNOWN ISSUE 2026-05-05 — Cliff 2b at >50K single-prompt context\n3\t#     Genesis v7.72.2 PN59 streaming-GDN was advertised as the structural\n4\t#     Cliff 2b fix but doesn't engage on the chunked-prefill code path\n5\t#     24 GB single-card configs are forced to take. Filed at:\n6\t#         https://github.com/Sandermage/genesis-vllm-patches/issues/22\n7\t#     Workarounds: use docker-compose.dual.yml / dual-turbo.yml (TP=2\n8\t#     escapes the cliff) or llamacpp recipes (different engine, no\n9\t#     Cliff 2b). See docs/CLIFFS.md for the full diagnosis.\n10\t# ===========================================================================\n11\t# Long-text variant — 214K context, text-only (no vision).\n12\t#\n13\t# ⚠️ NOT SAFE FOR IDE-AGENT WORKLOADS (Cline / OpenCode / Roo / Claude Code /\n14\t# Cursor / any flow with sys-prompt + tool schemas). Use `tools-text.yml`\n15\t# instead until Genesis PN25 lands default-on. See club-3090#16.\n16\t#\n17\t# Reproducer (2026-05-01 PM): a 5,900-char system prompt + 10 typical tool\n18\t# schemas + 346-char user request crashes the engine with a 98 MiB OOM at\n19\t# `empty_strided_cuda((s, 17408), ...)` in `inductor_cache/...py:1208`.\n20\t# vLLM's torch.compile inductor inlines `SiluAndMul.forward_native` and\n21\t# bypasses Genesis PN12's `FFNIntermediateCache` pool. PN25 (the proper\n22\t# compile-path opaque-op fix) is on Genesis dev but blocked by a worker-\n23\t# fork registration bug — see Sandermage/genesis-vllm-patches#16.\n24\t#\n25\t# Workloads that ARE safe on this compose:\n26\t#   - Pure long-form text generation (essay, doc Q&A) — no tool schemas in prompt\n27\t#   - RAG without tool calling (just context + question)\n28\t#   - Chat without tools\n29\t# Workloads that should use `tools-text.yml` (75K + fp8 KV + PN8) instead:\n30\t#   - Any IDE coding agent\n31\t#   - Any flow that puts tool/function schemas in the system prompt\n32\t#\n33\t# Stack: vLLM v0.20.1rc1.dev16 + Genesis v7.69 dev (commit 2db18df) +\n34\t# TurboQuant 3-bit KV + MTP n=3 + vision tower disabled.\n35\t#\n36\t# v7.69 key patches active:\n37\t#   - P103 (Cliff 2 chunked fwd_h+fwd_o orchestrator) — chunk.py self-install\n38\t#     hook survives `exec vllm serve` worker spawn (v7.69 F2 fix). Also\n39\t#     applied: Codex r1 cu_seqlens=[0,T] dense-single-seq gate fix that\n40\t#     allows the chunked path to engage on real serving.\n41\t#   - PN30 v7.68 part1+2+3 — DS conv state layout + spec-decode AL>1 fix\n42\t#     (drift-marker bug fixed in v7.69)\n43\t#   - PN32 v2 — GDN _forward_core chunked-prefill (Cliff 2 outer split)\n44\t#   - PN34 — workspace_lock relaxation (replaces our former\n45\t#     patch_workspace_lock_disable.py sidecar)\n46\t#   - PN26b: Genesis-original sparse-V Triton kernel for SM86 (Ampere\n47\t#     consumer) — first SM86 sparse-V kernel in any public tree. 27B\n48\t#     tuning: BLOCK_KV=8 num_warps=4 threshold=0.01.\n49\t#   - P38B / P15B: Cliff 1 mech B compile-safe hook + FA varlen clamp\n50\t#\n51\t# Cliff 2 closure recipe (cross-rig validated 2026-05-02 PM):\n52\t#   - vllm#35975 backport sidecar (`patch_inputs_embeds_optional.py`) frees\n53\t#     ~444 MiB at boot on text-only models (skips inputs_embeds buffers\n54\t#     for both gpu_model_runner.py + llm_base_proposer.py).\n55\t#   - --gpu-memory-utilization 0.93 — middle ground between 0.92 (cliff\n56\t#     just barely closed) and 0.95 (cliff fires). Recovers ~30K of KV\n57\t#     pool concurrency vs 0.92 while keeping ~494 MiB margin at peak.\n58\t#   - --max-model-len 180000 — engine pre-check admission ceiling on\n59\t#     this config (200K rejected: KV pool needs 3.31 GiB, only 2.19 GiB\n60\t#     available at 0.93). Cliff 2 single-prompt safe envelope: ~60K\n61\t#     verified pass; ~70-80K is the OOM ceiling.\n62\t#\n63\t# Validation (2026-05-02 PM, branch v7.69-cliff2-test):\n64\t#   - 60K MTP-on probe: HTTP 200 in 623s, recall correct, MTP AL=4.00\n65\t#   - 90K MTP-on probe: HTTP 500 OOM at 112s (50 MiB / 24.5 MiB free)\n66\t#   - Probes 2-6 (tool prefill, IDE-agent, multi-turn, LCB-coding,\n67\t#     reasoning-heavy): all PASS\n68\t#\n69\t# For workloads that need >60K single-prompt or 200K declared context,\n70\t# use docker-compose.long-text-no-mtp.yml (max-context safety variant\n71\t# trades MTP decode speedup for prefill stability). For coding agents\n72\t# on dual-card, use docker-compose.dual-turbo.yml.\n73\t#\n74\t# Recommended client max_tokens: 8192 (FREE thinking on this compose; budget\n75\t# for hard reasoning runs). 16384 for competition-grade problems. See\n76\t# docs/EXAMPLES.md \"max_tokens defaults\" section for the full table.\n77\t#\n78\t# When to pick this over long-vision:\n79\t#   - You don't need image input\n80\t#   - You want the maximum context on a single 3090\n81\t#   - You're doing long-context text workloads (codebase analysis, RAG\n82\t#     retrievals, multi-turn chat with steady-state accumulation)\n83\t#\n84\t# When NOT to pick this:\n85\t#   - Anything that needs vision — use long-vision (198K + vision) instead\n86\t#   - Single-shot prompts ≥50–60K tokens — Cliff 2 (DeltaNet GDN) still\n87\t#     applies on single-card. Route those to dual.yml (TP=2 splits the\n88\t#     state across cards) or llamacpp/default (262K, different engine).\n89\t#\n90\t# Local sidecar (single one remaining on this compose):\n91\t#   - patch_workspace_lock_disable.py — relaxes vllm#39226 strict\n92\t#     WorkspaceManager.lock() assertion to a one-shot WARNING. P98 in v7.65\n93\t#     covers this functionally but its drift marker auto-skips on v0.20\n94\t#     (`UNIFORM_SINGLE_TOKEN_DECODE` false-positive). Drop when Sandermage\n95\t#     ships the marker fix.\n96\t#\n97\t# Variants in this dir:\n98\t#   File                                  Ctx     KV       Vision   Notes\n99\t#   docker-compose.yml (default)          48K     TQ3      ✅       Production-safe; below both cliffs\n100\t#   docker-compose.long-vision.yml        198K    TQ3      ✅       Long ctx + vision (cliffs closed by v0.20 + v7.65)\n101\t#   docker-compose.long-text.yml (this)   214K    TQ3      ❌       Engine-ceiling text-only on v0.20 + 0.985 mem-util\n102\t#   docker-compose.bounded-thinking.yml   214K    TQ3      ❌       Long-text + structured-CoT grammar in <think>\n103\t#   docker-compose.tools-text.yml         75K     fp8      ❌       IDE agents — fp8 KV alternative\n104\t#\n105\t# Run with:\n106\t#   cd <repo>/models/qwen3.6-27b/vllm/compose\n107\t#   docker compose -f docker-compose.long-text.yml up -d\n108\t# ===========================================================================\n109\tservices:\n110\t  vllm-qwen36-27b-long-text:\n111\t    image: vllm/vllm-openai:nightly-01d4d1ad375dc5854779c593eee093bcebb0cada\n112\t    container_name: vllm-qwen36-27b-long-text\n113\t    restart: \"no\"\n114\t    ports:\n115\t      - \"${BIND_HOST:-0.0.0.0}:${PORT:-8020}:8000\"\n116\t    volumes:\n117\t      - ${MODEL_DIR:-../../../../models-cache}:/root/.cache/huggingface\n118\t      # torch.compile + Triton kernel caches — first boot warms (~60-90 sec\n119\t      # for Dynamo trace + Inductor compile + Triton JIT); subsequent boots\n120\t      # reuse cached graphs and skip recompile. Pattern from Sandermage's PROD\n121\t      # launch (start_27b_int4_TQ_k8v4.sh:31-32). Closes club-3090 #22.\n122\t      - ../cache/torch_compile:/root/.cache/vllm/torch_compile_cache\n123\t      - ../cache/triton:/root/.triton/cache\n124\t      - ../patches/genesis/vllm/_genesis:/usr/local/lib/python3.12/dist-packages/vllm/_genesis:ro\n125\t      - ../patches/local/qwen3coder_tool_parser_deferred_commit.py:/patches/qwen3coder_tool_parser_deferred_commit.py:ro\n126\t      # workspace_lock_disable DROPPED on v7.69 — Genesis PN34 covers the\n127\t      # boot-time AND runtime _decode_attention paths. Opt-in via\n128\t      # GENESIS_ENABLE_PN34_WORKSPACE_LOCK_RELAX=1 in environment block.\n129\t      # patch_inputs_embeds_optional.py — backport of vllm#35975 (~64 MiB GPU +\n130\t      # 64 MiB pinned CPU savings on text-only models). Drops when PR merges.\n131\t      # NOTE: patch_pn12_ffn_pool_anchor.py + patch_pn12_compile_safe_custom_op.py\n132\t      # + patch_fa_max_seqlen_clamp.py all DROPPED — Genesis v7.65 covers their\n133\t      # surface natively (PN12 native + PN25 + PN17 + P15B). See migration\n134\t      # commit history if you need to resurrect.\n135\t    environment:\n136\t      # Uncomment the next line to pin to a specific GPU (e.g. GPU 0):\n137\t      # - CUDA_VISIBLE_DEVICES=0\n138\t      - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN:-}\n139\t      - VLLM_WORKER_MULTIPROC_METHOD=spawn\n140\t      - NCCL_CUMEM_ENABLE=0\n141\t      - NCCL_P2P_DISABLE=1\n142\t      # v0.20 made cudagraph memory profiling default-ON (vllm#38284), eats\n143\t      # ~0.0055 mem-util ≈ 120 MiB KV pool. Disabling restores ~4K of ctx.\n144\t      # Override via .env: VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS=1 to\n145\t      # pre-reserve CUDA graph space (required when CUDA graphs are enabled\n146\t      # and VRAM headroom is tight — e.g. 5090 Laptop 24 GB).\n147\t      - VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS=${VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS:-0}\n148\t      - VLLM_NO_USAGE_STATS=1\n149\t      # expandable_segments:True crashes boot on some setups (likely cuMemMap path).\n150\t      # Known: JusefPol on NVLink (PR #31), WSL2 single-card 3090 Ti.\n151\t      # Override via .env: PYTORCH_CUDA_ALLOC_CONF=expandable_segments:False\n152\t      - PYTORCH_CUDA_ALLOC_CONF=${PYTORCH_CUDA_ALLOC_CONF:-expandable_segments:True,max_split_size_mb:512}\n153\t      - VLLM_FLOAT32_MATMUL_PRECISION=high\n154\t      - VLLM_USE_FLASHINFER_SAMPLER=1\n155\t      - OMP_NUM_THREADS=1\n156\t      - CUDA_DEVICE_MAX_CONNECTIONS=8\n157\t      - VLLM_ALLOW_LONG_MAX_MODEL_LEN=1\n158\t      - VLLM_MARLIN_USE_ATOMIC_ADD=1\n159\t      - TRITON_CACHE_DIR=/root/.triton/cache\n160\t      # FULL Genesis v7.65 PROD env-var set per Sandermage's\n161\t      # bare_metal_27b_int4_TQ_k8v4.sh. Validated 2026-05-01 PM dual-3090:\n162\t      # +50% TPS over the prior subset (code 77 → 116 wall_TPS).\n163\t      - GENESIS_ENABLE_P4=1\n164\t      - GENESIS_ENABLE_P58_ASYNC_PLACEHOLDER_FIX=1\n165\t      - GENESIS_ENABLE_P60_GDN_NGRAM_FIX=1\n166\t      - GENESIS_ENABLE_P60B_TRITON_KERNEL=1\n167\t      - GENESIS_ENABLE_P61_QWEN3_MULTI_TOOL=1\n168\t      - GENESIS_ENABLE_P61B_STREAMING_OVERLAP=1\n169\t      - GENESIS_ENABLE_P62_STRUCT_OUT_SPEC_TIMING=1\n170\t      - GENESIS_ENABLE_P64_QWEN3CODER_MTP_STREAMING=1\n171\t      # P65 (cudagraph downgrade workaround) dropped 2026-05-03 — Genesis\n172\t      # v7.69 dispatcher v2 declares P65 mutually exclusive with P67/P67b\n173\t      # (the proper Triton kernel fix that supersedes the workaround).\n174\t      - GENESIS_ENABLE_P66_CUDAGRAPH_SIZE_FILTER=1\n175\t      - GENESIS_ENABLE_P67_TQ_MULTI_QUERY_KERNEL=1\n176\t      # P68/P69 — v7.65 raised default threshold 8000 → 50000 chars (~12.5K\n177\t      # tokens). Now safe with typical IDE-agent system+tool prompts; below\n178\t      # the threshold both gates are no-op. Override via the env var if needed.\n179\t      - GENESIS_ENABLE_P68_AUTO_FORCE_TOOL=1\n180\t      - GENESIS_ENABLE_P69_LONG_CTX_TOOL_REMINDER=1\n181\t      - GENESIS_P68_P69_LONG_CTX_THRESHOLD_CHARS=50000\n182\t      - GENESIS_ENABLE_P72_PROFILE_RUN_CAP=1\n183\t      - GENESIS_PROFILE_RUN_CAP_M=4128\n184\t      - GENESIS_ENABLE_P74_CHUNK_CLAMP=1\n185\t      - GENESIS_ENABLE_P83=1\n186\t      # P85 (hybrid fine-shadow prefix cache) dropped 2026-05-03 — Genesis\n187\t      # v7.69 dispatcher v2 declares P85 requires P84 (hash_block_size\n188\t      # override) which we don't enable. P85 is opt-in optimization not\n189\t      # load-bearing for correctness; enabling P84 separately is a future\n190\t      # decision (would need bench validation).\n191\t      - GENESIS_ENABLE_P87=1\n192\t      - GENESIS_ENABLE_P91=1\n193\t      - GENESIS_ENABLE_P94=1\n194\t      - GENESIS_ENABLE_P98=1\n195\t      - GENESIS_ENABLE_P99=1\n196\t      - GENESIS_ENABLE_P100=1\n197\t      - GENESIS_ENABLE_P101=1\n198\t      # ─── Cliff 2 closure recipe (v7.69, single-24GB-GPU TP=1) ──────────\n199\t      # P103 closes the FLA inner h tensor via chunk.py self-install hook\n200\t      # (v7.69 F2 fix — survives `exec vllm serve` worker spawn).\n201\t      # PN32 v2 closes the outer FLA call via _forward_core direct patch\n202\t      # + initial_state threading (v7.69 F3 fix — composes with P103).\n203\t      # Both required for Cliff 2 ceiling > 50K on TP=1.\n204\t      - GENESIS_ENABLE_P103=1\n205\t      - GENESIS_ENABLE_PN32_GDN_CHUNKED_PREFILL=1\n206\t      - GENESIS_PN32_GDN_CHUNK_SIZE=8192\n207\t      - GENESIS_PN32_GDN_CHUNK_THRESHOLD=16384\n208\t      - GENESIS_FLA_FWD_H_MAX_T=16384\n209\t      # PN34 (workspace_lock relaxation, opt-in default OFF). Replaces our\n210\t      # patch_workspace_lock_disable.py sidecar.\n211\t      - GENESIS_ENABLE_PN34_WORKSPACE_LOCK_RELAX=1\n212\t      - GENESIS_ENABLE_PN8_MTP_DRAFT_ONLINE_QUANT=1\n213\t      - GENESIS_ENABLE_PN9_INDEPENDENT_DRAFTER_ATTN=1\n214\t      - GENESIS_ENABLE_PN11_GDN_AB_CONTIGUOUS=1\n215\t      - GENESIS_ENABLE_PN12_FFN_INTERMEDIATE_POOL=1\n216\t      - GENESIS_ENABLE_PN13_CUDA_GRAPH_LAMBDA_ARITY=1\n217\t      - GENESIS_ENABLE_PN14_TQ_DECODE_OOB_CLAMP=1\n218\t      - GENESIS_ENABLE_PN17_FA2_LSE_CLAMP=1\n219\t      - GENESIS_ENABLE_PN25_SILU_INDUCTOR_SAFE=1\n220\t      # PN30 — RE-ENABLED with our local dst-shaped temp fix\n221\t      # (`patch_pn30_dst_shaped_temp_fix.py`, applied during setup.sh). The\n222\t      # original Sander a9977d8 .contiguous() approach corrupted DS row strides\n223\t      # by raw-memcpying a compact 10240×5 buffer into a 10240×6 destination\n224\t      # block (row 1+ landed at the wrong destination offset, causing later\n225\t      # TQ store CUDA assert). Our fix builds a destination-shaped temp inside\n226\t      # `collect_mamba_copy_meta` and reuses PN30's temp-list lifetime handling.\n227\t      # Diagnosis credit: ChatGPT/Codex CLI cross-check 2026-05-01 PM.\n228\t      - GENESIS_ENABLE_PN30_DS_LAYOUT_SPEC_DECODE=1\n229\t      # PN31 — DISABLED on 1×3090. Per-shape persistent VRAM grows as new\n230\t      # shapes are seen during prefill; on TP=1 + 24GB, the residence cost\n231\t      # outpaces the malloc-pressure relief. Sander explicitly warned in\n232\t      # `753344b` he couldn't validate on 24GB; cross-rig validated 2026-05-01\n233\t      # PM here that 0.95→0.93 mem-util can't carry both PN31 + DeltaNet\n234\t      # `chunk_fwd_o` activation peak at 29K depth. Reported back on\n235\t      # Sandermage/genesis-vllm-patches#15. Use tools-text.yml fp8 path for\n236\t      # 25K+ tool-RETURN workloads in the meantime.\n237\t      # - GENESIS_ENABLE_PN31_FA_VARLEN_PERSISTENT_OUT=1\n238\t      - GENESIS_ENABLE_PN19_SCOPED_MAX_SPLIT=1\n239\t      - GENESIS_ENABLE_PN22_LOCAL_ARGMAX_TP=1\n240\t      # PN26b sparse-V Triton kernel — first SM86 (Ampere consumer) sparse-V\n241\t      # kernel in any public tree. 27B-specific tuning per Sandermage's PROD.\n242\t      - GENESIS_ENABLE_PN26_SPARSE_V=1\n243\t      - GENESIS_ENABLE_PN59_STREAMING_GDN=1\n244\t      - GENESIS_PN59_DEBUG=1\n245\t      - GENESIS_PN26_SPARSE_V_BLOCK_KV=8\n246\t      - GENESIS_PN26_SPARSE_V_NUM_WARPS=4\n247\t      - GENESIS_PN26_SPARSE_V_THRESHOLD=0.01\n248\t      # v7.65 #14 + #15 fixes (compile-safe in-source hook + FA varlen clamp).\n249\t      # Replace our local patch_pn12_compile_safe_custom_op.py + P104 sidecars.\n250\t      - GENESIS_ENABLE_P38B_COMPILE_SAFE=1\n251\t      - GENESIS_ENABLE_P15B_FA_VARLEN_CLAMP=1\n252\t      - GENESIS_PREALLOC_TOKEN_BUDGET=4128\n253\t      - GENESIS_BUFFER_MODE=shared\n254\t      # Explicit OFFs to match Sandermage's PROD env-var set verbatim:\n255\t      # P78 (P78_TOLIST_CAPTURE_GUARD) — superseded by our patch_tolist_cudagraph.py\n256\t      # P81 (FP8 block-scaled M<=8) — FP8-specific, no-op on our TQ3 path\n257\t      # P82 — biased on small-batch single-stream Lorbus INT4 + MTP K=3 (Sander PROD)\n258\t      - GENESIS_ENABLE_P78_TOLIST_CAPTURE_GUARD=0\n259\t      - GENESIS_ENABLE_P81_FP8_BLOCK_SCALED_M_LE_8=0\n260\t      # P82 — off by default (biased toward Lorbus INT4 + MTP K=3 single-stream).\n261\t      # Enable via .env: GENESIS_ENABLE_P82=1 to test on this rig.\n262\t      - GENESIS_ENABLE_P82=${GENESIS_ENABLE_P82:-0}\n263\t      - GENESIS_P82_THRESHOLD_SINGLE=0.3\n264\t      # P40 — TQ k8v4 GQA grouping kernel (+15-30% on compute-regime GPUs with L2>=24MB).\n265\t      # RTX 5090 Laptop: 88MB L2 → enable via .env: GENESIS_ENABLE_P40=1\n266\t      # RTX 3090: 6MB L2 → no gain, default off.\n267\t      - GENESIS_ENABLE_P40=${GENESIS_ENABLE_P40:-0}\n268\t      # vLLM env knobs from Sandermage's launch:\n269\t      # VLLM_SSM_CONV_STATE_LAYOUT=DS — RE-ENABLED with our local PN30 fix\n270\t      # (`patch_pn30_dst_shaped_temp_fix.py`, applied during setup.sh).\n271\t      # Without our fix, Sander's PN30 a9977d8 corrupts DS row strides on\n272\t      # spec-decode AL>1 paths. With our fix, PN30 builds a destination-shaped\n273\t      # temp instead of a compact one, preserving DS layout. +6% TPS retained.\n274\t      - VLLM_SSM_CONV_STATE_LAYOUT=DS\n275\t      - VLLM_USE_FUSED_MOE_GROUPED_TOPK=1\n276\t    shm_size: \"16gb\"\n277\t    ipc: host\n278\t    deploy:\n279\t      resources:\n280\t        reservations:\n281\t          devices:\n282\t            - driver: nvidia\n283\t              count: all\n284\t              capabilities: [gpu]\n285\t    entrypoint:\n286\t      - /bin/bash\n287\t      - -c\n288\t      - |\n289\t        set -e\n290\t        pip install xxhash pandas scipy -q\n291\t        # Pre-flight: Genesis patches must be populated. Empty volume mount\n292\t        # = silent no-op apply_all = boot fails later with cryptic upstream\n293\t        # error (e.g. \"TurboQuant KV not supported for hybrid models\", #13).\n294\t        if [ ! -f /usr/local/lib/python3.12/dist-packages/vllm/_genesis/patches/apply_all.py ]; then\n295\t          echo \"ERROR: Genesis patches missing — host volume models/qwen3.6-27b/vllm/patches/genesis/ is empty.\" >&2\n296\t          echo \"       Run from repo root before 'docker compose up':\" >&2\n297\t          echo \"         bash scripts/setup.sh qwen3.6-27b\" >&2\n298\t          exit 1\n299\t        fi\n300\t        python3 -m vllm._genesis.patches.apply_all\n301\t        # Tool-parser deferred-commit fix for qwen3coder SSE-silence bug (issue #72).\n302\t        # Drops out when vllm-project/vllm lands the upstream fix.\n303\t        python3 /patches/qwen3coder_tool_parser_deferred_commit.py\n304\t        # patch_tolist_cudagraph.py — still needed pending upstream\n305\t        # vllm#40807 .tolist() capture-stream guard fix.\n306\t        # patch_inputs_embeds_optional.py — backport of vllm#35975\n307\t        # (free ~64 MiB GPU + ~64 MiB pinned CPU on text-only models).\n308\t        # Drops when PR merges upstream.\n309\t        # VLLM_ENFORCE_EAGER=1 in compose/.env disables CUDA graphs — use on\n310\t        # hardware where Cliff 2 GDN activation spikes occur at runtime.\n311\t        exec vllm serve ${VLLM_ENFORCE_EAGER:+--enforce-eager} \"$@\"\n312\t      - --\n313\t    command:\n314\t      - --model\n315\t      - /root/.cache/huggingface/qwen3.6-27b-autoround-int4\n316\t      - --served-model-name\n317\t      - qwen3.6-27b\n318\t      - --quantization\n319\t      - auto_round\n320\t      - --dtype\n321\t      - float16\n322\t      - --tensor-parallel-size\n323\t      - \"1\"\n324\t      # 180K + 0.97 — backed off from 214K + 0.985 on 2026-05-01 PM after\n325\t      # verify-stress probe 1 (10K-token long-context needle) crashed with\n326\t      # GDN forward OOM (`fla.ops.chunk.chunk_gated_delta_rule_fwd_h`\n327\t      # `k.new_empty(B, NT, H, V, K)` requested 98 MiB, 46 MiB free). At\n328\t      # 0.985 mem-util, activation budget was ~46 MiB at 10K prefill —\n329\t      # tight enough that ANY single prompt over a few thousand tokens was\n330\t      # a coin flip. The \"Cliff 2 fires only at 50-60K\" framing was\n331\t      # config-specific to the dev205 + 0.92 default; at tighter mem-util\n332\t      # the GDN forward buffer outpaces available activation much earlier.\n333\t      #\n334\t      # New target (validated 2026-05-01 PM via verify-stress.sh):\n335\t      # 180K + 0.97 → ~6.4 GB after weights, ~5.9 GB KV pool, ~500 MiB\n336\t      # activation safety. Probe 1 (10K needle) and probe 6 (math reasoning\n337\t      # at max_tokens=8192) PASS. Safe single-prompt envelope ~30K; full\n338\t      # 180K context for steady-state turn accumulation only.\n339\t      #\n340\t      # IDE-agent workloads (sys + tool schemas) STILL crash here per\n341\t      # club-3090#16 — different bug (Cliff 1 mech B inductor leak), not\n342\t      # fixed by mem-util tuning. Use tools-text.yml for those.\n343\t      - --max-model-len\n344\t      - \"${MAX_MODEL_LEN:-180000}\"\n345\t      # 0.95 — backed off from 0.97 on 2026-05-01 PM after PN25 v3 closed\n346\t      # Cliff 1 mech B at the FFN intermediate buffer (which now sits in\n347\t      # PN12+PN25's resident FFNIntermediateCache pool). Pool residence\n348\t      # adds ~140 MiB persistent VRAM, which tightened activation budget\n349\t      # at DeltaNet `chunk_fwd_o` (`o = torch.empty_like(v)`, 50 MiB) —\n350\t      # 0.97 left only 26 MiB free, OOM at 9.8K probe. 0.95 frees ~480 MiB\n351\t      # activation budget, validated up to 30K probes.\n352\t      - --gpu-memory-utilization\n353\t      - \"${GPU_MEMORY_UTILIZATION:-0.93}\"\n354\t      - --max-num-seqs\n355\t      - \"1\"\n356\t      - --max-num-batched-tokens\n357\t      - \"${MAX_NUM_BATCHED_TOKENS:-4128}\"\n358\t      - --kv-cache-dtype\n359\t      - turboquant_3bit_nc\n360\t      # Drops the vision tower → frees ~1 GB VRAM → lifts engine ceiling from\n361\t      # 198K (long-vision at 0.98) to 218K (this variant at 0.985). This is the only difference vs long-vision.\n362\t      - --language-model-only\n363\t      - --trust-remote-code\n364\t      - --reasoning-parser\n365\t      - qwen3\n366\t      - --enable-auto-tool-choice\n367\t      - --tool-call-parser\n368\t      - qwen3_coder\n369\t      - --enable-prefix-caching\n370\t      - --enable-chunked-prefill\n371\t      - --no-scheduler-reserve-full-isl\n372\t      - --speculative-config\n373\t      - '{\"method\":\"mtp\",\"num_speculative_tokens\":3}'\n374\t      - --host\n375\t      - 0.0.0.0\n376\t      - --port\n377\t      - \"8000\"\n378\t",
    "chars": 21883
  },
  {
    "turn": 13,
    "user_msg": "Read <repo>/scripts/soak-test.sh and tell me what's relevant to the issue.",
    "tool_name": "Read",
    "tool_result": "1\t#!/usr/bin/env bash\n2\t#\n3\t# Soak test - runtime VRAM accretion / multi-turn traffic validation.\n4\t#\n5\t# Run before shipping config, Genesis, vLLM, or memory-policy changes that can\n6\t# pass verify-full.sh and verify-stress.sh but still accrete VRAM under\n7\t# repeated agent turns. This is intentionally slow and not part of launch.\n8\t#\n9\t# Scope:\n10\t#   - Single-stream multi-turn agent traffic, no concurrency stress.\n11\t#   - Watches VRAM growth, engine liveness, TTFT growth, and decode TPS\n12\t#     retention across sessions.\n13\t#   - Read-only against the running deployment.\n14\t#\n15\t# Time budget:\n16\t#   Default SOAK_SESSIONS=20 x SOAK_TURNS=5, capped by SOAK_TIMEOUT_S=1800.\n17\t#   Expect 10-30 minutes depending on config.\n18\t#\n19\t# Usage:\n20\t#   CONTAINER=vllm-qwen36-27b-long-vision bash scripts/soak-test.sh\n21\t#   ENDPOINT=http://localhost:8020 SOAK_SESSIONS=8 bash scripts/soak-test.sh\n22\t#\n23\t# Env:\n24\t#   CONTAINER              Running container. Default: first vllm-qwen36-27b*\n25\t#                          container from `docker ps`.\n26\t#   ENDPOINT / URL         OpenAI endpoint. Default: mapped container port for\n27\t#                          8000/tcp, falling back to http://localhost:8020.\n28\t#   MODEL                  Served model. Default: first id from /v1/models.\n29\t#   SOAK_MODE              \"fresh\" (default) — each turn is an independent\n30\t#                          conversation; tests raw VRAM accretion across\n31\t#                          requests. \"continuous\" — each session is one\n32\t#                          multi-turn agentic conversation that ramps to\n33\t#                          ~22-25K accumulated context by turn 5; tests the\n34\t#                          context-accumulation accretion class that bit\n35\t#                          club-3090#41 (hermes/openhands traffic).\n36\t#   SOAK_SESSIONS          Independent sessions. Default: 20.\n37\t#   SOAK_TURNS             Turns per session, max 5 fixture shapes. Default: 5.\n38\t#                          (Continuous mode requires SOAK_TURNS=5 — the\n39\t#                          turn shapes are designed to ramp; partial\n40\t#                          sessions don't reach the target context size.)\n41\t#   SOAK_MAX_GROWTH_MIB    Fail if max VRAM growth exceeds this after warm\n42\t#                          baseline. Default: 200 MiB.\n43\t#   SOAK_TIMEOUT_S         Hard wall-clock cap. Default: 1800 seconds.\n44\t#   SOAK_REQ_TIMEOUT_S     Per-request timeout. Default: 600 seconds.\n45\t#   SOAK_OUTPUT            Output dir. Default: results/soak-YYYYmmdd-HHMMSS.\n46\t#\n47\t# Outputs:\n48\t#   results/<run>/baseline.json\n49\t#   results/<run>/turn-log.csv\n50\t#   results/<run>/gpu-log.csv\n51\t#   results/<run>/summary.md\n52\t#\n53\t# Exit codes:\n54\t#   0 pass\n55\t#   1 fail\n56\t#   2 inconclusive / timeout / preflight could not run\n57\t\n58\tset -euo pipefail\n59\t\n60\tSOAK_SESSIONS=\"${SOAK_SESSIONS:-20}\"\n61\tSOAK_TURNS=\"${SOAK_TURNS:-5}\"\n62\tSOAK_MODE=\"${SOAK_MODE:-fresh}\"\n63\tSOAK_MAX_GROWTH_MIB=\"${SOAK_MAX_GROWTH_MIB:-200}\"\n64\tSOAK_TIMEOUT_S=\"${SOAK_TIMEOUT_S:-1800}\"\n65\tSOAK_REQ_TIMEOUT_S=\"${SOAK_REQ_TIMEOUT_S:-600}\"\n66\tSOAK_OUTPUT=\"${SOAK_OUTPUT:-results/soak-$(date +%Y%m%d-%H%M%S)}\"\n67\t\n68\tcase \"$SOAK_MODE\" in\n69\t  fresh|continuous) ;;\n70\t  *) echo \"ERROR: SOAK_MODE='${SOAK_MODE}' — must be 'fresh' or 'continuous'.\" >&2; exit 2 ;;\n71\tesac\n72\tif [[ \"$SOAK_MODE\" == \"continuous\" && \"$SOAK_TURNS\" -ne 5 ]]; then\n73\t  echo \"ERROR: continuous mode requires SOAK_TURNS=5 (got ${SOAK_TURNS}). Turn shapes are designed to ramp; partial runs don't reach target context size.\" >&2\n74\t  exit 2\n75\tfi\n76\t\n77\tREPO_ROOT=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")/..\" && pwd)\"\n78\tHELPER=\"${REPO_ROOT}/scripts/soak-helper.py\"\n79\tcd \"$REPO_ROOT\"\n80\t\n81\tlog() { printf '[soak] %s\\n' \"$*\"; }\n82\tdie() { log \"ERROR: $*\"; exit 2; }\n83\tneed() { command -v \"$1\" >/dev/null 2>&1 || die \"'$1' not found in PATH\"; }\n84\tsoft_need() { command -v \"$1\" >/dev/null 2>&1; }\n85\t\n86\tneed curl\n87\tneed nvidia-smi\n88\tneed python3\n89\t[[ -x \"$HELPER\" || -f \"$HELPER\" ]] || die \"missing helper: $HELPER\"\n90\t\n91\t# docker is soft-required: only needed for container-mode tracking\n92\t# (docker stats + docker logs scrape). Host engines (e.g. llama.cpp host\n93\t# build, see #85, #87) use CONTAINER=none and run without docker.\n94\tHAVE_DOCKER=0\n95\tif soft_need docker; then HAVE_DOCKER=1; fi\n96\tif [[ \"${CONTAINER:-}\" == \"none\" ]]; then\n97\t  HOST_MODE=1\n98\telif [[ \"$HAVE_DOCKER\" == \"0\" ]]; then\n99\t  log \"docker not in PATH — running in host mode (CONTAINER=none implied)\"\n100\t  HOST_MODE=1\n101\t  CONTAINER=\"none\"\n102\telse\n103\t  HOST_MODE=0\n104\tfi\n105\t\n106\tauto_container() {\n107\t  docker ps --format '{{.Names}}' 2>/dev/null \\\n108\t    | grep -E '^(vllm-qwen36-27b|vllm-gemma-4-31b)' \\\n109\t    | head -1 || true\n110\t}\n111\t\n112\tendpoint_from_container() {\n113\t  local container=\"$1\"\n114\t  local mapped port\n115\t  mapped=\"$(docker port \"$container\" 8000/tcp 2>/dev/null | head -1 || true)\"\n116\t  if [[ -n \"$mapped\" ]]; then\n117\t    port=\"${mapped##*:}\"\n118\t    [[ \"$port\" =~ ^[0-9]+$ ]] && { printf 'http://localhost:%s\\n' \"$port\"; return 0; }\n119\t  fi\n120\t  printf 'http://localhost:8020\\n'\n121\t}\n122\t\n123\tvram_mib() {\n124\t  nvidia-smi --query-gpu=memory.used --format=csv,noheader,nounits 2>/dev/null \\\n125\t    | awk -F, '{gsub(/ /, \"\", $1); sum += $1} END {print sum + 0}'\n126\t}\n127\t\n128\tappend_gpu_snapshot() {\n129\t  local session=\"$1\"\n130\t  local turn=\"$2\"\n131\t  nvidia-smi --query-gpu=index,memory.used,utilization.gpu --format=csv,noheader,nounits 2>/dev/null \\\n132\t    | awk -F, -v s=\"$session\" -v t=\"$turn\" '{\n133\t        for (i = 1; i <= NF; i++) gsub(/^ +| +$/, \"\", $i)\n134\t        printf \"%s,%s,%s,%s,%s\\n\", s, t, $1, $2, $3\n135\t      }' >> \"$GPU_LOG\" || true\n136\t}\n137\t\n138\tcapture_state() {\n139\t  local label=\"$1\"\n140\t  nvidia-smi --query-gpu=index,name,memory.used,memory.total,utilization.gpu,power.draw,temperature.gpu \\\n141\t    --format=csv,noheader,nounits > \"${SOAK_OUTPUT}/nvidia-smi-${label}.csv\" 2>/dev/null || true\n142\t  if [[ \"$HOST_MODE\" == \"0\" ]]; then\n143\t    docker stats --no-stream --format '{{json .}}' \"$CONTAINER\" \\\n144\t      > \"${SOAK_OUTPUT}/docker-stats-${label}.jsonl\" 2>/dev/null || true\n145\t  fi\n146\t}\n147\t\n148\tfinish() {\n149\t  local rc=$?\n150\t  capture_state \"final\"\n151\t  log \"artifacts: ${SOAK_OUTPUT}\"\n152\t  exit \"$rc\"\n153\t}\n154\ttrap finish EXIT\n155\ttrap 'log \"interrupted\"; exit 2' INT TERM\n156\t\n157\tif [[ \"$HOST_MODE\" == \"1\" ]]; then\n158\t  log \"host mode: CONTAINER=none — skipping docker checks (URL must be set or auto-detected)\"\n159\t  CONTAINER=\"none\"\n160\telse\n161\t  CONTAINER=\"${CONTAINER:-$(auto_container)}\"\n162\t  [[ -n \"$CONTAINER\" ]] || die \"no running vllm-qwen36-27b*/vllm-gemma-4-31b* container found; set CONTAINER=... or CONTAINER=none for host engines\"\n163\t  docker inspect \"$CONTAINER\" >/dev/null 2>&1 || die \"container '$CONTAINER' not found (use CONTAINER=none for host engine builds)\"\n164\t  [[ \"$(docker inspect -f '{{.State.Running}}' \"$CONTAINER\" 2>/dev/null || echo false)\" == \"true\" ]] \\\n165\t    || die \"container '$CONTAINER' is not running\"\n166\tfi\n167\t\n168\tif [[ \"$HOST_MODE\" == \"1\" ]]; then\n169\t  # Host mode: URL must be set explicitly (or fall back to localhost:8020).\n170\t  # We can't sniff a port from a container that doesn't exist.\n171\t  ENDPOINT=\"${ENDPOINT:-${URL:-http://localhost:8020}}\"\n172\telse\n173\t  ENDPOINT=\"${ENDPOINT:-${URL:-$(endpoint_from_container \"$CONTAINER\")}}\"\n174\tfi\n175\tmkdir -p \"$SOAK_OUTPUT\"\n176\t\n177\tMODELS_JSON=\"${SOAK_OUTPUT}/models.json\"\n178\tcurl -sf -m 10 \"${ENDPOINT}/v1/models\" -o \"$MODELS_JSON\" \\\n179\t  || die \"no response from ${ENDPOINT}/v1/models\"\n180\tMODEL=\"${MODEL:-$(python3 \"$HELPER\" model \"$MODELS_JSON\")}\"\n181\t\n182\tTURN_LOG=\"${SOAK_OUTPUT}/turn-log.csv\"\n183\tGPU_LOG=\"${SOAK_OUTPUT}/gpu-log.csv\"\n184\tSUMMARY_MD=\"${SOAK_OUTPUT}/summary.md\"\n185\tREQUEST_DIR=\"${SOAK_OUTPUT}/requests\"\n186\tRESPONSE_DIR=\"${SOAK_OUTPUT}/responses\"\n187\tSTATE_DIR=\"${SOAK_OUTPUT}/states\"\n188\tmkdir -p \"$REQUEST_DIR\" \"$RESPONSE_DIR\" \"$STATE_DIR\"\n189\t\n190\tprintf 'session_id,turn_id,t_ms,vram_mib,ttft_ms,decode_tps,completion_tokens,status,error\\n' > \"$TURN_LOG\"\n191\tprintf 'session_id,turn_id,gpu_index,memory_used_mib,utilization_gpu_pct\\n' > \"$GPU_LOG\"\n192\t\n193\tcapture_state \"baseline\"\n194\tpython3 \"$HELPER\" baseline \"$SOAK_OUTPUT\" \"$CONTAINER\" \"$ENDPOINT\" \"$MODEL\" \\\n195\t  \"$SOAK_SESSIONS\" \"$SOAK_TURNS\" \"$SOAK_MAX_GROWTH_MIB\"\n196\t\n197\tlog \"running soak test against ${ENDPOINT} (model=${MODEL}, container=${CONTAINER})\"\n198\tlog \"mode=${SOAK_MODE} sessions=${SOAK_SESSIONS} turns=${SOAK_TURNS} max_growth=${SOAK_MAX_GROWTH_MIB}MiB timeout=${SOAK_TIMEOUT_S}s\"\n199\tlog \"output=${SOAK_OUTPUT}\"\n200\t\n201\tSTART_SECONDS=\"$SECONDS\"\n202\tBOOT_VRAM_MIB=\"\"\n203\tTIMED_OUT=0\n204\t\n205\tfor session in $(seq 1 \"$SOAK_SESSIONS\"); do\n206\t  log \"session ${session}/${SOAK_SESSIONS}\"\n207\t  state_file=\"${STATE_DIR}/state-s${session}.json\"\n208\t  if [[ \"$SOAK_MODE\" == \"continuous\" ]]; then\n209\t    python3 \"$HELPER\" init-session \"$state_file\" \"$session\"\n210\t  fi\n211\t  for turn in $(seq 1 \"$SOAK_TURNS\"); do\n212\t    if (( SECONDS - START_SECONDS >= SOAK_TIMEOUT_S )); then\n213\t      TIMED_OUT=1\n214\t      log \"timeout reached before session=${session} turn=${turn}\"\n215\t      break 2\n216\t    fi\n217\t\n218\t    req_file=\"${REQUEST_DIR}/s${session}-t${turn}.json\"\n219\t    metrics_file=\"${RESPONSE_DIR}/s${session}-t${turn}.metrics.json\"\n220\t    if [[ \"$SOAK_MODE\" == \"continuous\" ]]; then\n221\t      python3 \"$HELPER\" request-continuous \"$MODEL\" \"$state_file\" \"$turn\" \"$req_file\"\n222\t    else\n223\t      python3 \"$HELPER\" request \"$MODEL\" \"$session\" \"$turn\" \"$req_file\"\n224\t    fi\n225\t    python3 \"$HELPER\" run \"$ENDPOINT\" \"$req_file\" \"$SOAK_REQ_TIMEOUT_S\" \"$metrics_file\"\n226\t    if [[ \"$SOAK_MODE\" == \"continuous\" ]]; then\n227\t      python3 \"$HELPER\" ingest \"$state_file\" \"$metrics_file\" \"$turn\"\n228\t    fi\n229\t\n230\t    vram=\"$(vram_mib)\"\n231\t    append_gpu_snapshot \"$session\" \"$turn\"\n232\t    python3 \"$HELPER\" append-log \"$TURN_LOG\" \"$session\" \"$turn\" \"$vram\" \"$metrics_file\"\n233\t\n234\t    read -r status t_ms ttft_ms decode_tps < <(python3 \"$HELPER\" metric \"$metrics_file\")\n235\t    log \"  turn ${turn}/${SOAK_TURNS}: status=${status} wall=${t_ms}ms ttft=${ttft_ms}ms decode_tps=${decode_tps} vram=${vram}MiB\"\n236\t  done\n237\t\n238\t  # Capture warm baseline at END of session 1 — after all 5 turn shapes have\n239\t  # run once and prefix cache has filled. Real accretion is measured FROM\n240\t  # this baseline across sessions 2-N, so cache-fill (typically +500-1500\n241\t  # MiB on the first 12K-char tool-result paste) doesn't false-positive.\n242\t  # Calibration validated 2026-05-03 on long-text @ 0.93 + 180K — sessions\n243\t  # 2-10 stayed flat at session-1-end VRAM, confirming the test discriminates\n244\t  # cache fill from accretion correctly.\n245\t  if [[ -z \"$BOOT_VRAM_MIB\" ]]; then\n246\t    BOOT_VRAM_MIB=\"$(vram_mib)\"\n247\t    log \"warm baseline after session 1: ${BOOT_VRAM_MIB} MiB\"\n248\t  fi\n249\tdone\n250\t\n251\tif [[ -z \"$BOOT_VRAM_MIB\" ]]; then\n252\t  BOOT_VRAM_MIB=\"$(vram_mib)\"\n253\t  TIMED_OUT=1\n254\t  log \"no completed turns; writing inconclusive summary\"\n255\tfi\n256\t\n257\tset +e\n258\tpython3 \"$HELPER\" summary \"$TURN_LOG\" \"$SUMMARY_MD\" \"$BOOT_VRAM_MIB\" \\\n259\t  \"$SOAK_MAX_GROWTH_MIB\" \"$TIMED_OUT\" \"$SOAK_SESSIONS\"\n260\trc=$?\n261\tset -e\n262\texit \"$rc\"\n263\t",
    "chars": 11279
  },
  {
    "turn": 14,
    "user_msg": "Read <repo>/scripts/report.sh and tell me what's relevant to the issue.",
    "tool_name": "Read",
    "tool_result": "1\t#!/usr/bin/env bash\n2\t# scripts/report.sh — paste-ready triage report for club-3090\n3\t#\n4\t# Run when filing a bug report, sharing cross-rig benchmark data, or replying\n5\t# to a triage thread. Captures hardware, OS, GPU, container runtime, stack\n6\t# version, and active container state in markdown ready to paste into a GitHub\n7\t# issue or discussion.\n8\t#\n9\t# Usage:\n10\t#   bash scripts/report.sh                   # default: hardware + stack + boot log highlights (~2 sec)\n11\t#   bash scripts/report.sh --verify          # adds verify-full.sh output (~1-2 min)\n12\t#   bash scripts/report.sh --stress          # adds verify-stress.sh 7/7 output (~5-10 min)\n13\t#   bash scripts/report.sh --soak            # adds SOAK_MODE=continuous summary (~25 min) — catches Cliff 2b\n14\t#   bash scripts/report.sh --bench           # adds bench.sh output (~3 min)\n15\t#   bash scripts/report.sh --agentic         # adds bench-agentic.sh output (1 session × 12 turns, ~8 min)\n16\t#   bash scripts/report.sh --full            # ALL five: verify + stress + soak + bench + agentic (~43 min, the canonical \"everything\" pass for cross-rig contributions)\n17\t#   bash scripts/report.sh --no-redact       # disable path/host/user redaction\n18\t#   bash scripts/report.sh --container NAME  # override container auto-detection\n19\t#   bash scripts/report.sh > my-rig.md       # capture for paste\n20\t#\n21\t# Why --soak is its own flag:\n22\t#   verify-full + verify-stress + bench all PASS on configs that FAIL the\n23\t#   multi-turn continuous soak (Cliff 2b at ~25K accumulated tokens). Until\n24\t#   the upstream fix lands, soak is the only test that catches the agentic-\n25\t#   workload failure mode. See docs/CLIFFS.md.\n26\t#\n27\t# By default, paths under user homes, hostnames, usernames, and HF tokens are\n28\t# redacted. Use --no-redact for internal sharing only.\n29\t\n30\tset -uo pipefail\n31\t\n32\tDO_VERIFY=0\n33\tDO_STRESS=0\n34\tDO_SOAK=0\n35\tDO_BENCH=0\n36\tDO_AGENTIC=0\n37\tREDACT=1\n38\tCONTAINER=\"\"\n39\t\n40\tprint_help() {\n41\t  sed -n '2,/^set/p' \"$0\" | sed 's/^# \\?//' | head -n -1\n42\t}\n43\t\n44\twhile [[ $# -gt 0 ]]; do\n45\t  case \"$1\" in\n46\t    --verify) DO_VERIFY=1; shift ;;\n47\t    --stress) DO_STRESS=1; shift ;;\n48\t    --soak) DO_SOAK=1; shift ;;\n49\t    --bench) DO_BENCH=1; shift ;;\n50\t    --agentic) DO_AGENTIC=1; shift ;;\n51\t    --full) DO_VERIFY=1; DO_STRESS=1; DO_SOAK=1; DO_BENCH=1; DO_AGENTIC=1; shift ;;\n52\t    --no-redact) REDACT=0; shift ;;\n53\t    --container) CONTAINER=\"${2:-}\"; shift 2 ;;\n54\t    -h|--help) print_help; exit 0 ;;\n55\t    *) echo \"Unknown arg: $1\" >&2; echo \"Try: bash scripts/report.sh --help\" >&2; exit 1 ;;\n56\t  esac\n57\tdone\n58\t\n59\t# ---------------------------------------------------------------------------\n60\t# Helpers\n61\t# ---------------------------------------------------------------------------\n62\t\n63\tREPO_ROOT=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")/..\" && pwd)\"\n64\tcd \"$REPO_ROOT\"\n65\t\n66\tHOST_SHORT=\"$(hostname -s 2>/dev/null || echo unknown)\"\n67\tUSER_NAME=\"${USER:-$(whoami 2>/dev/null || echo unknown)}\"\n68\t\n69\tredact() {\n70\t  if [[ $REDACT -eq 1 ]]; then\n71\t    sed \\\n72\t      -e \"s|/home/${USER_NAME}|~|g\" \\\n73\t      -e \"s|/root|~|g\" \\\n74\t      -e \"s|${HOST_SHORT}|<HOST>|g\" \\\n75\t      -e \"s|${USER_NAME}|<USER>|g\" \\\n76\t      -e 's|HF_TOKEN=[^ \"]*|HF_TOKEN=<REDACTED>|g' \\\n77\t      -e 's|HUGGING_FACE_HUB_TOKEN=[^ \"]*|HUGGING_FACE_HUB_TOKEN=<REDACTED>|g' \\\n78\t      -e 's|api_key=[^ \"]*|api_key=<REDACTED>|gi' \\\n79\t      -e 's|hf_[A-Za-z0-9]\\{30,\\}|hf_<REDACTED>|g'\n80\t  else\n81\t    cat\n82\t  fi\n83\t}\n84\t\n85\tsection() { printf '\\n## %s\\n\\n' \"$1\"; }\n86\tsubsection() { printf '\\n### %s\\n\\n' \"$1\"; }\n87\t\n88\tdetails() {\n89\t  local summary=\"$1\"\n90\t  printf '<details><summary>%s</summary>\\n\\n```\\n' \"$summary\"\n91\t  cat\n92\t  printf '```\\n\\n</details>\\n'\n93\t}\n94\t\n95\thave() { command -v \"$1\" >/dev/null 2>&1; }\n96\t\n97\t# ---------------------------------------------------------------------------\n98\t# Header\n99\t# ---------------------------------------------------------------------------\n100\t\n101\tcat <<EOF\n102\t# club-3090 rig report\n103\t\n104\tGenerated: $(date -u +'%Y-%m-%d %H:%M:%S UTC')\n105\tEOF\n106\t\n107\tif [[ $REDACT -eq 1 ]]; then\n108\t  printf '\\n_Redacted output (paths, host, user, tokens). Re-run with `--no-redact` for full data._\\n'\n109\tfi\n110\t\n111\t# ---------------------------------------------------------------------------\n112\t# System\n113\t# ---------------------------------------------------------------------------\n114\t\n115\tsection \"System\"\n116\t{\n117\t  os_name=\"unknown\"\n118\t  if [[ -r /etc/os-release ]]; then\n119\t    # shellcheck disable=SC1091\n120\t    . /etc/os-release\n121\t    os_name=\"${PRETTY_NAME:-${NAME:-unknown}}\"\n122\t  fi\n123\t  echo \"- **OS:** $os_name\"\n124\t  echo \"- **Kernel:** $(uname -r)\"\n125\t\n126\t  # Environment detection\n127\t  env_kind=\"bare metal\"\n128\t  if grep -qiE 'microsoft|wsl' /proc/version 2>/dev/null; then\n129\t    env_kind=\"WSL2\"\n130\t    if grep -qE 'WSL2' /proc/version 2>/dev/null; then\n131\t      env_kind=\"WSL2 (kernel reports WSL2)\"\n132\t    fi\n133\t  elif have systemd-detect-virt && [[ \"$(systemd-detect-virt 2>/dev/null)\" != \"none\" ]]; then\n134\t    env_kind=\"$(systemd-detect-virt 2>/dev/null) (virtualized)\"\n135\t  elif [[ -r /.dockerenv ]]; then\n136\t    env_kind=\"inside-container (unusual for this script)\"\n137\t  fi\n138\t  echo \"- **Environment:** $env_kind\"\n139\t\n140\t  echo \"- **Locale:** ${LANG:-unset}\"\n141\t  echo \"- **Timezone:** $(date +%Z)\"\n142\t  echo \"- **Uptime:** $(uptime -p 2>/dev/null || echo unknown)\"\n143\t} | redact\n144\t\n145\t# ---------------------------------------------------------------------------\n146\t# CPU + RAM\n147\t# ---------------------------------------------------------------------------\n148\t\n149\tsection \"CPU + RAM\"\n150\t{\n151\t  if have lscpu; then\n152\t    cpu_model=$(lscpu 2>/dev/null | awk -F: '/Model name/ {sub(/^ */, \"\", $2); print $2; exit}')\n153\t    cpu_cores=$(lscpu 2>/dev/null | awk -F: '/^CPU\\(s\\):/ {gsub(/ /, \"\", $2); print $2; exit}')\n154\t    echo \"- **CPU:** ${cpu_model:-unknown} (${cpu_cores:-?} threads)\"\n155\t  else\n156\t    echo \"- **CPU:** lscpu not available\"\n157\t  fi\n158\t\n159\t  if have free; then\n160\t    ram_total=$(free -h 2>/dev/null | awk '/^Mem:/ {print $2}')\n161\t    ram_avail=$(free -h 2>/dev/null | awk '/^Mem:/ {print $7}')\n162\t    echo \"- **RAM:** ${ram_total} total, ${ram_avail} available\"\n163\t    swap_total=$(free -h 2>/dev/null | awk '/^Swap:/ {print $2}')\n164\t    [[ \"$swap_total\" != \"0B\" && -n \"$swap_total\" ]] && echo \"- **Swap:** $swap_total\"\n165\t  fi\n166\t} | redact\n167\t\n168\t# ---------------------------------------------------------------------------\n169\t# Disk\n170\t# ---------------------------------------------------------------------------\n171\t\n172\tsection \"Disk\"\n173\t{\n174\t  declare -a checked_paths=()\n175\t  add_disk_row() {\n176\t    local p=\"$1\"\n177\t    [[ -z \"$p\" || ! -d \"$p\" ]] && return\n178\t    for seen in \"${checked_paths[@]:-}\"; do\n179\t      [[ \"$seen\" == \"$p\" ]] && return\n180\t    done\n181\t    checked_paths+=(\"$p\")\n182\t    local fs avail\n183\t    fs=$(df -T \"$p\" 2>/dev/null | awk 'NR==2 {print $2}')\n184\t    avail=$(df -h \"$p\" 2>/dev/null | awk 'NR==2 {print $4}')\n185\t    echo \"- **$p:** ${avail:-?} available, ${fs:-?} filesystem\"\n186\t  }\n187\t\n188\t  add_disk_row \"${MODEL_DIR:-}\"\n189\t  add_disk_row \"$REPO_ROOT/models-cache\"\n190\t  add_disk_row \"/mnt/models/huggingface\"\n191\t\n192\t  if have docker && docker info >/dev/null 2>&1; then\n193\t    docker_root=$(docker info --format '{{.DockerRootDir}}' 2>/dev/null)\n194\t    [[ -n \"$docker_root\" ]] && add_disk_row \"$docker_root\"\n195\t  fi\n196\t} | redact\n197\t\n198\t# ---------------------------------------------------------------------------\n199\t# GPU hardware\n200\t# ---------------------------------------------------------------------------\n201\t\n202\tsection \"GPU hardware\"\n203\tif ! have nvidia-smi; then\n204\t  echo \"_nvidia-smi not available — no NVIDIA GPU detected or driver not installed_\"\n205\telse\n206\t  {\n207\t    nvidia-smi --query-gpu=index,name,memory.total,driver_version,vbios_version,persistence_mode,power.limit,power.default_limit,power.max_limit,power.draw,pci.bus_id,pcie.link.gen.current,pcie.link.gen.max,pcie.link.width.current,pcie.link.width.max \\\n208\t      --format=csv,noheader 2>/dev/null \\\n209\t      | while IFS=, read -r idx name memtotal driver vbios persistence pwr_limit pwr_default pwr_max pwr_draw bus_id pcie_gen_cur pcie_gen_max pcie_width_cur pcie_width_max; do\n210\t          # trim leading spaces from CSV fields\n211\t          idx=\"${idx# }\"; name=\"${name# }\"; memtotal=\"${memtotal# }\"\n212\t          driver=\"${driver# }\"; vbios=\"${vbios# }\"; persistence=\"${persistence# }\"\n213\t          pwr_limit=\"${pwr_limit# }\"; pwr_default=\"${pwr_default# }\"\n214\t          pwr_max=\"${pwr_max# }\"; pwr_draw=\"${pwr_draw# }\"\n215\t          bus_id=\"${bus_id# }\"; pcie_gen_cur=\"${pcie_gen_cur# }\"; pcie_gen_max=\"${pcie_gen_max# }\"\n216\t          pcie_width_cur=\"${pcie_width_cur# }\"; pcie_width_max=\"${pcie_width_max# }\"\n217\t\n218\t          # Flag if user has capped below default\n219\t          power_note=\"\"\n220\t          pwr_limit_w=\"${pwr_limit% W}\"; pwr_limit_w=\"${pwr_limit_w%.*}\"\n221\t          pwr_default_w=\"${pwr_default% W}\"; pwr_default_w=\"${pwr_default_w%.*}\"\n222\t          if [[ \"$pwr_limit_w\" =~ ^[0-9]+$ ]] && [[ \"$pwr_default_w\" =~ ^[0-9]+$ ]]; then\n223\t            if [[ \"$pwr_limit_w\" -lt \"$pwr_default_w\" ]]; then\n224\t              power_note=\" ⚠ user-capped below default\"\n225\t            elif [[ \"$pwr_limit_w\" -gt \"$pwr_default_w\" ]]; then\n226\t              power_note=\" (overclocked above default)\"\n227\t            fi\n228\t          fi\n229\t\n230\t          # Flag if PCIe lane width is below max — that's hardware-level (slot\n231\t          # has fewer lanes wired, riser cables, BIOS bifurcation, etc.) and\n232\t          # affects model load speed + per-card all-reduce bandwidth.\n233\t          # NOTE: pcie.link.gen.current drops to Gen 1 at idle for power\n234\t          # saving — that's normal, not a degradation. Re-check under load if\n235\t          # you want the actual negotiated gen. Width is hardware-fixed.\n236\t          pcie_note=\"\"\n237\t          if [[ -n \"$pcie_width_cur\" && -n \"$pcie_width_max\" && \"$pcie_width_cur\" != \"$pcie_width_max\" ]]; then\n238\t            pcie_note=\" ⚠ slot is narrower than GPU capability — affects load + all-reduce bandwidth\"\n239\t          fi\n240\t\n241\t          echo \"- **GPU $idx:** $name | $memtotal | driver $driver | VBIOS $vbios | persistence=$persistence\"\n242\t          echo \"  - **Power:** limit=${pwr_limit} (default=${pwr_default}, max=${pwr_max}) | current_draw=${pwr_draw}${power_note}\"\n243\t          echo \"  - **PCIe:** x${pcie_width_cur} lanes negotiated (GPU max x${pcie_width_max}, Gen up to ${pcie_gen_max}) | bus ${bus_id}${pcie_note}\"\n244\t        done\n245\t\n246\t    cuda_ver=$(nvidia-smi 2>/dev/null | grep -oE 'CUDA Version: [0-9.]+' | head -1 | awk '{print $3}')\n247\t    [[ -n \"$cuda_ver\" ]] && echo \"- **CUDA Runtime (per driver):** $cuda_ver\"\n248\t\n249\t    # Persistence mode + ECC summary\n250\t    ecc_status=$(nvidia-smi --query-gpu=ecc.mode.current --format=csv,noheader 2>/dev/null | head -1 | tr -d ' ')\n251\t    [[ -n \"$ecc_status\" ]] && echo \"- **ECC mode:** $ecc_status (3090s don't have ECC; expect N/A)\"\n252\t  } | redact\n253\t\n254\t  subsection \"NVLink\"\n255\t  if nvidia-smi nvlink --status -i 0 2>/dev/null | grep -qE 'Link [0-9]+:'; then\n256\t    nvidia-smi nvlink --status 2>&1 | redact | details \"NVLink link status\"\n257\t  else\n258\t    echo \"_No NVLink detected (PCIe-only)_\"\n259\t  fi\n260\t\n261\t  subsection \"Topology\"\n262\t  nvidia-smi topo -m 2>&1 | redact | details \"PCIe / GPU topology matrix\"\n263\t\n264\t  subsection \"Full nvidia-smi\"\n265\t  nvidia-smi 2>&1 | redact | details \"Full nvidia-smi output\"\n266\tfi\n267\t\n268\t# ---------------------------------------------------------------------------\n269\t# Display / desktop state\n270\t# ---------------------------------------------------------------------------\n271\t\n272\tsection \"Display / desktop state\"\n273\t{\n274\t  if [[ -n \"${DISPLAY:-}\" ]]; then\n275\t    echo \"- **\\$DISPLAY:** ${DISPLAY} (X11 / Wayland session present)\"\n276\t  else\n277\t    echo \"- **\\$DISPLAY:** unset (headless)\"\n278\t  fi\n279\t  [[ -n \"${WAYLAND_DISPLAY:-}\" ]] && echo \"- **\\$WAYLAND_DISPLAY:** ${WAYLAND_DISPLAY}\"\n280\t\n281\t  compositor=\"\"\n282\t  for proc in Xorg Xwayland weston gnome-shell kwin sway hyprland mutter; do\n283\t    if pgrep -x \"$proc\" >/dev/null 2>&1; then\n284\t      compositor=\"$compositor $proc\"\n285\t    fi\n286\t  done\n287\t  if [[ -n \"$compositor\" ]]; then\n288\t    echo \"- **Display processes running:**$compositor\"\n289\t  else\n290\t    echo \"- **Display processes running:** none detected\"\n291\t  fi\n292\t\n293\t  if have nvidia-smi; then\n294\t    nvidia-smi --query-gpu=index,memory.used --format=csv,noheader,nounits 2>/dev/null \\\n295\t      | while IFS=, read -r idx used; do\n296\t          idx=\"${idx# }\"; used=\"${used# }\"\n297\t          if [[ \"$used\" =~ ^[0-9]+$ ]] && [[ \"$used\" -gt 100 ]]; then\n298\t            echo \"- **GPU $idx idle VRAM:** ${used} MiB ⚠ something is using this GPU (display, browser, container)\"\n299\t          else\n300\t            echo \"- **GPU $idx idle VRAM:** ${used} MiB ✓\"\n301\t          fi\n302\t        done\n303\t  fi\n304\t} | redact\n305\t\n306\t# ---------------------------------------------------------------------------\n307\t# Container runtime\n308\t# ---------------------------------------------------------------------------\n309\t\n310\tsection \"Container runtime\"\n311\t{\n312\t  if have docker; then\n313\t    if docker info >/dev/null 2>&1; then\n314\t      docker_ver=$(docker version --format '{{.Server.Version}}' 2>/dev/null)\n315\t      echo \"- **Docker:** ${docker_ver:-unknown}\"\n316\t\n317\t      if docker compose version >/dev/null 2>&1; then\n318\t        compose_ver=$(docker compose version --short 2>/dev/null)\n319\t        echo \"- **docker compose (v2):** ${compose_ver:-unknown}\"\n320\t      elif have docker-compose; then\n321\t        compose_ver=$(docker-compose version --short 2>/dev/null)\n322\t        echo \"- **docker-compose (v1):** ${compose_ver:-unknown}\"\n323\t      fi\n324\t\n325\t      if have nvidia-ctk; then\n326\t        nvct_ver=$(nvidia-ctk --version 2>&1 | head -1 | awk '{print $NF}')\n327\t        echo \"- **NVIDIA Container Toolkit:** ${nvct_ver:-unknown}\"\n328\t      elif have nvidia-container-toolkit; then\n329\t        nvct_ver=$(nvidia-container-toolkit --version 2>&1 | head -1 | awk '{print $NF}')\n330\t        echo \"- **NVIDIA Container Toolkit:** ${nvct_ver:-unknown}\"\n331\t      fi\n332\t    else\n333\t      echo \"- **Docker:** installed but daemon not accessible\"\n334\t    fi\n335\t  else\n336\t    echo \"- **Docker:** not installed\"\n337\t  fi\n338\t} | redact\n339\t\n340\t# ---------------------------------------------------------------------------\n341\t# Stack version\n342\t# ---------------------------------------------------------------------------\n343\t\n344\tsection \"Stack version\"\n345\t{\n346\t  if [[ -d .git ]]; then\n347\t    commit=$(git rev-parse --short HEAD 2>/dev/null)\n348\t    branch=$(git branch --show-current 2>/dev/null)\n349\t    echo \"- **club-3090:** \\`${commit:-unknown}\\` (branch: \\`${branch:-detached}\\`)\"\n350\t    if ! git diff --quiet 2>/dev/null || ! git diff --cached --quiet 2>/dev/null; then\n351\t      echo \"- **Working tree:** ⚠ has uncommitted changes (run \\`git status\\` to inspect)\"\n352\t    fi\n353\t  else\n354\t    echo \"- **club-3090:** not a git repo\"\n355\t  fi\n356\t\n357\t  if [[ -f scripts/setup.sh ]]; then\n358\t    # Parse `GENESIS_PIN=\"${GENESIS_PIN:-<default>}\"` — extract just the default value\n359\t    genesis_pin=$(grep -E '^GENESIS_PIN=' scripts/setup.sh 2>/dev/null | head -1 \\\n360\t      | sed -E 's/.*:-([^}]+)\\}.*/\\1/; t; s/.*=//' \\\n361\t      | tr -d '\"' | tr -d \"'\")\n362\t    [[ -n \"$genesis_pin\" ]] && echo \"- **GENESIS_PIN default:** \\`$genesis_pin\\` (per scripts/setup.sh)\"\n363\t    # Override from env if set\n364\t    [[ -n \"${GENESIS_PIN:-}\" ]] && echo \"- **GENESIS_PIN env override:** \\`$GENESIS_PIN\\`\"\n365\t  fi\n366\t\n367\t  if have docker && docker info >/dev/null 2>&1; then\n368\t    cached=$(docker images vllm/vllm-openai --format '{{.Tag}} {{.Digest}} {{.CreatedSince}}' 2>/dev/null | head -3)\n369\t    if [[ -n \"$cached\" ]]; then\n370\t      echo \"- **Cached vLLM images:**\"\n371\t      echo \"$cached\" | while read -r tag digest age rest; do\n372\t        echo \"  - tag \\`$tag\\` digest \\`$digest\\` ($age $rest)\"\n373\t      done\n374\t    fi\n375\t  fi\n376\t} | redact\n377\t\n378\t# ---------------------------------------------------------------------------\n379\t# Active container\n380\t# ---------------------------------------------------------------------------\n381\t\n382\tsection \"Active container\"\n383\t# Engine-agnostic auto-detection: try vllm-* first (most common on this stack),\n384\t# fall back to llama-cpp-* (the alternate engine we ship). User can override\n385\t# with CONTAINER=... env var for non-standard naming (microk8s deployments,\n386\t# host engine builds via CONTAINER=none, etc.).\n387\tif [[ -z \"$CONTAINER\" ]] && have docker && docker info >/dev/null 2>&1; then\n388\t  CONTAINER=$(docker ps --format '{{.Names}}' --filter 'name=vllm-qwen36' 2>/dev/null | head -1)\n389\t  [[ -z \"$CONTAINER\" ]] && CONTAINER=$(docker ps --format '{{.Names}}' --filter 'name=vllm-' 2>/dev/null | head -1)\n390\t  [[ -z \"$CONTAINER\" ]] && CONTAINER=$(docker ps --format '{{.Names}}' --filter 'name=llama-cpp-' 2>/dev/null | head -1)\n391\tfi\n392\t\n393\t# Engine class — drives which probes run inside the container body. Inferred\n394\t# from container name; user can override with ENGINE_KIND=vllm|llamacpp env var.\n395\tcase \"${ENGINE_KIND:-}\" in\n396\t  vllm|llamacpp|unknown) ;;  # respect user override\n397\t  *)\n398\t    case \"$CONTAINER\" in\n399\t      vllm-*)      ENGINE_KIND=\"vllm\" ;;\n400\t      llama-cpp-*) ENGINE_KIND=\"llamacpp\" ;;\n401\t      *)           ENGINE_KIND=\"unknown\" ;;\n402\t    esac ;;\n403\tesac\n404\t\n405\tif [[ -z \"$CONTAINER\" ]]; then\n406\t  echo \"_No vLLM or llama.cpp container running. Start one with \\`bash scripts/launch.sh\\` and re-run for the full report._\"\n407\telse\n408\t  {\n409\t    status=$(docker ps --filter \"name=$CONTAINER\" --format '{{.Status}}' 2>/dev/null | head -1)\n410\t    ports=$(docker ps --filter \"name=$CONTAINER\" --format '{{.Ports}}' 2>/dev/null | head -1)\n411\t    image=$(docker ps --filter \"name=$CONTAINER\" --format '{{.Image}}' 2>/dev/null | head -1)\n412\t    echo \"- **Name:** \\`$CONTAINER\\`\"\n413\t    echo \"- **Engine:** \\`${ENGINE_KIND}\\`\"\n414\t    echo \"- **Status:** ${status:-unknown}\"\n415\t    echo \"- **Ports:** ${ports:-unknown}\"\n416\t    echo \"- **Image:** \\`${image:-unknown}\\`\"\n417\t  } | redact\n418\t\n419\t  # Engine-specific probes from this point. vLLM container has Python +\n420\t  # PyTorch + Genesis markers; llama.cpp container ships a stripped C++\n421\t  # binary with no Python — different probe set.\n422\t\n423\t  # Engine-specific subsections. vLLM container has Python + PyTorch + Genesis\n424\t  # markers; llama.cpp container ships a stripped C++ binary with no Python\n425\t  # exec available — different probe set.\n426\t\n427\t  if [[ \"$ENGINE_KIND\" == \"llamacpp\" ]]; then\n428\t    # ---- llama.cpp probe set ----\n429\t    subsection \"Container engine state (llama.cpp)\"\n430\t    {\n431\t      # llama-server prints its version + build flags on startup. Grep the\n432\t      # boot log for the version banner instead of trying to docker exec\n433\t      # (the llama-cpp image doesn't ship interactive shell utilities).\n434\t      llama_version=$(docker logs \"$CONTAINER\" 2>&1 | grep -E '^build_info:|^version:|^system_info:' | head -3)\n435\t      if [[ -n \"$llama_version\" ]]; then\n436\t        echo \"**llama-server version + build:**\"\n437\t        echo '```'\n438\t        echo \"$llama_version\"\n439\t        echo '```'\n440\t        echo\n441\t      fi\n442\t\n443\t      # Loaded model + ctx + KV type — surfaces model identity from boot log.\n444\t      model_loaded=$(docker logs \"$CONTAINER\" 2>&1 | grep -E 'load_model:|llama_model_load_from_file_impl:|llama_kv_cache_init:|llama_init_from_model:' | head -8)\n445\t      if [[ -n \"$model_loaded\" ]]; then\n446\t        echo \"**Model load + KV cache init:**\"\n447\t        echo '```'\n448\t        echo \"$model_loaded\"\n449\t        echo '```'\n450\t        echo\n451\t      fi\n452\t\n453\t      # llama.cpp doesn't have Genesis / vLLM SpecDecoding metrics. Skip\n454\t      # those grep patterns. Capture warnings/errors only.\n455\t      boot_errors=$(docker logs \"$CONTAINER\" 2>&1 | grep -iE '^(warn|error|fatal|abort)|panic|core dumped' | tail -5)\n456\t      if [[ -n \"$boot_errors\" ]]; then\n457\t        echo \"**Recent warnings/errors (last 5):**\"\n458\t        echo '```'\n459\t        echo \"$boot_errors\"\n460\t        echo '```'\n461\t      fi\n462\t    } | redact\n463\t\n464\t    subsection \"Full boot log (first 200 lines)\"\n465\t    docker logs \"$CONTAINER\" 2>&1 | head -200 | redact | details \"First 200 lines of docker logs\"\n466\t\n467\t  else\n468\t  # ---- vLLM probe set (default for engine=vllm or unknown) ----\n469\t  subsection \"Container Python / CUDA versions\"\n470\t  {\n471\t    # vLLM version + Torch CUDA build vs host driver mismatch is one of the\n472\t    # rare failure modes that image SHA pinning doesn't catch. Quick docker\n473\t    # exec to surface what the container actually sees.\n474\t    py_versions=$(docker exec \"$CONTAINER\" python3 -c \\\n475\t      'import torch, sys; print(f\"torch={torch.__version__} torch_cuda_build={torch.version.cuda} cudnn={torch.backends.cudnn.version()}\")' \\\n476\t      2>&1)\n477\t    if [[ -n \"$py_versions\" ]] && [[ \"$py_versions\" != *\"Error\"* ]] && [[ \"$py_versions\" != *\"error\"* ]]; then\n478\t      echo \"- **PyTorch:** \\`${py_versions}\\`\"\n479\t    else\n480\t      echo \"- **PyTorch:** (could not query — \\`docker exec\\` failed or torch not importable)\"\n481\t    fi\n482\t\n483\t    vllm_ver=$(docker exec \"$CONTAINER\" python3 -c 'import vllm; print(vllm.__version__)' 2>&1)\n484\t    if [[ -n \"$vllm_ver\" ]] && [[ \"$vllm_ver\" != *\"Error\"* ]] && [[ \"$vllm_ver\" != *\"error\"* ]]; then\n485\t      echo \"- **vLLM:** \\`${vllm_ver}\\`\"\n486\t    else\n487\t      echo \"- **vLLM:** (could not query)\"\n488\t    fi\n489\t\n490\t    # Container's view of the GPUs — should match host driver, but if NVIDIA\n491\t    # Container Toolkit is mis-configured this surfaces the mismatch.\n492\t    cuda_in_container=$(docker exec \"$CONTAINER\" nvidia-smi --query-gpu=index,name,driver_version --format=csv,noheader 2>&1 | head -4)\n493\t    if [[ -n \"$cuda_in_container\" ]] && [[ \"$cuda_in_container\" != *\"Error\"* ]] && [[ \"$cuda_in_container\" != *\"command not found\"* ]]; then\n494\t      echo \"- **nvidia-smi inside container:**\"\n495\t      echo '  ```'\n496\t      echo \"$cuda_in_container\" | sed 's/^/  /'\n497\t      echo '  ```'\n498\t    fi\n499\t  } | redact\n500\t\n501\t  subsection \"Boot log highlights\"\n502\t  {\n503\t    genesis_results=$(docker logs \"$CONTAINER\" 2>&1 | grep -E '\\[INFO:genesis\\.apply_all\\] (Genesis|✅) Results' | tail -1)\n504\t    if [[ -n \"$genesis_results\" ]]; then\n505\t      echo \"**Genesis patches applied:**\"\n506\t      echo '```'\n507\t      echo \"$genesis_results\" | sed 's/.*Genesis Results: /Genesis Results: /'\n508\t      echo '```'\n509\t      echo\n510\t    fi\n511\t\n512\t    sidecar_status=$(docker logs \"$CONTAINER\" 2>&1 | grep -E '^\\[(tolist_cudagraph_fix|inputs_embeds_optional|workspace_lock_disable|pn25_genesis_register_fix|pn30_dst_shaped_temp_fix|fa_max_seqlen_clamp|pn12_ffn_pool_anchor|pn12_compile_safe_custom_op)\\]' | head -10)\n513\t    if [[ -n \"$sidecar_status\" ]]; then\n514\t      echo \"**Local sidecar application:**\"\n515\t      echo '```'\n516\t      echo \"$sidecar_status\"\n517\t      echo '```'\n518\t      echo\n519\t    fi\n520\t\n521\t    kv_pool=$(docker logs \"$CONTAINER\" 2>&1 | grep -E 'Available KV cache memory|GPU KV cache size:|Maximum concurrency for' | tail -3)\n522\t    if [[ -n \"$kv_pool\" ]]; then\n523\t      echo \"**KV pool sizing:**\"\n524\t      echo '```'\n525\t      echo \"$kv_pool\"\n526\t      echo '```'\n527\t      echo\n528\t    fi\n529\t\n530\t    # Engine config — the line containing \"non-default args\" or \"Initializing a V1 LLM engine\"\n531\t    # captures every important CLI flag (max_model_len, mem_util, kv dtype, spec config, etc.)\n532\t    engine_config=$(docker logs \"$CONTAINER\" 2>&1 | grep -E 'non-default args:|Initializing a V1 LLM engine' | head -2)\n533\t    if [[ -n \"$engine_config\" ]]; then\n534\t      echo \"**Engine config (CLI flags + engine init):**\"\n535\t      echo '```'\n536\t      echo \"$engine_config\"\n537\t      echo '```'\n538\t      echo\n539\t    fi\n540\t\n541\t    boot_errors=$(docker logs \"$CONTAINER\" 2>&1 | grep -E '^(WARNING|ERROR|CRITICAL)' | tail -5)\n542\t    if [[ -n \"$boot_errors\" ]]; then\n543\t      echo \"**Recent warnings/errors (last 5):**\"\n544\t      echo '```'\n545\t      echo \"$boot_errors\"\n546\t      echo '```'\n547\t    fi\n548\t  } | redact\n549\t\n550\t  subsection \"Full boot log (first 200 lines)\"\n551\t  docker logs \"$CONTAINER\" 2>&1 | head -200 | redact | details \"First 200 lines of docker logs\"\n552\t  fi  # end of vLLM/llamacpp engine branch\n553\tfi  # end of \"if no container running\"\n554\t\n555\t# ---------------------------------------------------------------------------\n556\t# Recent failed boot attempts\n557\t# ---------------------------------------------------------------------------\n558\t# Capture exited vLLM/llama.cpp containers from the last 24h. Most valuable\n559\t# diagnostic data for boot-failure scenarios — without this, contributors hit\n560\t# \"no container running\" and have to manually paste docker logs ad-hoc.\n561\t# Engine-agnostic: matches both vllm-* and llama-cpp-* container patterns.\n562\t\n563\tsection \"Recent failed boot attempts\"\n564\tif ! have docker; then\n565\t  echo \"_docker not available — skipping._\"\n566\telif ! docker info >/dev/null 2>&1; then\n567\t  echo \"_docker daemon unreachable — skipping._\"\n568\telse\n569\t  # Get exited containers matching club-3090 engine patterns. `docker ps -a`\n570\t  # without a time filter; we'll filter to last 24h via the FinishedAt field.\n571\t  exited_lines=$(docker ps -a \\\n572\t    --format '{{.Names}}\\t{{.Image}}\\t{{.Status}}\\t{{.ID}}' \\\n573\t    --filter 'status=exited' 2>/dev/null \\\n574\t    | grep -E '^(vllm-|llama-cpp-)' || true)\n575\t\n576\t  if [[ -z \"$exited_lines\" ]]; then\n577\t    echo \"_No recently-exited vLLM or llama.cpp containers found._\"\n578\t  else\n579\t    found_recent=0\n580\t    while IFS=$'\\t' read -r ex_name ex_image ex_status ex_id; do\n581\t      [[ -z \"$ex_name\" ]] && continue\n582\t      # Cutoff: last 24h. docker inspect gives ISO-8601 FinishedAt.\n583\t      finished_at=$(docker inspect \"$ex_id\" --format '{{.State.FinishedAt}}' 2>/dev/null || echo \"\")\n584\t      exit_code=$(docker inspect \"$ex_id\" --format '{{.State.ExitCode}}' 2>/dev/null || echo \"?\")\n585\t      [[ -z \"$finished_at\" ]] && continue\n586\t\n587\t      # Skip containers that exited >24h ago (epoch comparison)\n588\t      finished_epoch=$(date -d \"$finished_at\" +%s 2>/dev/null || echo 0)\n589\t      cutoff_epoch=$(date -d '24 hours ago' +%s 2>/dev/null || echo 0)\n590\t      [[ \"$finished_epoch\" -lt \"$cutoff_epoch\" ]] && continue\n591\t\n592\t      found_recent=1\n593\t      relative_when=$(date -d \"$finished_at\" '+%Y-%m-%dT%H:%M:%SZ (%s seconds ago)' 2>/dev/null || echo \"$finished_at\")\n594\t      # Format relative_when nicely: how many minutes ago?\n595\t      mins_ago=$(( ($(date +%s) - finished_epoch) / 60 ))\n596\t      if [[ $mins_ago -lt 60 ]]; then\n597\t        relative_label=\"${mins_ago} min ago\"\n598\t      else\n599\t        hrs_ago=$(( mins_ago / 60 ))\n600\t        rem_mins=$(( mins_ago % 60 ))\n601\t        relative_label=\"${hrs_ago}h ${rem_mins}min ago\"\n602\t      fi\n603\t\n604\t      subsection \"\\`$ex_name\\` — exited $relative_label (code $exit_code)\"\n605\t      {\n606\t        echo \"- **Name:** \\`$ex_name\\`\"\n607\t        echo \"- **Image:** \\`$ex_image\\`\"\n608\t        echo \"- **Status:** $ex_status\"\n609\t        echo \"- **Exit code:** $exit_code\"\n610\t        echo \"- **Finished at:** $finished_at\"\n611\t      } | redact\n612\t\n613\t      docker logs --tail 80 \"$ex_id\" 2>&1 | redact | details \"Last 80 log lines from \\`$ex_name\\`\"\n614\t    done <<< \"$exited_lines\"\n615\t\n616\t    if [[ \"$found_recent\" == \"0\" ]]; then\n617\t      echo \"_Exited vLLM/llama.cpp containers exist but all >24h old — likely not relevant to current investigation._\"\n618\t    fi\n619\t  fi\n620\tfi\n621\t\n622\t# ---------------------------------------------------------------------------\n623\t# Optional: verify-full\n624\t# ---------------------------------------------------------------------------\n625\t\n626\tif [[ $DO_VERIFY -eq 1 ]]; then\n627\t  section \"verify-full.sh output\"\n628\t  if [[ -f scripts/verify-full.sh ]]; then\n629\t    bash scripts/verify-full.sh 2>&1 | redact | details \"verify-full output\"\n630\t  else\n631\t    echo \"_scripts/verify-full.sh not found_\"\n632\t  fi\n633\tfi\n634\t\n635\t# ---------------------------------------------------------------------------\n636\t# Optional: verify-stress\n637\t# ---------------------------------------------------------------------------\n638\t\n639\tif [[ $DO_STRESS -eq 1 ]]; then\n640\t  section \"verify-stress.sh output\"\n641\t  if [[ -f scripts/verify-stress.sh ]]; then\n642\t    bash scripts/verify-stress.sh 2>&1 | redact | details \"verify-stress output (7 boundary checks incl. Cliff 2 needle recall)\"\n643\t  else\n644\t    echo \"_scripts/verify-stress.sh not found_\"\n645\t  fi\n646\tfi\n647\t\n648\t# ---------------------------------------------------------------------------\n649\t# Optional: soak-continuous (catches Cliff 2b — the only test that does)\n650\t# ---------------------------------------------------------------------------\n651\t\n652\tif [[ $DO_SOAK -eq 1 ]]; then\n653\t  section \"soak-test.sh (SOAK_MODE=continuous) output\"\n654\t  if [[ -f scripts/soak-test.sh ]]; then\n655\t    soak_run_dir=\"results/report-soak-$(date +%Y%m%d-%H%M%S)\"\n656\t    SOAK_MODE=continuous SOAK_SESSIONS=5 SOAK_TURNS=5 SOAK_OUTPUT=\"$soak_run_dir\" \\\n657\t      SOAK_TIMEOUT_S=\"${SOAK_TIMEOUT_S:-1800}\" \\\n658\t      bash scripts/soak-test.sh 2>&1 | redact | details \"soak-test stdout (5-session × 5-turn ramping conversation, ~25 min)\"\n659\t    if [[ -f \"$soak_run_dir/summary.md\" ]]; then\n660\t      echo\n661\t      echo \"**Soak summary** (\\`$soak_run_dir/summary.md\\`):\"\n662\t      echo\n663\t      redact < \"$soak_run_dir/summary.md\"\n664\t    else\n665\t      echo \"_soak summary.md not produced — check stdout above_\"\n666\t    fi\n667\t  else\n668\t    echo \"_scripts/soak-test.sh not found_\"\n669\t  fi\n670\tfi\n671\t\n672\t# ---------------------------------------------------------------------------\n673\t# Optional: bench\n674\t# ---------------------------------------------------------------------------\n675\t\n676\tif [[ $DO_BENCH -eq 1 ]]; then\n677\t  section \"bench.sh output\"\n678\t  if [[ -f scripts/bench.sh ]]; then\n679\t    bash scripts/bench.sh 2>&1 | redact | details \"bench output (3 warmups + 5 measured per prompt)\"\n680\t  else\n681\t    echo \"_scripts/bench.sh not found_\"\n682\t  fi\n683\tfi\n684\t\n685\t# ---------------------------------------------------------------------------\n686\t# Optional: agentic bench (multi-turn prefill stress)\n687\t# ---------------------------------------------------------------------------\n688\t\n689\tif [[ $DO_AGENTIC -eq 1 ]]; then\n690\t  section \"bench-agentic.sh output\"\n691\t  if [[ -f scripts/bench-agentic.sh ]]; then\n692\t    SESSIONS=1 bash scripts/bench-agentic.sh 2>&1 | redact | details \"agentic bench (1 session × 12 turns, multi-turn prefill stress, ~8 min)\"\n693\t  else\n694\t    echo \"_scripts/bench-agentic.sh not found_\"\n695\t  fi\n696\tfi\n697\t\n698\t# ---------------------------------------------------------------------------\n699\t# Footer\n700\t# ---------------------------------------------------------------------------\n701\t\n702\tcat <<'EOF'\n703\t\n704\t---\n705\t\n706\t_Generated by `bash scripts/report.sh`. Flags: `--verify` (verify-full), `--stress` (verify-stress 7/7 incl. Cliff 2 needles), `--soak` (SOAK_MODE=continuous, catches Cliff 2b), `--bench` (canonical TPS), `--agentic` (multi-turn prefill stress, ~8 min), `--full` (all five, ~43 min). Use `--no-redact` to disable redaction (internal sharing only)._\n707\tEOF\n",
    "chars": 31817
  },
  {
    "turn": 15,
    "user_msg": "Read <repo>/scripts/verify-full.sh and tell me what's relevant to the issue.",
    "tool_name": "Read",
    "tool_result": "1\t#!/usr/bin/env bash\n2\t#\n3\t# Full-functional test — fast functional smoke covering server reachability,\n4\t# patch application, basic completion, tool calling, streaming, thinking\n5\t# mode, output quality / cascade detection, and MTP acceptance verification.\n6\t# Run before publishing or after any major patch / vLLM image bump.\n7\t#\n8\t# This is the FAST functional check (~1-2 min). It does NOT exercise long-\n9\t# context recall or large prefill activation peaks — for those run\n10\t# `bash scripts/verify-stress.sh` (5-10 min, KV-cache + prefill boundary tests).\n11\t#\n12\t# Checks (in order):\n13\t#   1. Server reachable\n14\t#   2. Genesis patches applied\n15\t#   3. Basic completion (Paris)\n16\t#   4. Tool calling (KNOWN TO FAIL in default compose; PASS in tools variants)\n17\t#   5. Streaming (SSE) — non-tool prompt, verify chunks add up to coherent text\n18\t#   6. Thinking mode — reasoning prompt, verify reasoning + content both populated\n19\t#   7. Output quality / cascade detection — 2K-token completion, scan for\n20\t#      <tool_call> inline cascade and repetitive degeneracy\n21\t#   8. MTP acceptance length — parse SpecDecoding metrics from docker logs,\n22\t#      assert mean AL >= 2.0 (sanity that spec-decode is contributing)\n23\t#\n24\t# For boundary / stress validation (longctx needle ladder, ~25K-token tool\n25\t# prefill OOM detection): see scripts/verify-stress.sh\n26\t#\n27\t# Usage:\n28\t#   CONTAINER=<your-container> bash scripts/verify-full.sh\n29\t#\n30\t# Env (optional):\n31\t#   URL          Default: http://localhost:8020\n32\t#   MODEL        Default: qwen3.6-27b\n33\t#   CONTAINER    Default: vllm-qwen36-27b\n34\t#   SKIP_TOOLS   Set to 1 to skip the tool-call test entirely (useful when\n35\t#                running against the default config which is known to fail\n36\t#                tool calls — see README \"Known issue\" section).\n37\t#\n38\t# Optional flag:\n39\t#   --bench      After all correctness checks pass, run scripts/bench.sh\n40\t#                (3 warmup + 5 measured) to report wall_TPS / decode_TPS /\n41\t#                TTFT mean+std+CV. Adds ~1-2 minutes.\n42\t\n43\tset -euo pipefail\n44\t\n45\tRUN_BENCH=0\n46\tfor arg in \"$@\"; do\n47\t  case \"$arg\" in\n48\t    --bench) RUN_BENCH=1 ;;\n49\t  esac\n50\tdone\n51\t\n52\t# Auto-detect running container + port (URL/CONTAINER env vars still win).\n53\t# See scripts/preflight.sh::preflight_autodetect_endpoint.\n54\tROOT_DIR=\"$(cd -- \"$(dirname -- \"${BASH_SOURCE[0]}\")/..\" && pwd)\"\n55\tif [[ -f \"${ROOT_DIR}/scripts/preflight.sh\" ]]; then\n56\t  # shellcheck source=preflight.sh\n57\t  source \"${ROOT_DIR}/scripts/preflight.sh\"\n58\t  preflight_autodetect_endpoint\n59\tfi\n60\tURL=\"${URL:-http://localhost:8020}\"\n61\tMODEL=\"${MODEL:-qwen3.6-27b}\"\n62\tCONTAINER=\"${CONTAINER:-vllm-qwen36-27b}\"\n63\t\n64\tpass() { printf \"  \\033[32m✓\\033[0m %s\\n\" \"$1\"; }\n65\tfail() { printf \"  \\033[31m✗\\033[0m %s\\n\" \"$1\"; printf \"    \\033[33m→\\033[0m %s\\n\" \"$2\"; return 1; }\n66\tskip() { printf \"  \\033[33m⊘\\033[0m %s (skipped)\\n\" \"$1\"; }\n67\t\n68\t# ---- Engine detection ---------------------------------------------------\n69\t# Returns one of: vllm | llamacpp | sglang | unknown\n70\t# Used to gate engine-coupled checks (Genesis markers, MTP-acceptance log\n71\t# scrape) so non-vLLM engines (especially llama.cpp host builds without\n72\t# Docker) get clean skips rather than misleading failures or fail-paths\n73\t# that the user can't act on. Surfaced by @lamentofhighborne in #85, fixed\n74\t# per #87. Engine class is detected ONCE at startup and cached.\n75\tdetect_engine() {\n76\t  # Hint 1: llama-server's /props endpoint (vLLM doesn't ship it)\n77\t  if curl -sf -m 3 \"${URL}/props\" >/dev/null 2>&1; then\n78\t    echo \"llamacpp\"; return 0\n79\t  fi\n80\t  # Hint 2: vLLM's chat-completion response includes system_fingerprint\n81\t  # like \"vllm-0.20.2rc1.dev9+g01d4d1ad3-tp2-c9120464\".\n82\t  local fp\n83\t  fp=\"$(curl -sf -m 5 \"${URL}/v1/chat/completions\" \\\n84\t    -H 'Content-Type: application/json' \\\n85\t    -d \"{\\\"model\\\":\\\"${MODEL}\\\",\\\"messages\\\":[{\\\"role\\\":\\\"user\\\",\\\"content\\\":\\\"hi\\\"}],\\\"max_tokens\\\":1}\" 2>/dev/null \\\n86\t    | python3 -c \"import sys,json; d=json.load(sys.stdin); print(d.get('system_fingerprint','') or '')\" 2>/dev/null)\"\n87\t  case \"$fp\" in\n88\t    vllm-*)   echo \"vllm\"; return 0 ;;\n89\t    sglang-*) echo \"sglang\"; return 0 ;;\n90\t  esac\n91\t  # Hint 3: container name pattern as a fallback (cheap, no extra HTTP)\n92\t  case \"$CONTAINER\" in\n93\t    vllm-*)      echo \"vllm\"; return 0 ;;\n94\t    llama-cpp-*) echo \"llamacpp\"; return 0 ;;\n95\t  esac\n96\t  echo \"unknown\"\n97\t}\n98\tENGINE_KIND=\"$(detect_engine)\"\n99\t\n100\tFAILED=0\n101\trun_check() {\n102\t  local label=\"$1\"; shift\n103\t  if \"$@\"; then :; else FAILED=$((FAILED + 1)); fi\n104\t}\n105\t\n106\techo \"Running FULL functional test against ${URL}\"\n107\techo \"  model=${MODEL}  container=${CONTAINER}  engine=${ENGINE_KIND}\"\n108\techo \"\"\n109\t\n110\t# --------------------------------------------------------------------\n111\t# 1. Server reachable\n112\t# --------------------------------------------------------------------\n113\tcheck_server() {\n114\t  echo \"[1/8] Server reachable on /v1/models ...\"\n115\t  if curl -sf -m 5 \"${URL}/v1/models\" >/dev/null 2>&1; then\n116\t    pass \"server is serving\"\n117\t  else\n118\t    fail \"no response from ${URL}/v1/models\" \\\n119\t         \"Start the stack: cd compose && docker compose up -d ; docker logs -f ${CONTAINER}\"\n120\t  fi\n121\t}\n122\trun_check \"server\" check_server\n123\t\n124\t# --------------------------------------------------------------------\n125\t# 2. Genesis patches applied\n126\t# --------------------------------------------------------------------\n127\tcheck_patches() {\n128\t  echo \"[2/8] Genesis patches applied ...\"\n129\t  # Genesis is a vLLM-only patcher. Skip cleanly on other engines instead of\n130\t  # leaving the user wondering whether \"no Genesis marker\" means a real\n131\t  # problem or a category error.\n132\t  case \"$ENGINE_KIND\" in\n133\t    llamacpp) skip \"llama.cpp engine — Genesis is vLLM-only, not applicable\"; return 0 ;;\n134\t    sglang)   skip \"SGLang engine — Genesis is vLLM-only, not applicable\";    return 0 ;;\n135\t    unknown)  ;;  # fall through; might still be vLLM under a non-standard container name\n136\t  esac\n137\t  if ! command -v docker >/dev/null 2>&1; then\n138\t    skip \"docker not in PATH (host engine build?)\"\n139\t    return 0\n140\t  fi\n141\t  if ! docker inspect \"${CONTAINER}\" >/dev/null 2>&1; then\n142\t    skip \"container '${CONTAINER}' not found (host engine build? CONTAINER=none for host endpoints)\"\n143\t    return 0\n144\t  fi\n145\t  # Anchors updated 2026-05-02 for Genesis v7.14+ logging conventions (the old\n146\t  # \"[OK] Qwen3 tool_call fix\" string is no longer emitted; markers are now\n147\t  # \"[Genesis] applied:\" per patch + \"apply_all elapsed\" at the end + \"FAILED:\"\n148\t  # for any patch that errored). Reported by @troymroberts in club-3090#25.\n149\t  #\n150\t  # Don't tail — Genesis v7.14+ emits 50+ \"[Genesis] applied:\" lines plus a\n151\t  # dispatcher matrix dump, so tail -10 was cutting off the canonical\n152\t  # \"apply_all elapsed:\" anchor that fires LAST. Reported by @JusefPol in\n153\t  # club-3090#29. We grep -q each anchor in priority order on the full log.\n154\t  local docker_logs\n155\t  docker_logs=\"$(docker logs \"${CONTAINER}\" 2>&1)\"\n156\t  if echo \"$docker_logs\" | grep -q \"\\[Genesis\\] FAILED\"; then\n157\t    fail \"Genesis apply_all reported FAILED patch(es)\" \\\n158\t         \"Inspect: docker logs ${CONTAINER} 2>&1 | grep -E 'Genesis.*FAILED' | head\"\n159\t  elif echo \"$docker_logs\" | grep -q \"apply_all elapsed\"; then\n160\t    pass \"Genesis patches applied (apply_all completed clean)\"\n161\t  elif echo \"$docker_logs\" | grep -q \"\\[Genesis\\] applied:\"; then\n162\t    pass \"Genesis patches applied (partial log — apply_all may still be running)\"\n163\t  else\n164\t    skip \"no Genesis marker in logs (container restarted, or Genesis not loaded)\"\n165\t  fi\n166\t}\n167\trun_check \"patches\" check_patches\n168\t\n169\t# --------------------------------------------------------------------\n170\t# 3. Basic completion — Paris sanity\n171\t# --------------------------------------------------------------------\n172\tcheck_basic() {\n173\t  echo \"[3/8] Basic completion — capital of France ...\"\n174\t  local resp\n175\t  resp=\"$(curl -sf -m 30 \"${URL}/v1/chat/completions\" \\\n176\t    -H \"Content-Type: application/json\" \\\n177\t    -d \"{\n178\t      \\\"model\\\": \\\"${MODEL}\\\",\n179\t      \\\"messages\\\": [{\\\"role\\\": \\\"user\\\", \\\"content\\\": \\\"What is the capital of France? One short sentence.\\\"}],\n180\t      \\\"max_tokens\\\": 30,\n181\t      \\\"temperature\\\": 0.6,\n182\t      \\\"chat_template_kwargs\\\": {\\\"enable_thinking\\\": false}\n183\t    }\")\" || { fail \"completion request failed\" \"Check docker logs ${CONTAINER}\"; return 1; }\n184\t  local content\n185\t  content=\"$(echo \"$resp\" | python3 -c \"import sys,json; print(json.load(sys.stdin)['choices'][0]['message']['content'])\" 2>/dev/null || true)\"\n186\t  if echo \"$content\" | grep -qi \"Paris\"; then\n187\t    pass \"reply contains 'Paris'\"\n188\t  else\n189\t    fail \"reply didn't mention Paris: $(echo \"$content\" | head -c 80)\" \\\n190\t         \"Model may be loading badly or wrong chat template.\"\n191\t  fi\n192\t}\n193\trun_check \"basic\" check_basic\n194\t\n195\t# --------------------------------------------------------------------\n196\t# 4. Tool calling\n197\t# --------------------------------------------------------------------\n198\tcheck_tools() {\n199\t  echo \"[4/8] Tool calling ...\"\n200\t  if [[ \"${SKIP_TOOLS:-0}\" == \"1\" ]]; then\n201\t    skip \"SKIP_TOOLS=1 (expected for default config — see README Known issue)\"\n202\t    return 0\n203\t  fi\n204\t  local resp\n205\t  resp=\"$(curl -sf -m 60 \"${URL}/v1/chat/completions\" \\\n206\t    -H \"Content-Type: application/json\" \\\n207\t    -d \"{\n208\t      \\\"model\\\": \\\"${MODEL}\\\",\n209\t      \\\"messages\\\": [{\\\"role\\\": \\\"user\\\", \\\"content\\\": \\\"What is the weather in San Francisco? Use the get_weather tool.\\\"}],\n210\t      \\\"tools\\\": [{\\\"type\\\":\\\"function\\\",\\\"function\\\":{\\\"name\\\":\\\"get_weather\\\",\\\"description\\\":\\\"Get weather for a city.\\\",\\\"parameters\\\":{\\\"type\\\":\\\"object\\\",\\\"properties\\\":{\\\"city\\\":{\\\"type\\\":\\\"string\\\"}},\\\"required\\\":[\\\"city\\\"]}}}],\n211\t      \\\"tool_choice\\\": \\\"auto\\\", \\\"max_tokens\\\": 200, \\\"temperature\\\": 0.3,\n212\t      \\\"chat_template_kwargs\\\": {\\\"enable_thinking\\\": false}\n213\t    }\")\" || { fail \"tool-call request failed\" \"Check docker logs\"; return 1; }\n214\t  local tool_calls\n215\t  tool_calls=\"$(echo \"$resp\" | python3 -c \"\n216\timport sys, json\n217\ttry:\n218\t    d = json.load(sys.stdin)\n219\t    tc = d['choices'][0]['message'].get('tool_calls')\n220\t    if tc:\n221\t        print(json.dumps(tc, indent=2))\n222\t    else:\n223\t        content = d['choices'][0]['message'].get('content') or ''\n224\t        if '<tool_call>' in content:\n225\t            print('__INLINED__')\n226\t        else:\n227\t            print('__NONE__')\n228\texcept Exception as e:\n229\t    print(f'__PARSE_ERROR__: {e}')\n230\t\" 2>&1)\"\n231\t  if echo \"$tool_calls\" | grep -q \"__INLINED__\"; then\n232\t    fail \"model emitted <tool_call> as inline text (tool_calls[] empty)\" \\\n233\t         \"Known issue: MTP × TurboQuant incompat. Use docker-compose.tools.yml or .tools-text.yml. See README Known issues.\"\n234\t  elif echo \"$tool_calls\" | grep -qi \"get_weather\"; then\n235\t    pass \"tool_calls[] populated with get_weather\"\n236\t  else\n237\t    fail \"unexpected tool_calls structure\" \"Raw: $(echo \"$tool_calls\" | head -c 300)\"\n238\t  fi\n239\t}\n240\trun_check \"tools\" check_tools\n241\t\n242\t# --------------------------------------------------------------------\n243\t# 5. Streaming — SSE chunks add up to coherent text\n244\t# --------------------------------------------------------------------\n245\tcheck_streaming() {\n246\t  echo \"[5/8] Streaming (SSE) ...\"\n247\t  # Collect streamed chunks for 15 seconds max\n248\t  local stream_out\n249\t  stream_out=\"$(curl -sf -m 45 --no-buffer \"${URL}/v1/chat/completions\" \\\n250\t    -H \"Content-Type: application/json\" \\\n251\t    -d \"{\n252\t      \\\"model\\\": \\\"${MODEL}\\\",\n253\t      \\\"messages\\\": [{\\\"role\\\": \\\"user\\\", \\\"content\\\": \\\"Write a three-sentence haiku about debugging.\\\"}],\n254\t      \\\"max_tokens\\\": 120,\n255\t      \\\"temperature\\\": 0.6,\n256\t      \\\"stream\\\": true,\n257\t      \\\"chat_template_kwargs\\\": {\\\"enable_thinking\\\": false}\n258\t    }\" 2>/dev/null)\" || { fail \"streaming request failed\" \"Check docker logs\"; return 1; }\n259\t\n260\t  local text chunks\n261\t  text=\"$(echo \"$stream_out\" | python3 -c \"\n262\timport sys, json\n263\ttext = ''\n264\tchunks = 0\n265\tfor line in sys.stdin:\n266\t    line = line.strip()\n267\t    if not line or not line.startswith('data: '):\n268\t        continue\n269\t    payload = line[6:]\n270\t    if payload == '[DONE]':\n271\t        break\n272\t    try:\n273\t        d = json.loads(payload)\n274\t        delta = d['choices'][0].get('delta', {}).get('content') or ''\n275\t        if delta:\n276\t            text += delta\n277\t            chunks += 1\n278\t    except Exception:\n279\t        pass\n280\tprint(f'{chunks}||{text}')\n281\t\" 2>/dev/null)\"\n282\t  chunks=\"${text%%||*}\"\n283\t  local final_text=\"${text#*||}\"\n284\t  if [[ -z \"$final_text\" ]] || [[ \"$chunks\" == \"0\" ]]; then\n285\t    fail \"no streaming content received ($chunks chunks)\" \\\n286\t         \"Streaming broken — check that vLLM isn't buffering. stream_out head: $(echo \"$stream_out\" | head -c 200)\"\n287\t  elif [[ \"$chunks\" -lt 5 ]]; then\n288\t    fail \"suspiciously few chunks ($chunks) for 120 max_tokens\" \\\n289\t         \"SSE may be buffering. Final text: $(echo \"$final_text\" | head -c 120)\"\n290\t  elif [[ ${#final_text} -lt 20 ]]; then\n291\t    fail \"streamed text too short (${#final_text} chars)\" \\\n292\t         \"Content: $final_text\"\n293\t  else\n294\t    pass \"streamed $chunks chunks, ${#final_text} chars:  $(echo \"$final_text\" | head -c 80 | tr '\\n' ' ')...\"\n295\t  fi\n296\t}\n297\trun_check \"streaming\" check_streaming\n298\t\n299\t# --------------------------------------------------------------------\n300\t# 6. Thinking mode — reasoning + content both populated\n301\t# --------------------------------------------------------------------\n302\tcheck_thinking() {\n303\t  echo \"[6/8] Thinking / reasoning mode ...\"\n304\t  local resp\n305\t  # enable_thinking: true (Qwen3 default). Math problem that needs visible reasoning.\n306\t  resp=\"$(curl -sf -m 120 \"${URL}/v1/chat/completions\" \\\n307\t    -H \"Content-Type: application/json\" \\\n308\t    -d \"{\n309\t      \\\"model\\\": \\\"${MODEL}\\\",\n310\t      \\\"messages\\\": [{\\\"role\\\": \\\"user\\\", \\\"content\\\": \\\"What is 2+2? One-line answer.\\\"}],\n311\t      \\\"max_tokens\\\": 4000,\n312\t      \\\"temperature\\\": 0.3,\n313\t      \\\"chat_template_kwargs\\\": {\\\"enable_thinking\\\": true}\n314\t    }\")\" || { fail \"thinking request failed\" \"Check docker logs\"; return 1; }\n315\t  local analyzed\n316\t  analyzed=\"$(echo \"$resp\" | python3 -c \"\n317\timport sys, json\n318\td = json.load(sys.stdin)\n319\tmsg = d['choices'][0]['message']\n320\treasoning = msg.get('reasoning') or ''\n321\tcontent = msg.get('content') or ''\n322\tfinish = d['choices'][0].get('finish_reason')\n323\tprint(f'{len(reasoning)}|{len(content)}|{finish}|{(reasoning[:60] or \\\"(empty)\\\").replace(chr(10), \\\" \\\")}|{(content[:60] or \\\"(empty)\\\").replace(chr(10), \\\" \\\")}')\n324\t\" 2>/dev/null)\"\n325\t  IFS='|' read -r r_len c_len fin r_head c_head <<< \"$analyzed\"\n326\t  if [[ -z \"$r_len\" ]]; then\n327\t    fail \"couldn't parse thinking response\" \"$(echo \"$resp\" | head -c 300)\"\n328\t  elif [[ \"$r_len\" == \"0\" ]]; then\n329\t    fail \"reasoning field empty (thinking mode didn't engage)\" \\\n330\t         \"May indicate Genesis Patch 12 didn't land or chat_template_kwargs not honored. content='$c_head'\"\n331\t  elif [[ \"$c_len\" == \"0\" ]] && [[ \"$fin\" == \"length\" ]]; then\n332\t    # Reasoning populated but model didn't finish before max_tokens — thinking\n333\t    # mode is working (reasoning field extracted cleanly), just verbose.\n334\t    pass \"reasoning $r_len chars (model kept thinking, hit max_tokens before finishing — Qwen3.6 is verbose; thinking IS extracting correctly)\"\n335\t    printf \"    \\033[2mreasoning head:\\033[0m %s...\\n\" \"$r_head\"\n336\t  elif [[ \"$c_len\" == \"0\" ]]; then\n337\t    fail \"reasoning present but content empty, finish=$fin (not length)\" \\\n338\t         \"Likely genuine stall — finish_reason should be length if it's just verbosity. reasoning: $r_head\"\n339\t  elif [[ \"$r_len\" -lt 50 ]]; then\n340\t    fail \"reasoning suspiciously short ($r_len chars)\" \"reasoning: $r_head\"\n341\t  else\n342\t    pass \"reasoning $r_len chars, content $c_len chars (finish=$fin)\"\n343\t    printf \"    \\033[2mreasoning:\\033[0m %s...\\n\" \"$r_head\"\n344\t    printf \"    \\033[2mcontent:  \\033[0m %s...\\n\" \"$c_head\"\n345\t  fi\n346\t}\n347\trun_check \"thinking\" check_thinking\n348\t# --------------------------------------------------------------------\n349\t# 9. Output quality / cascade detection — 2K-token completion, scan\n350\t#    for the silent <tool_call> inline cascade (MTP × TurboQuant bug)\n351\t#    and for repetitive degeneracy (stale-draft / sampling collapse).\n352\t# --------------------------------------------------------------------\n353\tcheck_output_quality() {\n354\t  echo \"[7/8] Output quality / cascade detection (2K-token completion) ...\"\n355\t  local resp\n356\t  resp=\"$(curl -sf -m 180 \"${URL}/v1/chat/completions\" \\\n357\t    -H \"Content-Type: application/json\" \\\n358\t    -d \"{\n359\t      \\\"model\\\": \\\"${MODEL}\\\",\n360\t      \\\"messages\\\": [{\\\"role\\\": \\\"user\\\", \\\"content\\\": \\\"Write a detailed 1500-word essay explaining how transformer attention works. Cover: query/key/value projections, scaled dot-product attention, softmax, multi-head attention, positional encodings, and a brief comparison with RNN-based attention.\\\"}],\n361\t      \\\"max_tokens\\\": 2000,\n362\t      \\\"temperature\\\": 0.6,\n363\t      \\\"chat_template_kwargs\\\": {\\\"enable_thinking\\\": false}\n364\t    }\")\" || { fail \"output quality request failed\" \"Check docker logs ${CONTAINER}\"; return 1; }\n365\t\n366\t  local analysis\n367\t  analysis=\"$(echo \"$resp\" | python3 -c \"\n368\timport sys, json, re\n369\ttry:\n370\t    d = json.load(sys.stdin)\n371\t    c = d['choices'][0]['message'].get('content') or ''\n372\t    finish = d['choices'][0].get('finish_reason') or 'n/a'\n373\t    clen = len(c)\n374\t    cascade = 'tool_call_cascade' if '<tool_call>' in c else 'none'\n375\t    # Repetitive cascade: same non-empty line appearing >=5 times consecutively\n376\t    lines = [l.strip() for l in c.split('\\n') if l.strip()]\n377\t    max_repeat, cur_line, cur_count = 0, '', 0\n378\t    for l in lines:\n379\t        if l == cur_line:\n380\t            cur_count += 1\n381\t            max_repeat = max(max_repeat, cur_count)\n382\t        else:\n383\t            cur_line, cur_count = l, 1\n384\t    # Lexical variety over the first 200 words (samples coherence)\n385\t    words = re.findall(r\\\"[A-Za-z']+\\\", c.lower())\n386\t    sample = words[:200]\n387\t    variety = (len(set(sample)) / len(sample)) if sample else 0.0\n388\t    print(f'{clen}|{cascade}|{max_repeat}|{variety:.3f}|{finish}')\n389\texcept Exception as e:\n390\t    print(f'err|{e}|0|0|n/a')\n391\t\" 2>/dev/null)\"\n392\t\n393\t  IFS='|' read -r clen cascade max_repeat variety finish <<< \"$analysis\"\n394\t  if [[ \"$clen\" == \"err\" ]]; then\n395\t    fail \"couldn't parse response: $cascade\" \"$(echo \"$resp\" | head -c 200)\"\n396\t  elif [[ \"${clen:-0}\" == \"0\" ]]; then\n397\t    fail \"empty completion (finish=${finish})\" \"Likely silent generation failure\"\n398\t  elif [[ \"$cascade\" == \"tool_call_cascade\" ]]; then\n399\t    fail \"MTP × TurboQuant cascade — <tool_call> emitted in normal text\" \\\n400\t         \"Genesis P64/P65 not active or compose using broken MTP path. See README Known issues.\"\n401\t  elif [[ \"${max_repeat:-0}\" -ge 5 ]]; then\n402\t    fail \"repetitive degeneracy — line repeats ${max_repeat}× consecutively\" \\\n403\t         \"Sampling collapsed (stale-draft? sampler bug?). Check finish_reason=${finish}, vLLM ngram/spec settings.\"\n404\t  elif python3 -c \"import sys; sys.exit(0 if float('${variety:-0}') >= 0.30 else 1)\" 2>/dev/null; then\n405\t    pass \"output OK — ${clen} chars, variety=${variety}, max_line_repeat=${max_repeat}, finish=${finish}\"\n406\t  else\n407\t    fail \"low lexical variety (${variety}, threshold 0.30)\" \\\n408\t         \"Possible degenerate output. clen=${clen}, finish=${finish}\"\n409\t  fi\n410\t}\n411\trun_check \"output_quality\" check_output_quality\n412\t\n413\t# --------------------------------------------------------------------\n414\t# 10. MTP acceptance length — assert spec-decode is contributing speedup.\n415\t#     Mean AL >= 2.0 means each step accepts >=1 drafted token on average\n416\t#     (target_only baseline = 1.0). Production sees AL 3.4-3.8 with n=3.\n417\t# --------------------------------------------------------------------\n418\tcheck_mtp_acceptance() {\n419\t  echo \"[8/8] MTP acceptance length threshold ...\"\n420\t  # Spec-decode metrics extraction is engine-specific:\n421\t  #   vLLM emits \"SpecDecoding metrics: Mean acceptance length: N.NN\" to stdout\n422\t  #   llama.cpp llama-server doesn't emit a \"Mean acceptance length\" line; spec\n423\t  #     metrics are inferred from per-slot accept counts in the response timings\n424\t  #     (engine-internal, not exposed via OpenAI API)\n425\t  #   SGLang has its own format\n426\t  # For non-vLLM engines we skip rather than fail — the per-engine spec-decode\n427\t  # validation is the user's responsibility (e.g. llama.cpp users run their own\n428\t  # verify-full-mtp.sh adaptations like @lamentofhighborne's, until #87 lands a\n429\t  # generalized harness).\n430\t  case \"$ENGINE_KIND\" in\n431\t    llamacpp) skip \"llama.cpp engine — MTP acceptance check is vLLM-log-format-specific (run engine-side verification separately)\"; return 0 ;;\n432\t    sglang)   skip \"SGLang engine — MTP acceptance check is vLLM-log-format-specific\";   return 0 ;;\n433\t  esac\n434\t  if ! command -v docker >/dev/null 2>&1; then\n435\t    skip \"docker not in PATH (host engine build? — see #87 for generalized harness work)\"\n436\t    return 0\n437\t  fi\n438\t  if ! docker inspect \"${CONTAINER}\" >/dev/null 2>&1; then\n439\t    skip \"container '${CONTAINER}' not found (CONTAINER=none for host endpoints)\"\n440\t    return 0\n441\t  fi\n442\t\n443\t  # Trigger a fresh decode to populate metrics (some vLLM builds only emit\n444\t  # SpecDecoding stats after a non-trivial generation completes).\n445\t  curl -sf -m 60 \"${URL}/v1/chat/completions\" \\\n446\t    -H \"Content-Type: application/json\" \\\n447\t    -d \"{\n448\t      \\\"model\\\": \\\"${MODEL}\\\",\n449\t      \\\"messages\\\": [{\\\"role\\\": \\\"user\\\", \\\"content\\\": \\\"Count from 1 to 80, one number per line.\\\"}],\n450\t      \\\"max_tokens\\\": 500,\n451\t      \\\"temperature\\\": 0.0,\n452\t      \\\"chat_template_kwargs\\\": {\\\"enable_thinking\\\": false}\n453\t    }\" >/dev/null 2>&1 || { fail \"metrics-trigger request failed\" \"Check docker logs\"; return 1; }\n454\t  sleep 3  # let log line flush\n455\t\n456\t  local recent\n457\t  recent=\"$(docker logs --tail 200 \"${CONTAINER}\" 2>&1 | grep -iE \"SpecDecoding|acceptance length|spec_decode\" | tail -3)\"\n458\t  if [[ -z \"$recent\" ]]; then\n459\t    skip \"no SpecDecoding metrics in logs (compose may not have spec-decode enabled)\"\n460\t    return 0\n461\t  fi\n462\t\n463\t  local al\n464\t  al=\"$(echo \"$recent\" | grep -oiE \"(mean acceptance length|acceptance length|al|mean_acceptance_length)[: ]+[0-9]+\\.[0-9]+\" \\\n465\t        | grep -oE \"[0-9]+\\.[0-9]+\" | tail -1)\"\n466\t  if [[ -z \"$al\" ]]; then\n467\t    skip \"couldn't parse AL from: $(echo \"$recent\" | head -c 240 | tr '\\n' ' ')\"\n468\t    return 0\n469\t  fi\n470\t\n471\t  if python3 -c \"import sys; sys.exit(0 if float('$al') >= 2.0 else 1)\" 2>/dev/null; then\n472\t    pass \"MTP acceptance length = ${al} (>=2.0 — spec-decode contributing)\"\n473\t  else\n474\t    fail \"MTP acceptance length = ${al} (<2.0 — spec-decode degraded or off)\" \\\n475\t         \"Either MTP routing broken (P65 not active?) or accept rate collapsed. Check spec_decode kernel + Genesis env vars.\"\n476\t  fi\n477\t}\n478\trun_check \"mtp\" check_mtp_acceptance\n479\t\n480\techo \"\"\n481\tif [[ \"$FAILED\" == \"0\" ]]; then\n482\t  printf \"\\033[32mAll checks passed.\\033[0m Stack is ready for full-functionality use.\\n\"\n483\telse\n484\t  printf \"\\033[31m%d check(s) failed.\\033[0m See hints above.\\n\" \"$FAILED\"\n485\tfi\n486\t\n487\tif [[ \"$RUN_BENCH\" == \"1\" && \"$FAILED\" == \"0\" ]]; then\n488\t  echo \"\"\n489\t  echo \"==========================================\"\n490\t  echo \"  --bench: running scripts/bench.sh\"\n491\t  echo \"==========================================\"\n492\t  SCRIPT_DIR=\"$(cd -- \"$(dirname -- \"${BASH_SOURCE[0]}\")\" && pwd)\"\n493\t  URL=\"${URL}\" MODEL=\"${MODEL}\" CONTAINER=\"${CONTAINER}\" \\\n494\t    bash \"${SCRIPT_DIR}/bench.sh\"\n495\tfi\n496\t\n497\texit \"$FAILED\"\n",
    "chars": 24325
  }
]
